node_modules/
.next/
next-env.d.ts
*.tsbuildinfo
.vercel/
# the kernel, a build output (../c Makefile, wasm-web): built by the pre-hooks
# and by .github/workflows/uttt-web.yml, never committed
public/uttt.wasm
