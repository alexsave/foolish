--------------------------------------------------------------------------------
Profile data file 'bench_results/T1e_epoll_ws_lines/raw/callgrind.out' (creator: callgrind-3.22.0)
--------------------------------------------------------------------------------
I1 cache: 
D1 cache: 
LL cache: 
Timerange: Basic block 0 - 20715892
Trigger: Program termination
Profiled target:  ./foolish_server_prof 8402 --no-db --game-workers=4 (PID 26062, part 1)
Events recorded:  Ir
Events shown:     Ir
Event sort order: Ir
Thresholds:       99
Include dirs:     
User annotated:   
Auto-annotation:  on

--------------------------------------------------------------------------------
Ir                  
--------------------------------------------------------------------------------
91,243,096 (100.0%)  PROGRAM TOTALS

--------------------------------------------------------------------------------
Ir                   file:function
--------------------------------------------------------------------------------
19,005,461 (20.83%)  foolish_server.c:wsasync_feed [/home/user/foolish/server/impls/native/foolish_server_prof]
11,981,541 (13.13%)  foolish_server.c:epoll_worker_main [/home/user/foolish/server/impls/native/foolish_server_prof]
 8,826,933 ( 9.67%)  ws.c:ws_send_frame [/home/user/foolish/server/impls/native/foolish_server_prof]
 8,809,452 ( 9.65%)  ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms [/usr/lib/x86_64-linux-gnu/libc.so.6]
 6,121,830 ( 6.71%)  foolish_server.c:ws_service_message [/home/user/foolish/server/impls/native/foolish_server_prof]
 4,610,000 ( 5.05%)  ./nptl/./nptl/cancellation.c:__pthread_enable_asynccancel [/usr/lib/x86_64-linux-gnu/libc.so.6]
 4,364,534 ( 4.78%)  foolish_server.c:econn_try_flush [/home/user/foolish/server/impls/native/foolish_server_prof]
 4,321,785 ( 4.74%)  ./nptl/./nptl/cancellation.c:__pthread_disable_asynccancel [/usr/lib/x86_64-linux-gnu/libc.so.6]
 3,659,260 ( 4.01%)  ./nptl/./nptl/pthread_mutex_lock.c:pthread_mutex_lock@@GLIBC_2.2.5 [/usr/lib/x86_64-linux-gnu/libc.so.6]
 3,350,673 ( 3.67%)  conn.c:conn_write [/home/user/foolish/server/impls/native/foolish_server_prof]
 2,643,049 ( 2.90%)  ./nptl/./nptl/pthread_mutex_unlock.c:pthread_mutex_unlock@@GLIBC_2.2.5 [/usr/lib/x86_64-linux-gnu/libc.so.6]
 2,436,944 ( 2.67%)  ./io/../sysdeps/unix/sysv/linux/write.c:write [/usr/lib/x86_64-linux-gnu/libc.so.6]
 2,426,760 ( 2.66%)  ./io/../sysdeps/unix/sysv/linux/read.c:read [/usr/lib/x86_64-linux-gnu/libc.so.6]
 2,292,105 ( 2.51%)  ./misc/../sysdeps/unix/sysv/linux/epoll_wait.c:epoll_wait [/usr/lib/x86_64-linux-gnu/libc.so.6]
 1,039,660 ( 1.14%)  ./string/../sysdeps/x86_64/multiarch/../multiarch/memset-vec-unaligned-erms.S:memset [/usr/lib/x86_64-linux-gnu/ld-linux-x86-64.so.2]
 1,014,590 ( 1.11%)  ws.c:ws_conn_init [/home/user/foolish/server/impls/native/foolish_server_prof]
   576,250 ( 0.63%)  ./nptl/../nptl/descr.h:__pthread_enable_asynccancel
   532,297 ( 0.58%)  ./string/../sysdeps/x86_64/multiarch/memset-vec-unaligned-erms.S:__memset_avx2_unaligned_erms [/usr/lib/x86_64-linux-gnu/libc.so.6]
   405,812 ( 0.44%)  ./conn.h:epoll_worker_main
   364,208 ( 0.40%)  ./elf/./elf/dl-lookup.c:do_lookup_x [/usr/lib/x86_64-linux-gnu/ld-linux-x86-64.so.2]
   256,362 ( 0.28%)  ../../../c/src/view.c:state_put [/home/user/foolish/server/impls/native/foolish_server_prof]
   204,617 ( 0.22%)  ./elf/../sysdeps/x86_64/dl-machine.h:_dl_relocate_object
   141,913 ( 0.16%)  ./elf/./elf/do-rel.h:_dl_relocate_object
   138,979 ( 0.15%)  foolish_server.c:main [/home/user/foolish/server/impls/native/foolish_server_prof]
   116,066 ( 0.13%)  ../../../c/src/../wasm/wire.h:state_put
   101,459 ( 0.11%)  ./conn.h:ws_send_frame
    90,350 ( 0.10%)  ./elf/./elf/dl-lookup.c:_dl_lookup_symbol_x [/usr/lib/x86_64-linux-gnu/ld-linux-x86-64.so.2]
    87,374 ( 0.10%)  ./elf/../sysdeps/generic/dl-new-hash.h:_dl_lookup_symbol_x
    85,324 ( 0.09%)  ./string/../sysdeps/x86_64/multiarch/../multiarch/strcmp-sse2.S:strcmp [/usr/lib/x86_64-linux-gnu/ld-linux-x86-64.so.2]
    79,727 ( 0.09%)  ./elf/./elf/dl-tunables.c:__GI___tunables_init [/usr/lib/x86_64-linux-gnu/ld-linux-x86-64.so.2]
    60,127 ( 0.07%)  ./elf/./elf/dl-lookup.c:check_match [/usr/lib/x86_64-linux-gnu/ld-linux-x86-64.so.2]
    57,551 ( 0.06%)  ./elf/./elf/dl-reloc.c:_dl_relocate_object [/usr/lib/x86_64-linux-gnu/ld-linux-x86-64.so.2]
    56,615 ( 0.06%)  ./nptl/./nptl/pthread_cond_wait.c:pthread_cond_wait@@GLIBC_2.3.2 [/usr/lib/x86_64-linux-gnu/libc.so.6]
    54,279 ( 0.06%)  ../../../c/src/game.c:handle_attack [/home/user/foolish/server/impls/native/foolish_server_prof]
    52,644 ( 0.06%)  ws.c:sha1_update [/home/user/foolish/server/impls/native/foolish_server_prof]

--------------------------------------------------------------------------------
-- Auto-annotated source: ws.c
--------------------------------------------------------------------------------
Ir                 

-- line 18 ----------------------------------------
        .           
        .           typedef struct {
        .               uint32_t state[5];
        .               uint64_t bitlen;
        .               uint8_t  buf[64];
        .               int      buflen;
        .           } Sha1;
        .           
    3,648 ( 0.00%)  static uint32_t sha1_rol(uint32_t v, int n) { return (v << n) | (v >> (32 - n)); }
        .           
        .           static void sha1_block(Sha1 *c, const uint8_t *p) {
        .               uint32_t w[80];
      576 ( 0.00%)      for (int i = 0; i < 16; i++)
      192 ( 0.00%)          w[i] = ((uint32_t)p[i * 4] << 24) | ((uint32_t)p[i * 4 + 1] << 16) |
      384 ( 0.00%)                 ((uint32_t)p[i * 4 + 2] << 8) | (uint32_t)p[i * 4 + 3];
    6,144 ( 0.01%)      for (int i = 16; i < 80; i++) w[i] = sha1_rol(w[i - 3] ^ w[i - 8] ^ w[i - 14] ^ w[i - 16], 1);
        .           
       60 ( 0.00%)      uint32_t a = c->state[0], b = c->state[1], cc = c->state[2], d = c->state[3], e = c->state[4];
    2,880 ( 0.00%)      for (int i = 0; i < 80; i++) {
        .                   uint32_t f, k;
    2,880 ( 0.00%)          if (i < 20)      { f = (b & cc) | ((~b) & d);       k = 0x5A827999u; }
    2,160 ( 0.00%)          else if (i < 40) { f = b ^ cc ^ d;                  k = 0x6ED9EBA1u; }
    2,400 ( 0.00%)          else if (i < 60) { f = (b & cc) | (b & d) | (cc & d); k = 0x8F1BBCDCu; }
      720 ( 0.00%)          else             { f = b ^ cc ^ d;                  k = 0xCA62C1D6u; }
    3,840 ( 0.00%)          uint32_t t = sha1_rol(a, 5) + f + e + k + w[i];
        .                   e = d; d = cc; cc = sha1_rol(b, 30); b = a; a = t;
        .               }
      120 ( 0.00%)      c->state[0] += a; c->state[1] += b; c->state[2] += cc; c->state[3] += d; c->state[4] += e;
        .           }
        .           
        .           static void sha1_init(Sha1 *c) {
       12 ( 0.00%)      c->state[0] = 0x67452301u; c->state[1] = 0xEFCDAB89u; c->state[2] = 0x98BADCFEu;
        6 ( 0.00%)      c->state[3] = 0x10325476u; c->state[4] = 0xC3D2E1F0u;
       18 ( 0.00%)      c->bitlen = 0; c->buflen = 0;
        .           }
        .           
        .           static void sha1_update(Sha1 *c, const void *data, size_t len) {
        .               const uint8_t *p = (const uint8_t *)data;
      744 ( 0.00%)      c->bitlen += (uint64_t)len * 8;
    5,580 ( 0.01%)      while (len > 0) {
    2,232 ( 0.00%)          size_t want = (size_t)(64 - c->buflen);
      744 ( 0.00%)          size_t take = len < want ? len : want;
    2,232 ( 0.00%)          memcpy(c->buf + c->buflen, p, take);
    5,910 ( 0.01%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (372x)
      744 ( 0.00%)          c->buflen += (int)take; p += take; len -= take;
      780 ( 0.00%)          if (c->buflen == 64) { sha1_block(c, c->buf); c->buflen = 0; }
        .               }
      372 ( 0.00%)  }
        .           
        .           static void sha1_final(Sha1 *c, uint8_t out[20]) {
        6 ( 0.00%)      uint64_t bits = c->bitlen;
       30 ( 0.00%)      uint8_t pad = 0x80; sha1_update(c, &pad, 1);
      372 ( 0.00%)  => ws.c:sha1_update (6x)
    2,142 ( 0.00%)      pad = 0x00; while (c->buflen != 56) sha1_update(c, &pad, 1);
   39,714 ( 0.04%)  => ws.c:sha1_update (354x)
        6 ( 0.00%)      c->bitlen = bits;   // padding must not count toward the length field
        .               uint8_t lenbe[8];
       12 ( 0.00%)      for (int i = 0; i < 8; i++) lenbe[i] = (uint8_t)(bits >> (56 - 8 * i));
       12 ( 0.00%)      sha1_update(c, lenbe, 8);
   18,120 ( 0.02%)  => ws.c:sha1_update (6x)
        .               for (int i = 0; i < 5; i++) {
       30 ( 0.00%)          out[i * 4]     = (uint8_t)(c->state[i] >> 24);
        .                   out[i * 4 + 1] = (uint8_t)(c->state[i] >> 16);
        .                   out[i * 4 + 2] = (uint8_t)(c->state[i] >> 8);
       60 ( 0.00%)          out[i * 4 + 3] = (uint8_t)(c->state[i]);
        .               }
        .           }
        .           
       60 ( 0.00%)  void ws_sha1(const unsigned char *data, size_t len, unsigned char out[20]) {
       12 ( 0.00%)      Sha1 c; sha1_init(&c); sha1_update(&c, data, len); sha1_final(&c, out);
      348 ( 0.00%)  => ws.c:sha1_update (6x)
       42 ( 0.00%)  }
        .           
        .           // ---------------------------------------------------------------------------
        .           // base64
        .           // ---------------------------------------------------------------------------
        .           
        .           int ws_base64_encode(const unsigned char *data, int len, char *out, int cap) {
        .               static const char tbl[] = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/";
        .               int oi = 0;
        .               for (int i = 0; i < len; i += 3) {
       84 ( 0.00%)          if (oi + 4 >= cap) return 0;
      126 ( 0.00%)          unsigned int v = (unsigned int)data[i] << 16;
       90 ( 0.00%)          if (i + 1 < len) v |= (unsigned int)data[i + 1] << 8;
       72 ( 0.00%)          if (i + 2 < len) v |= (unsigned int)data[i + 2];
      132 ( 0.00%)          out[oi++] = tbl[(v >> 18) & 0x3F];
      168 ( 0.00%)          out[oi++] = tbl[(v >> 12) & 0x3F];
      162 ( 0.00%)          out[oi++] = (i + 1 < len) ? tbl[(v >> 6) & 0x3F] : '=';
      120 ( 0.00%)          out[oi++] = (i + 2 < len) ? tbl[v & 0x3F] : '=';
        .               }
        .               if (oi >= cap) return 0;
        .               out[oi] = 0;
        .               return oi;
        .           }
        .           
       54 ( 0.00%)  int ws_accept_from_key(const char *client_key, char *out, int cap) {
        .               static const char GUID[] = "258EAFA5-E914-47DA-95CA-C5AB0DC85B11";
        .               char buf[256];
       48 ( 0.00%)      int n = snprintf(buf, sizeof buf, "%s%s", client_key, GUID);
    3,342 ( 0.00%)  => ./stdio-common/./stdio-common/snprintf.c:snprintf (6x)
       18 ( 0.00%)      if (n <= 0 || n >= (int)sizeof buf) return 0;
        .               unsigned char digest[20];
        6 ( 0.00%)      ws_sha1((const unsigned char *)buf, (size_t)n, digest);
   61,026 ( 0.07%)  => ws.c:ws_sha1 (6x)
        .               return ws_base64_encode(digest, 20, out, cap);
       42 ( 0.00%)  }
        .           
        .           // ---------------------------------------------------------------------------
        .           // Reliable read/write
        .           // ---------------------------------------------------------------------------
        .           
        .           // EINTR is already handled inside conn_read/conn_write (conn.c) for both
        .           // the plaintext and TLS cases — this loop only needs to keep going on a
        .           // genuine short read/write, which is normal POSIX (and SSL_read/SSL_write)
-- line 124 ----------------------------------------
-- line 133 ----------------------------------------
        .                   got += (int)r;
        .               }
        .               return got;
        .           }
        .           
        .           int ws_write_full(Conn *c, const void *buf, int n) {
        .               const unsigned char *p = (const unsigned char *)buf;
        .               int sent = 0;
  608,754 ( 0.67%)      while (sent < n) {
  507,295 ( 0.56%)          ssize_t w = conn_write(c, p + sent, (size_t)(n - sent));
4,565,681 ( 5.00%)  => /home/user/foolish/server/impls/native/conn.c:conn_write (101,459x)
  608,754 ( 0.67%)          if (w < 0) return -1;
        .                   if (w == 0) return -1;
        .                   sent += (int)w;
        .               }
        .               return sent;
        .           }
        .           
        .           // Reliable writev(): loops on a short write by advancing past fully-written
        .           // iovecs and trimming a partially-written one, exactly like ws_write_full
-- line 151 ----------------------------------------
-- line 174 ----------------------------------------
        .               }
        .               return total;
        .           }
        .           
        .           // ---------------------------------------------------------------------------
        .           // WsConn: buffered read (drains `pending` before touching the socket again)
        .           // ---------------------------------------------------------------------------
        .           
  202,918 ( 0.22%)  void ws_conn_init(WsConn *c, Conn conn, int mask_outgoing) {
  405,836 ( 0.44%)      c->conn = conn;
  101,459 ( 0.11%)      c->mask_outgoing = mask_outgoing;
  101,459 ( 0.11%)      c->pending_len = 0;
        .               c->pending_off = 0;
  202,918 ( 0.22%)  }
        .           
        .           void ws_conn_prime(WsConn *c, const unsigned char *data, int len) {
        .               if (len <= 0) return;
        .               int cap = (int)sizeof c->pending;
        .               if (len > cap) len = cap;   // realistic peers never pipeline frames ahead of the handshake
        .               memcpy(c->pending, data, (size_t)len);
        .               c->pending_len = len;
        .               c->pending_off = 0;
-- line 195 ----------------------------------------
-- line 233 ----------------------------------------
        .           }
        .           
        .           // ---------------------------------------------------------------------------
        .           // Frame write: 0..125 / 126 (+u16) / 127 (+u64) length encoding, masked iff
        .           // c->mask_outgoing (RFC 6455 5.1: client->server MUST be masked, server->
        .           // client MUST NOT be).
        .           // ---------------------------------------------------------------------------
        .           
1,116,049 ( 1.22%)  int ws_send_frame(WsConn *c, int opcode, const unsigned char *payload, int64_t len) {
        .               unsigned char hdr[14];
        .               int hn = 0;
  304,377 ( 0.33%)      hdr[hn++] = (unsigned char)(0x80 | (opcode & 0x0F));   // FIN=1
  608,754 ( 0.67%)      unsigned char maskbit = c->mask_outgoing ? 0x80 : 0x00;
  202,918 ( 0.22%)      if (len <= 125) {
  101,459 ( 0.11%)          hdr[hn++] = (unsigned char)(maskbit | (unsigned char)len);
        .               } else if (len <= 0xFFFF) {
        .                   hdr[hn++] = (unsigned char)(maskbit | 126);
        .                   hdr[hn++] = (unsigned char)((len >> 8) & 0xFF);
        .                   hdr[hn++] = (unsigned char)(len & 0xFF);
        .               } else {
        .                   hdr[hn++] = (unsigned char)(maskbit | 127);
        .                   for (int i = 7; i >= 0; i--) hdr[hn++] = (unsigned char)((len >> (8 * i)) & 0xFF);
        .               }
  101,459 ( 0.11%)      unsigned char mkey[4] = {0, 0, 0, 0};
  101,459 ( 0.11%)      if (c->mask_outgoing) {
        .                   for (int i = 0; i < 4; i++) mkey[i] = (unsigned char)(rand() & 0xFF);
        .                   memcpy(hdr + hn, mkey, 4); hn += 4;
        .               }
        .               if (!c->mask_outgoing) {
        .                   // Server (unmasked) path — the measured hot one (PROFILE_HOTPATH.md
        .                   // "T1c": ws_send_frame was ~9-10% of instructions self-cost under
        .                   // WS+legal load). No masking means no transform is needed before
        .                   // the payload goes out.
  202,918 ( 0.22%)          if (len <= 0) return ws_write_full(&c->conn, hdr, hn) == hn ? 0 : -1;
  304,377 ( 0.33%)          if (!c->conn.ssl && !conn_is_buffered(&c->conn)) {
        .                       // Plaintext, real socket: one writev() of header+payload
        .                       // replaces two write() syscalls per frame — it can be handed to
        .                       // the kernel as-is instead of copied through a scratch buffer
        .                       // first.
        .                       struct iovec iov[2] = {
        .                           { .iov_base = hdr, .iov_len = (size_t)hn },
        .                           { .iov_base = (void *)(uintptr_t)payload, .iov_len = (size_t)len },
        .                       };
-- line 275 ----------------------------------------
-- line 287 ----------------------------------------
        .                   // and a fresh ~64KB stack frame on every single frame.
        .                   // Capacity: the largest frame either side of this protocol ever
        .                   // sends is state_put's documented worst case (h_state/h_ws share
        .                   // the same 65536 cap) plus the 1-byte ok/poll flag plus this
        .                   // function's own <=14-byte frame header — sized with the same
        .                   // "documented worst case + real margin" discipline VIEW_CACHE_CAP
        .                   // uses in foolish_server.c.
        .                   static _Thread_local unsigned char scratch[14 + 1 + 65536];
  101,459 ( 0.11%)          int64_t total = (int64_t)hn + len;
  202,918 ( 0.22%)          if (total > (int64_t)sizeof scratch) return -1;   // never happens at this build's caps — defensive, not reachable in practice
  710,213 ( 0.78%)          memcpy(scratch, hdr, (size_t)hn);
1,826,262 ( 2.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (101,459x)
  608,754 ( 0.67%)          memcpy(scratch + hn, payload, (size_t)len);
1,185,606 ( 1.30%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (101,459x)
  405,836 ( 0.44%)          return ws_write_full(&c->conn, scratch, (int)total) == (int)total ? (int)len : -1;
        .               }
        .           
        .               if (ws_write_full(&c->conn, hdr, hn) != hn) return -1;
        .               if (len <= 0) return 0;
        .               // Masked send: XOR through a bounded scratch buffer so the caller's
        .               // payload (often a shared response buffer) is never mutated.
        .               unsigned char chunk[4096];
        .               int64_t off = 0;
        .               while (off < len) {
        .                   int64_t take = len - off; if (take > (int64_t)sizeof chunk) take = (int64_t)sizeof chunk;
        .                   for (int64_t i = 0; i < take; i++) chunk[i] = payload[off + i] ^ mkey[(off + i) & 3];
        .                   if (ws_write_full(&c->conn, chunk, (int)take) != (int)take) return -1;
        .                   off += take;
        .               }
        .               return (int)len;
  811,672 ( 0.89%)  }
        .           
        .           void ws_send_close(WsConn *c, uint16_t code) {
        .               unsigned char payload[2] = { (unsigned char)(code >> 8), (unsigned char)(code & 0xFF) };
        .               ws_send_frame(c, WS_OP_CLOSE, payload, 2);
        .           }
        .           
        .           // ---------------------------------------------------------------------------
        .           // Frame read + message assembly
-- line 323 ----------------------------------------

1,230,810 ( 1.35%)  <counts for unidentified lines in ws.c>

--------------------------------------------------------------------------------
-- Auto-annotated source: conn.c
--------------------------------------------------------------------------------
Ir               

-- line 3 ----------------------------------------
      .           #include "conn.h"
      .           
      .           #include <errno.h>
      .           #include <stdio.h>
      .           #include <string.h>
      .           #include <unistd.h>
      .           #include <openssl/err.h>
      .           
    294 ( 0.00%)  ssize_t conn_read(Conn *c, void *buf, size_t n) {
     84 ( 0.00%)      if (c->buf_out) return -1;   // Stage 6: a buffered Conn is a write-only encode sink, never a read source
    126 ( 0.00%)      if (!c->ssl) {
      .                   ssize_t r;
    382 ( 0.00%)          do { r = read(c->fd, buf, n); } while (r < 0 && errno == EINTR);
  2,394 ( 0.00%)  => ./io/../sysdeps/unix/sysv/linux/read.c:read (42x)
    810 ( 0.00%)  => ./elf/../sysdeps/x86_64/dl-trampoline.h:_dl_runtime_resolve_xsave (1x)
      .                   return r;
      .               }
      .               if (n == 0) return 0;
      .               for (;;) {
      .                   int r = SSL_read(c->ssl, buf, (int)n);
      .                   if (r > 0) return (ssize_t)r;
      .                   int err = SSL_get_error(c->ssl, r);
      .                   // WANT_READ/WANT_WRITE on a blocking socket only happens around a
-- line 23 ----------------------------------------
-- line 25 ----------------------------------------
      .                   // (set in tls_*_ctx_create) makes OpenSSL retry this internally in
      .                   // practice, but handle it explicitly too rather than depend on that
      .                   // mode alone (correctness over relying on a single guard).
      .                   if (err == SSL_ERROR_WANT_READ || err == SSL_ERROR_WANT_WRITE) continue;
      .                   if (err == SSL_ERROR_ZERO_RETURN) return 0;   // peer's clean close_notify — same as read()==0
      .                   if (err == SSL_ERROR_SYSCALL && errno == EINTR) continue;
      .                   return -1;   // peer gone / real error — caller already treats <0 as "tear this connection down", never crashes the process
      .               }
    294 ( 0.00%)  }
      .           
913,833 ( 1.00%)  ssize_t conn_write(Conn *c, const void *buf, size_t n) {
304,611 ( 0.33%)      if (c->buf_out) {   // Stage 6: append into the caller's memory buffer instead of a real write()
304,533 ( 0.33%)          int room = c->buf_cap - c->buf_len;
203,022 ( 0.22%)          int take = (int)n; if (take > room) take = room;
812,088 ( 0.89%)          if (take > 0) { memcpy(c->buf_out + c->buf_len, buf, (size_t)take); c->buf_len += take; }
1,218,680 ( 1.34%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (101,511x)
101,511 ( 0.11%)          return take;   // short iff it wouldn't fit — callers (ws_write_full et al.) already treat a short result as "stop"
      .               }
     78 ( 0.00%)      if (!c->ssl) {
      .                   ssize_t r;
    238 ( 0.00%)          do { r = write(c->fd, buf, n); } while (r < 0 && errno == EINTR);
  1,482 ( 0.00%)  => ./io/../sysdeps/unix/sysv/linux/write.c:write (26x)
    834 ( 0.00%)  => ./elf/../sysdeps/x86_64/dl-trampoline.h:_dl_runtime_resolve_xsave (1x)
      .                   return r;
      .               }
      .               if (n == 0) return 0;
      .               for (;;) {
      .                   int r = SSL_write(c->ssl, buf, (int)n);
      .                   if (r > 0) return (ssize_t)r;
      .                   int err = SSL_get_error(c->ssl, r);
      .                   if (err == SSL_ERROR_WANT_READ || err == SSL_ERROR_WANT_WRITE) continue;
      .                   if (err == SSL_ERROR_SYSCALL && errno == EINTR) continue;
      .                   return -1;
      .               }
710,759 ( 0.78%)  }
      .           
     65 ( 0.00%)  void conn_close(Conn *c) {
     39 ( 0.00%)      if (c->ssl) {
      .                   SSL_shutdown(c->ssl);   // best-effort clean shutdown; result never checked, see conn.h's doc
      .                   SSL_free(c->ssl);
      .                   c->ssl = NULL;
      .               }
     39 ( 0.00%)      if (c->fd >= 0) {
     30 ( 0.00%)          close(c->fd);
    818 ( 0.00%)  => ./elf/../sysdeps/x86_64/dl-trampoline.h:_dl_runtime_resolve_xsave (1x)
    689 ( 0.00%)  => ./io/../sysdeps/unix/sysv/linux/close.c:close (13x)
     13 ( 0.00%)          c->fd = -1;
      .               }
     52 ( 0.00%)  }
      .           
      .           bool conn_tls_accept(Conn *out, SSL_CTX *ctx, int fd) {
      .               SSL *ssl = SSL_new(ctx);
      .               if (!ssl) return false;
      .               SSL_set_fd(ssl, fd);
      .               int r = SSL_accept(ssl);
      .               if (r != 1) { SSL_free(ssl); return false; }
      .               out->fd = fd;
-- line 76 ----------------------------------------

    126 ( 0.00%)  <counts for unidentified lines in conn.c>

--------------------------------------------------------------------------------
-- Auto-annotated source: ../../../c/src/game.c
--------------------------------------------------------------------------------
Ir              

-- line 60 ----------------------------------------
     .           //     shuffle (~522 ns) on every drawing move — 20-40× per game — for that
     .           //     negative space "saving".
     .           //   - Size: ChaCha (deal_rng, ~1.4 KB) is in the binary either way; the alt
     .           //     only ADDS code (seed-in-blob + re-derive on load).
     .           //   - Security: the deck in the blob is masked per-viewer (WIRE_CARD_HIDDEN);
     .           //     the seed stays in a server-only column. Putting the seed in the blob would
     .           //     drag the crown-jewel seed toward the client boundary.
     .           static void deal_shuffle(Game *g) {
   560 ( 0.00%)      for (int i = g->deck_count - 1; i > 0; i--) {
   420 ( 0.00%)          int j = (int)deal_rng_bounded(&g_deal_rng, (uint32_t)(i + 1));
19,388 ( 0.02%)  => /home/user/foolish/server/impls/native/../../../c/src/deal_rng.c:deal_rng_bounded (140x)
   700 ( 0.00%)          Card t = g->deck[i]; g->deck[i] = g->deck[j]; g->deck[j] = t;
     .               }
     .           }
     .           #endif
     .           
     .           // Random index in [0, n) for a DECK DRAW. Deterministic mode pops the top of the
     .           // pre-shuffled deck (0); legacy consumes one game_random() and clamps, byte-for-
     .           // byte as the original inline draw code did (including on a 1-card deck, so the
     .           // pinned LCG stream never desyncs). Deterministic mode is signalled two ways,
     .           // both meaning "the deck is pre-shuffled, pop it": g_deal_wide during the deal
     .           // itself, and the per-game deterministic_deck flag mid-game (restored from the
     .           // durable blob, since the thread-local does not survive between kernel calls).
     .           static int draw_index(const Game *g, int n) {
   474 ( 0.00%)      if (g_deal_wide || g->deterministic_deck) return 0;
     .               int idx = (int)(game_random() * n);
     .               if (idx < 0) idx = 0;
     .               if (idx >= n) idx = n - 1;
     .               return idx;
     .           }
     .           
     .           // Random index in [0, n) for the first-attacker fallback (pick among players
     .           // when nobody holds a trump). Deterministic mode draws it, unbiased, from the
-- line 91 ----------------------------------------
-- line 121 ----------------------------------------
     .               g_deal_wide = 0;   // revert the deal to the legacy 32-bit LCG path
     .           #ifdef GRPO_RNG_DEBUG
     .               g_seed_set = 1;
     .           #endif
     .           }
     .           
     .           #ifndef DEAL_RNG_DISABLED
     .           void game_set_deal_seed_bytes(const uint8_t *seed, int len) {
    28 ( 0.00%)      if (!seed || len < FOOLISH_SEED_LEN) return;  // too little entropy: leave wide mode off
    16 ( 0.00%)      deal_rng_seed(&g_deal_rng, seed);
   100 ( 0.00%)  => /home/user/foolish/server/impls/native/../../../c/src/deal_rng.c:deal_rng_seed (4x)
     8 ( 0.00%)      g_deal_wide = 1;                 // start_game will shuffle; draws then pop
     .           #ifdef GRPO_RNG_DEBUG
     .               g_seed_set = 1;                  // a wide seed also satisfies the init guard
     .           #endif
     4 ( 0.00%)  }
     .           #else
     .           void game_set_deal_seed_bytes(const uint8_t *seed, int len) { (void)seed; (void)len; }
     .           #endif
     .           
     .           int game_deal_seed_active(void) { return g_deal_wide; }
     .           
     .           // Deal-RNG save/restore (see game.h). Layout is private to this file: byte 0 is
     .           // the wide flag, the rest is the ChaCha state. Nothing persists or ships it, so
-- line 143 ----------------------------------------
-- line 202 ----------------------------------------
     .           #endif
     .               g_rand_seed = g_rand_seed * 1664525u + 1013904223u;
     .               return (double)g_rand_seed / 4294967296.0;
     .           }
     .           
     .           // ---------- Helpers ----------------------------------------------------
     .           
     .           bool can_cover(Card attack, Card defense, int power_suit) {
   787 ( 0.00%)      if (defense.suit != attack.suit) {
    61 ( 0.00%)          return defense.suit == power_suit && attack.suit != power_suit;
     .               }
   180 ( 0.00%)      return defense.value > attack.value;
     .           }
     .           
    12 ( 0.00%)  int get_next_player_index(const Game *g, int current) {
   478 ( 0.00%)      int n = g->num_players;
     .               // TS guard: with one (or zero) players still IN the rotation is
     .               // meaningless — return the caller's seat unchanged. This only fires in
     .               // the endgame (game_done is imminent) but it decides the final stored
     .               // first_attacker/defender, so it must match.
     .               int in_count = 0;
 6,651 ( 0.01%)      for (int i = 0; i < n; i++) if (g->players[i].status == PLAYER_STATUS_IN) in_count++;
   900 ( 0.00%)      if (in_count <= 1) return current;
     .               int next = (current + 1) % n;
 1,342 ( 0.00%)      while (g->players[next].status == PLAYER_STATUS_OUT) {
     .                   next = (next + 1) % n;
     .               }
     .               return next;
    24 ( 0.00%)  }
     .           
 1,452 ( 0.00%)  int game_done(const Game *g) {
     .               int in_count = 0, out_count = 0, last_in = -1;
13,189 ( 0.01%)      for (int i = 0; i < g->num_players; i++) {
12,718 ( 0.01%)          if (g->players[i].status == PLAYER_STATUS_IN) { in_count++; last_in = i; }
     4 ( 0.00%)          else if (g->players[i].status == PLAYER_STATUS_OUT) { out_count++; }
     .               }
 2,915 ( 0.00%)      if (in_count == 1 && out_count == g->num_players - 1) return last_in;
     .               return -1;
   484 ( 0.00%)  }
     .           
     .           // The kernel records its own end. handle_* leave the loser on player statuses
     .           // (game_done reads it), but never touched g->status — so "is it over" was a fact
     .           // every host recomputed and cached in a status of its own. Now the apply paths
     .           // call this after a move, and g->status carries the answer for everyone.
     .           void game_settle_status(Game *g) {
 1,501 ( 0.00%)      if (g && g->status == GAME_STATUS_PLAYING && game_done(g) >= 0)
     8 ( 0.00%)          g->status = GAME_STATUS_GAME_OVER;
     .           }
     .           
   484 ( 0.00%)  uint32_t game_human_mask(const Game *g) {
   968 ( 0.00%)      if (!g) return 0;
     .               uint32_t m = 0;
 9,196 ( 0.01%)      for (int i = 0; i < g->num_players; i++)
 5,808 ( 0.01%)          if (g->players[i].strategy_key == STRATEGY_KEY_HUMAN) m |= (1u << i);
     .               return m;
   484 ( 0.00%)  }
     .           
     8 ( 0.00%)  void game_seat_and_deal(Game *g, const int8_t *strategy_keys, int n) {
    20 ( 0.00%)      if (!g || n < 2 || n > MAX_PLAYERS) return;
     4 ( 0.00%)      g->num_players = (int8_t)n;
     .               // NULL keeps whatever kind the seats already hold — the incremental-lobby
     .               // case, where a host wired strategy_key as each player joined.
     8 ( 0.00%)      if (strategy_keys)
     .                   for (int i = 0; i < n; i++) g->players[i].strategy_key = strategy_keys[i];
     8 ( 0.00%)      start_game(g);   // deals; start_game_reset assigns each seated player's status
36,968 ( 0.04%)  => ../../../c/src/game.c:start_game (4x)
     .           }
     .           
     .           // ---------- Logs -------------------------------------------------------
     .           
     .           #ifdef GUARDS_VALIDATE_ONLY
     .           // GUARDS_VALIDATE_ONLY: guards.wasm is a MOVE VALIDATOR. It dry-runs the real
     .           // handle_* on a throwaway clone and reads ONLY the reject code — the animation
     .           // logs those handlers emit (and the end-of-round stock refill, see
-- line 274 ----------------------------------------
-- line 289 ----------------------------------------
     .               return &g_log_sink;
     .           }
     .           static void log_add_card(GameLog *l, Card c) { (void)l; (void)c; }
     .           static void log_add_pair(GameLog *l, Card primary, Card target) { (void)l; (void)primary; (void)target; }
     .           #else
     .           
     .           static GameLog *log_alloc(Game *g, int log_type, int player_idx) {
     .               bool drop;
 1,647 ( 0.00%)      if (g->log_cap > 0) {
     .                   // Short-log instance (sampled-world slot, see game.h): keep only
     .                   // LOG_DISCARD entries, and only while a full-size instance would
     .                   // still have kept the append (log_virt mirrors what num_logs would
     .                   // be if every append had landed) — so espresso's discard memory
     .                   // sees the exact same discard set as before.
     .                   int virt = g->log_virt;
     .                   if (virt < MAX_LOGS) g->log_virt = (int16_t)(virt + 1);
     .                   drop = virt >= MAX_LOGS || log_type != LOG_DISCARD
     .                       || g->num_logs >= g->log_cap;
     .               } else {
     .                   // Should never happen at MAX_LOGS=512 for sane games. Drop silently.
 1,422 ( 0.00%)          drop = g->num_logs >= MAX_LOGS;
     .               }
   711 ( 0.00%)      if (drop) {
     .                   // _Thread_local (Stage 5): this IS live on the server's concurrent
     .                   // play path (a log-cap overflow during a real game), unlike the
     .                   // GUARDS_VALIDATE_ONLY sink above — two games' handle_* calls
     .                   // overflowing in the same instant would otherwise tear one shared
     .                   // GameLog between threads.
     .                   static _Thread_local GameLog scratch;
     .                   memset(&scratch, 0, sizeof(scratch));
     .                   scratch.log_type = log_type;
     .                   scratch.player_idx = player_idx;
     .                   scratch.defender_index = -1;
     .                   return &scratch;
     .               }
 3,573 ( 0.00%)      GameLog *l = &g->logs[g->num_logs++];
   925 ( 0.00%)      l->log_type = log_type;
   588 ( 0.00%)      l->player_idx = player_idx;
   989 ( 0.00%)      l->defender_index = -1;
     .               l->num_pairs = 0;
     .               return l;
   187 ( 0.00%)  }
     .           
     .           static void log_add_card(GameLog *l, Card c) {
 2,285 ( 0.00%)      if (l->num_pairs >= MAX_LOG_PAIRS) return;
 1,222 ( 0.00%)      LogPair *p = &l->pairs[l->num_pairs++];
   611 ( 0.00%)      p->primary = c;
   721 ( 0.00%)      p->target = CARD_NONE;
     .           }
     .           
     .           static void log_add_pair(GameLog *l, Card primary, Card target) {
   363 ( 0.00%)      if (l->num_pairs >= MAX_LOG_PAIRS) return;
   242 ( 0.00%)      LogPair *p = &l->pairs[l->num_pairs++];
   121 ( 0.00%)      p->primary = primary;
   121 ( 0.00%)      p->target = target;
     .           }
     .           #endif  // GUARDS_NO_LOG
     .           
     .           // ---------- Hand ops ---------------------------------------------------
     .           
     .           static void hand_remove_card(Player *p, Card c) {
 8,073 ( 0.01%)      for (int i = 0; i < p->hand_count; i++) {
 1,259 ( 0.00%)          if (card_eq(p->hand[i], c)) {
 7,688 ( 0.01%)              for (int j = i + 1; j < p->hand_count; j++) p->hand[j - 1] = p->hand[j];
   774 ( 0.00%)              p->hand_count--;
     .                       return;
     .                   }
     .               }
     .           }
     .           
     .           static bool hand_contains(const Player *p, Card c) {
 7,062 ( 0.01%)      for (int i = 0; i < p->hand_count; i++) if (card_eq(p->hand[i], c)) return true;
     .               return false;
     .           }
     .           
     .           // ---------- Deck / draw ------------------------------------------------
     .           
     .           static void refill_deck(Game *g) {
     .               int idx = 0;
     .               int min_v = min_value_for(g->num_players);
     .               for (int suit = 0; suit < NUM_SUITS; suit++) {
   400 ( 0.00%)          for (int v = min_v; v <= ACE_VALUE; v++) {
     .                       g->deck[idx].suit = (int8_t)suit;
   460 ( 0.00%)              g->deck[idx].value = (int8_t)v;
    12 ( 0.00%)              idx++;
     .                   }
     .               }
     4 ( 0.00%)      g->deck_count = (int16_t)idx;
     .           }
     .           
     .           // Draw one card. Mirrors common_utils.ts `draw`: picks a random card from the
     .           // deck (Math.random()) and splices it; if the deck is empty, returns the
     .           // flipped card if any.
     .           static bool draw_card(Game *g, Card *out) {
   485 ( 0.00%)      if (g->deck_count == 0) {
    12 ( 0.00%)          if (!g->has_flipped) return false;
     4 ( 0.00%)          *out = g->flipped;
     8 ( 0.00%)          g->has_flipped = false;
     .                   return true;
     .               }
     .               int idx = draw_index(g, g->deck_count);
   290 ( 0.00%)      *out = g->deck[idx];
20,968 ( 0.02%)      for (int i = idx + 1; i < g->deck_count; i++) g->deck[i - 1] = g->deck[i];
   295 ( 0.00%)      g->deck_count--;
     .               return true;
     .           }
     .           
     .           static void deal_initial(Game *g) {
     .               // Player-major deal, mirroring the TS start_game: each player draws all
     .               // CARDS_PER_PLAYER cards before the next player starts, and a snapshot
     .               // hook fires per player (that's the per-player DEAL animation event, with
     .               // the deck draining 36 → 30 → 24 → ... between snapshots).
    88 ( 0.00%)      for (int j = 0; j < g->num_players; j++) g->players[j].hand_count = 0;
    32 ( 0.00%)      for (int j = 0; j < g->num_players; j++) {
   144 ( 0.00%)          for (int i = 0; i < CARDS_PER_PLAYER; i++) {
     .                       Card c;
     .                       if (!draw_card(g, &c)) break;
   192 ( 0.00%)              g->players[j].hand[g->players[j].hand_count++] = c;
     .                   }
    24 ( 0.00%)          SNAP(g, ENGINE_HOOK_DEAL, j);
     .               }
     .           }
     .           
     .           // When nobody was dealt a trump there is nothing to derive the first attacker
     .           // FROM, and the engine rolls for it (deal_index). That roll is unreproducible
     .           // from a replay code — the code records the deal, not the RNG state it was
     .           // rolled against — so a replay of such a game would rebuild the right hands and
     .           // then pick a different opening seat, at random, on every run.
-- line 416 ----------------------------------------
-- line 434 ----------------------------------------
     .           // replay_difftest, replay_v6_test — none of which are multi-threaded).
     .           static int g_forced_first_attacker = -1;
     .           
     .           void game_force_first_attacker(int seat) { g_forced_first_attacker = seat; }
     .           
     .           static int determine_lowest_power_index(Game *g) {
     .               int lowest_v = ACE_VALUE + 1;
     .               int lowest_p = -1;
    60 ( 0.00%)      for (int i = 0; i < g->num_players; i++) {
   160 ( 0.00%)          for (int j = 0; j < g->players[i].hand_count; j++) {
    48 ( 0.00%)              Card c = g->players[i].hand[j];
   300 ( 0.00%)              if (c.suit == g->power_suit && c.value < lowest_v) {
     .                           lowest_v = c.value;
     .                           lowest_p = i;
     .                       }
     .                   }
     .               }
     8 ( 0.00%)      if (lowest_p == -1) {
     .                   lowest_p = (g_forced_first_attacker >= 0 && g_forced_first_attacker < g->num_players)
     .                            ? g_forced_first_attacker
     .                            : deal_index(g->num_players);
     .               }
     .               return lowest_p;
     .           }
     .           
     .           // The lobby->playing reset every start path shares.
     .           static void start_game_reset(Game *g) {
     4 ( 0.00%)      g->status = GAME_STATUS_PLAYING;
     4 ( 0.00%)      g->num_battles = 0;
     4 ( 0.00%)      g->num_eliminated = 0;
     4 ( 0.00%)      g->num_logs = 0;
     4 ( 0.00%)      g->discard_pile_length = 0;
     4 ( 0.00%)      g->good_players_mask = 0;
     4 ( 0.00%)      g->has_good_timestamp = false;
     .           
     .               // Game start log (system event, no player_idx).
     .               log_alloc(g, LOG_GAME_START, -1);
     .           
    80 ( 0.00%)      for (int i = 0; i < g->num_players; i++) {
     8 ( 0.00%)          g->players[i].status = PLAYER_STATUS_IN;
     .               }
     .           }
     .           
     .           // Everything downstream of "the deck is sitting there in pop order": the deal,
     .           // the flip, the opening seats. Split out of start_game so a rebuilt deck runs
     .           // the identical path (start_game_with_deck) — the alternative was a second
     .           // deal in replay_steps.c, which is the duplication this consolidation exists
     .           // to delete.
    36 ( 0.00%)  static void start_game_dealt(Game *g) {
     .               // TS emits its opening MAGIC_TRANSITION here: PLAYING status, full deck,
     .               // hands still empty from the lobby.
    16 ( 0.00%)      SNAP(g, ENGINE_HOOK_START_MAGIC, -1);
     .               deal_initial(g);
     .           
     .               // Flip a non-Ace.
     .               Card f;
     .               while (true) {
     .                   if (!draw_card(g, &f)) break;
    20 ( 0.00%)          if (f.value == ACE_VALUE) {
     .                       // push back to deck; same as TS (no shuffle, but draw picks random)
     5 ( 0.00%)              g->deck[g->deck_count++] = f;
     .                       continue;
     .                   }
     .                   break;
     .               }
     4 ( 0.00%)      g->flipped = f;
     4 ( 0.00%)      g->has_flipped = true;
    16 ( 0.00%)      g->power_suit = f.suit;
    12 ( 0.00%)      SNAP(g, ENGINE_HOOK_FLIPPED, -1);
     .           
     .               int lowest = determine_lowest_power_index(g);
     4 ( 0.00%)      g->first_attacker = (int8_t)lowest;
    20 ( 0.00%)      g->defender = (int8_t)((lowest + 1) % g->num_players);
    12 ( 0.00%)      SNAP(g, ENGINE_HOOK_START_DEFENDER, g->defender);
    32 ( 0.00%)  }
     .           
    28 ( 0.00%)  void start_game(Game *g) {
     .               start_game_reset(g);
     .           
     .               refill_deck(g);
     .               // Record how this game draws, so mid-game kernel calls (which restore this
     .               // from the durable blob, not the thread-local) keep popping the pre-shuffled
     .               // deck. Legacy deals leave it false and draw at random, exactly as before.
     8 ( 0.00%)      g->deterministic_deck = g_deal_wide ? true : false;
     .           #ifndef DEAL_RNG_DISABLED
     .               // Seed-dealt game: shuffle the whole deck once from the ChaCha stream, then
     .               // every draw below (and every mid-game refill) pops the top — the full deal
     .               // and game are reproducible from the seed.
     4 ( 0.00%)      if (g_deal_wide) deal_shuffle(g);
     .           #endif
    28 ( 0.00%)      start_game_dealt(g);
14,460 ( 0.02%)  => ../../../c/src/game.c:start_game_dealt (4x)
     .           }
     .           
     .           void game_reset_to_lobby(Game *g, unsigned int bot_mask) {
     .               g->status = GAME_STATUS_WAITING;
     .           
     .               for (int i = 0; i < g->num_players; i++) {
     .                   Player *p = &g->players[i];
     .                   p->status = (bot_mask & (1u << i)) ? PLAYER_STATUS_READY : PLAYER_STATUS_IDLE;
-- line 532 ----------------------------------------
-- line 565 ----------------------------------------
     .               g->deterministic_deck = true;
     .           
     .               start_game_dealt(g);
     .           }
     .           
     .           // Refill phase: defender first if their hand is empty, then around starting
     .           // from first_attacker, mirroring refillPlayerHandsWithEvents.
     .           static bool no_cards_left(const Game *g) {
   254 ( 0.00%)      return g->deck_count == 0 && !g->has_flipped;
     .           }
     .           
     .           #ifdef GUARDS_VALIDATE_ONLY
     .           // Validate-only build: the stock refill runs only in a move's COMMIT phase,
     .           // AFTER every reject check has passed — no reject code depends on the cards it
     .           // draws, and guards discards the post-move state. No-op it so the whole draw
     .           // path (draw_card, refill_deck) dead-code-eliminates out of the validator.
     .           // (handle_* still runs full mutation, so cover's mid-apply re-check and pass's
     .           // post-mutation PASS_OVERFLOW reject stay byte-for-byte identical to the server.)
     .           static void refill_player_hands(Game *g) { (void)g; }
     .           #else
   837 ( 0.00%)  static void refill_player_hands(Game *g) {
    28 ( 0.00%)      if (no_cards_left(g)) {
   338 ( 0.00%)          for (int i = 0; i < g->num_players; i++) {
   104 ( 0.00%)              if (g->players[i].hand_count == 0 && g->players[i].status == PLAYER_STATUS_IN) {
     .                           g->players[i].status = PLAYER_STATUS_OUT;
     .                           g->players[i].awaiting_attack = false;
     .                           g->elimination_order[g->num_eliminated++] = (int8_t)i;
     .                       }
     .                   }
     .                   return;
     .               }
     .           
     .               // Defender draws first if their hand is empty.
    67 ( 0.00%)      int defender = g->defender;
   268 ( 0.00%)      if (g->players[defender].hand_count == 0) {
     .                   Card drawn[CARDS_PER_PLAYER];
     .                   int n_drawn = 0;
     .                   while (g->players[defender].hand_count < CARDS_PER_PLAYER) {
     .                       Card c;
     .                       if (!draw_card(g, &c)) break;
     .                       g->players[defender].hand[g->players[defender].hand_count++] = c;
     .                       drawn[n_drawn++] = c;
     .                   }
     .                   if (n_drawn > 0) {
     .                       GameLog *l = log_alloc(g, LOG_DRAW, defender);
     .                       for (int i = 0; i < n_drawn; i++) log_add_card(l, drawn[i]);
     .                       SNAP(g, ENGINE_HOOK_DRAW, defender);
     .                   }
     .               }
     .           
    67 ( 0.00%)      int p_idx = g->first_attacker;
   402 ( 0.00%)      bool visited[MAX_PLAYERS] = { false };
   469 ( 0.00%)      do {
   402 ( 0.00%)          if (visited[p_idx]) break;
   134 ( 0.00%)          visited[p_idx] = true;
     .                   Card drawn[CARDS_PER_PLAYER];
     .                   int n_drawn = 0;
   690 ( 0.00%)          while (g->players[p_idx].hand_count < CARDS_PER_PLAYER) {
     .                       Card c;
     .                       if (!draw_card(g, &c)) break;
   656 ( 0.00%)              g->players[p_idx].hand[g->players[p_idx].hand_count++] = c;
   192 ( 0.00%)              drawn[n_drawn++] = c;
     .                   }
   110 ( 0.00%)          if (n_drawn > 0) {
     .                       GameLog *l = log_alloc(g, LOG_DRAW, p_idx);
   478 ( 0.00%)              for (int i = 0; i < n_drawn; i++) log_add_card(l, drawn[i]);
     .                       // TS pushes the refill event (and its snapshot) BEFORE the
     .                       // zero-hand OUT check below, so the hook fires here.
   165 ( 0.00%)              SNAP(g, ENGINE_HOOK_DRAW, p_idx);
     .                   }
    55 ( 0.00%)          if (g->players[p_idx].hand_count == 0
    55 ( 0.00%)              && g->players[p_idx].status == PLAYER_STATUS_IN) {
     .                       g->players[p_idx].status = PLAYER_STATUS_OUT;
     .                       g->players[p_idx].awaiting_attack = false;
     .                       g->elimination_order[g->num_eliminated++] = (int8_t)p_idx;
     .                   }
     .                   p_idx = get_next_player_index(g, p_idx);
   402 ( 0.00%)      } while (p_idx != g->first_attacker);
   744 ( 0.00%)  }
     .           #endif  // GUARDS_VALIDATE_ONLY
     .           
     .           // ---------- Action: attack --------------------------------------------
     .           
     .           static int count_uncovered(const Game *g) {
     .               int n = 0;
 3,332 ( 0.00%)      for (int i = 0; i < g->num_battles; i++) if (!!card_is_none(g->table_battles[i].defense)) n++;
     .               return n;
     .           }
     .           
     .           static bool table_has_value(const Game *g, int v) {
   327 ( 0.00%)      for (int i = 0; i < g->num_battles; i++) {
   376 ( 0.00%)          if (g->table_battles[i].attack.value == v) return true;
   302 ( 0.00%)          if (!card_is_none(g->table_battles[i].defense) && g->table_battles[i].defense.value == v) return true;
     .               }
     .               return false;
     .           }
     .           
     .           // Shared move-validation predicates. Pure (no state mutation, no reject
     .           // side-effect): each handler keeps its own REJECT at its own call site, so
     .           // the per-handler reject-code ORDER — a tested parity contract vs the old TS
     .           // validators — is untouched. Factored out so attack/cover/pass don't each
     .           // inline their own copy of these sweeps (-Oz keeps them as one shared func).
     .           static bool all_in_hand(const Player *p, const Card *c, int n) {
 5,443 ( 0.01%)      for (int i = 0; i < n; i++) if (!hand_contains(p, c[i])) return false;
     .               return true;
     .           }
     .           static bool has_dup(const Card *c, int n) {
 2,164 ( 0.00%)      for (int i = 0; i < n; i++)
 2,697 ( 0.00%)          for (int j = i + 1; j < n; j++) if (card_eq(c[i], c[j])) return true;
     .               return false;
     .           }
     .           static bool all_same_value(const Card *c, int n) {
   470 ( 0.00%)      for (int i = 1; i < n; i++) if (c[i].value != c[0].value) return false;
     .               return true;
     .           }
     .           
 2,104 ( 0.00%)  bool handle_attack(Game *g, int player_idx, const Card *cards, int n_cards) {
   789 ( 0.00%)      engine_last_reject = ENGINE_REJECT_NONE;
   526 ( 0.00%)      if (n_cards <= 0) REJECT(ENGINE_REJECT_EMPTY);
   526 ( 0.00%)      if (g->status != GAME_STATUS_PLAYING) REJECT(ENGINE_REJECT_NOT_PLAYING);
   799 ( 0.00%)      if (player_idx == g->defender) REJECT(ENGINE_REJECT_IS_DEFENDER);
     .           
     .               // Validation ordering mirrors TS validateAttack: full in-hand sweep
     .               // first, then the duplicate sweep, so multi-fault inputs reject for the
     .               // same reason on both engines.
   516 ( 0.00%)      Player *p = &g->players[player_idx];
   596 ( 0.00%)      if (!all_in_hand(p, cards, n_cards)) REJECT(ENGINE_REJECT_NOT_IN_HAND);
   436 ( 0.00%)      if (has_dup(cards, n_cards)) REJECT(ENGINE_REJECT_DUPLICATES);
     .           
   436 ( 0.00%)      bool first_attack = (g->num_battles == 0);
   218 ( 0.00%)      if (first_attack) {
    17 ( 0.00%)          if (!all_same_value(cards, n_cards)) REJECT(ENGINE_REJECT_NOT_SAME_VALUE);
   388 ( 0.00%)          if (player_idx != g->first_attacker) REJECT(ENGINE_REJECT_NOT_FIRST_ATTACKER);
     .               } else {
   936 ( 0.00%)          for (int i = 0; i < n_cards; i++) {
   442 ( 0.00%)              if (!table_has_value(g, cards[i].value)) REJECT(ENGINE_REJECT_VALUE_NOT_ON_TABLE);
     .                   }
     .               }
     .           
     .               int uncovered = count_uncovered(g);
   320 ( 0.00%)      int defender_cards = g->players[g->defender].hand_count;
   482 ( 0.00%)      if (defender_cards < uncovered + n_cards) REJECT(ENGINE_REJECT_DEFENDER_CAPACITY);
     .           
     .               // Apply.
 2,466 ( 0.00%)      for (int i = 0; i < n_cards; i++) {
   186 ( 0.00%)          hand_remove_card(p, cards[i]);
   558 ( 0.00%)          Battle *b = &g->table_battles[g->num_battles++];
   372 ( 0.00%)          b->attack = cards[i];
   186 ( 0.00%)          b->defense = CARD_NONE;
     .               }
     .           
   477 ( 0.00%)      GameLog *l = log_alloc(g, LOG_ATTACK, player_idx);
 2,544 ( 0.00%)  => ../../../c/src/game.c:log_alloc (159x)
   691 ( 0.00%)      for (int i = 0; i < n_cards; i++) log_add_card(l, cards[i]);
     .           
   159 ( 0.00%)      g->good_players_mask = 0;
   159 ( 0.00%)      g->has_good_timestamp = false;
   636 ( 0.00%)      SNAP(g, ENGINE_HOOK_ATTACK, player_idx);
     .           
     .               // Attackers only LEAVE the game when the stock is exhausted too — with
     .               // cards still in the deck they sit out the bout and refill at round end
     .               // (mirrors the TS no_cards_left guard; without it, dumping a whole hand
     .               // as throw-ins "won" instantly with 20+ cards still in the stock).
   322 ( 0.00%)      if (p->hand_count == 0 && no_cards_left(g)) {
     8 ( 0.00%)          p->status = PLAYER_STATUS_OUT;
     4 ( 0.00%)          p->awaiting_attack = false;
    20 ( 0.00%)          g->elimination_order[g->num_eliminated++] = (int8_t)player_idx;
     8 ( 0.00%)          log_alloc(g, LOG_PLAYER_OUT, player_idx);
    64 ( 0.00%)  => ../../../c/src/game.c:log_alloc (4x)
    16 ( 0.00%)          SNAP(g, ENGINE_HOOK_OUT, player_idx);
     .                   return true;
     .               }
     .           
   310 ( 0.00%)      bool was_first = (g->num_battles == n_cards);
   155 ( 0.00%)      if (was_first) {
 2,093 ( 0.00%)          for (int i = 0; i < g->num_players; i++) if (i != g->defender) g->players[i].awaiting_attack = true;
     .               } else {
   186 ( 0.00%)          p->awaiting_attack = true;
     .               }
     .               return true;
 2,367 ( 0.00%)  }
     .           
     .           // ---------- Action: cover ---------------------------------------------
     .           
     .           bool handle_cover(Game *g, int player_idx,
 1,440 ( 0.00%)                    const Card *cover_cards, const Card *attack_cards, int n) {
   540 ( 0.00%)      engine_last_reject = ENGINE_REJECT_NONE;
   360 ( 0.00%)      if (g->status != GAME_STATUS_PLAYING) REJECT(ENGINE_REJECT_NOT_PLAYING);
   360 ( 0.00%)      if (n <= 0) REJECT(ENGINE_REJECT_EMPTY);
     .           
     .               // TS validateCover checks for uncovered attacks BEFORE the defender
     .               // check; keep that priority.
     .               int uncovered = count_uncovered(g);
   426 ( 0.00%)      if (uncovered == 0) REJECT(ENGINE_REJECT_NO_UNCOVERED);
   390 ( 0.00%)      if (player_idx != g->defender) REJECT(ENGINE_REJECT_NOT_DEFENDER);
     .           
   120 ( 0.00%)      Player *def = &g->players[player_idx];
   255 ( 0.00%)      if (!all_in_hand(def, cover_cards, n)) REJECT(ENGINE_REJECT_NOT_IN_HAND);
   210 ( 0.00%)      if (has_dup(cover_cards, n)) REJECT(ENGINE_REJECT_DUPLICATES);
     .           
     .               // Each attack card must be on the table & uncovered — matched by EXACT
     .               // card (suit+value), not just value. The value-only lookup let a request
     .               // naming an already-covered card slip past validation when another
     .               // same-rank attack was still uncovered (the defender double-tap bug the
     .               // TS side fixed in validateCover).
   996 ( 0.00%)      for (int i = 0; i < n; i++) {
     .                   bool found = false;
   360 ( 0.00%)          for (int j = 0; j < g->num_battles; j++) {
   186 ( 0.00%)              if (!!card_is_none(g->table_battles[j].defense)
   428 ( 0.00%)                  && card_eq(g->table_battles[j].attack, attack_cards[i])) {
     .                           found = true; break;
     .                       }
     .                   }
     .                   if (!found) REJECT(ENGINE_REJECT_ATTACK_NOT_ON_TABLE);
     .               }
   198 ( 0.00%)      if (has_dup(attack_cards, n)) REJECT(ENGINE_REJECT_DUPLICATES);
   858 ( 0.00%)      for (int i = 0; i < n; i++) {
   363 ( 0.00%)          if (!can_cover(attack_cards[i], cover_cards[i], g->power_suit)) REJECT(ENGINE_REJECT_CANNOT_COVER);
     .               }
     .           
     .               // Apply each cover (with logging) and record discards if defender clears
     .               // their hand.
 1,650 ( 0.00%)      for (int i = 0; i < n; i++) {
     .                   Card cover_card = cover_cards[i];
     .                   Card attack_card = attack_cards[i];
     .                   int idx = -1;
   539 ( 0.00%)          for (int j = 0; j < g->num_battles; j++) {
   149 ( 0.00%)              if (!!card_is_none(g->table_battles[j].defense)
   220 ( 0.00%)                  && card_eq(g->table_battles[j].attack, attack_card)) {
     .                           idx = j; break;
     .                       }
     .                   }
     .                   if (idx < 0) REJECT(ENGINE_REJECT_ATTACK_NOT_ON_TABLE);
   121 ( 0.00%)          g->table_battles[idx].defense = cover_card;
     .                   hand_remove_card(def, cover_card);
     .           
     .                   GameLog *l = log_alloc(g, LOG_COVER, player_idx);
     .                   log_add_pair(l, cover_card, attack_card);
   363 ( 0.00%)          SNAP(g, ENGINE_HOOK_COVER, idx);
     .               }
     .           
   198 ( 0.00%)      if (def->hand_count == 0) {
     .                   // Discard all table cards, refill, advance defender. Mirrors the TS
     .                   // `executeCover` end-of-round branch.
     .                   int discarded = g->num_battles * 2;
     .                   g->discard_pile_length += discarded;
     .           
     .                   GameLog *l = log_alloc(g, LOG_DISCARD, -1);
     .                   for (int i = 0; i < g->num_battles; i++) {
     .                       log_add_card(l, g->table_battles[i].attack);
-- line 812 ----------------------------------------
-- line 835 ----------------------------------------
     .                   g->defender = (int8_t)get_next_player_index(g, g->first_attacker);
     .           
     .                   GameLog *dc = log_alloc(g, LOG_DEFENDER_CHANGE, -1);
     .                   dc->defender_index = g->defender;
     .                   SNAP(g, ENGINE_HOOK_DEFENDER_MOVE, g->defender);
     .                   return true;
     .               }
     .           
    99 ( 0.00%)      g->good_players_mask = 0;
    99 ( 0.00%)      g->has_good_timestamp = false;
     .           
   360 ( 0.00%)      bool all_covered = (g->num_battles > 0);
   534 ( 0.00%)      for (int i = 0; i < g->num_battles; i++) {
   181 ( 0.00%)          if (!!card_is_none(g->table_battles[i].defense)) { all_covered = false; break; }
     .               }
    63 ( 0.00%)      if (all_covered) {
    63 ( 0.00%)          g->has_good_timestamp = true; // matches `game.good_timestamp = Date.now()`
   945 ( 0.00%)          for (int i = 0; i < g->num_players; i++) {
   567 ( 0.00%)              if (i != g->defender && g->players[i].status == PLAYER_STATUS_IN) {
    95 ( 0.00%)                  g->players[i].awaiting_attack = true;
     .                       }
     .                   }
     .               }
     .               return true;
 1,620 ( 0.00%)  }
     .           
     .           // ---------- Action: pass ----------------------------------------------
     .           
   208 ( 0.00%)  bool handle_pass(Game *g, int player_idx, const Card *cards, int n_cards) {
    78 ( 0.00%)      engine_last_reject = ENGINE_REJECT_NONE;
    52 ( 0.00%)      if (g->status != GAME_STATUS_PLAYING) REJECT(ENGINE_REJECT_NOT_PLAYING);
    52 ( 0.00%)      if (n_cards <= 0) REJECT(ENGINE_REJECT_EMPTY);
     .           
     .               // TS validatePass priority: same-value → duplicates → defender →
     .               // in-hand → table-nonempty → cover-present → values-match → capacity.
    26 ( 0.00%)      int v = cards[0].value;
     4 ( 0.00%)      if (!all_same_value(cards, n_cards)) REJECT(ENGINE_REJECT_NOT_SAME_VALUE);
    52 ( 0.00%)      if (has_dup(cards, n_cards)) REJECT(ENGINE_REJECT_DUPLICATES);
   104 ( 0.00%)      if (player_idx != g->defender) REJECT(ENGINE_REJECT_NOT_DEFENDER);
     .           
    42 ( 0.00%)      Player *def = &g->players[player_idx];
    63 ( 0.00%)      if (!all_in_hand(def, cards, n_cards)) REJECT(ENGINE_REJECT_NOT_IN_HAND);
    60 ( 0.00%)      if (g->num_battles == 0) REJECT(ENGINE_REJECT_NO_TABLE_CARDS);
   123 ( 0.00%)      for (int i = 0; i < g->num_battles; i++) if (!card_is_none(g->table_battles[i].defense)) REJECT(ENGINE_REJECT_COVER_PRESENT);
   114 ( 0.00%)      for (int i = 0; i < g->num_battles; i++) if (g->table_battles[i].attack.value != v) REJECT(ENGINE_REJECT_PASS_VALUES);
     .           
    12 ( 0.00%)      int next = get_next_player_index(g, g->defender);
   564 ( 0.00%)  => ../../../c/src/game.c:get_next_player_index (12x)
    84 ( 0.00%)      if (g->players[next].hand_count < n_cards + g->num_battles) REJECT(ENGINE_REJECT_PASS_CAPACITY);
     .           
   183 ( 0.00%)      for (int i = 0; i < n_cards; i++) {
    13 ( 0.00%)          hand_remove_card(def, cards[i]);
    39 ( 0.00%)          Battle *b = &g->table_battles[g->num_battles++];
    26 ( 0.00%)          b->attack = cards[i];
    13 ( 0.00%)          b->defense = CARD_NONE;
     .               }
     .           
    36 ( 0.00%)      GameLog *l = log_alloc(g, LOG_PASS, player_idx);
   192 ( 0.00%)  => ../../../c/src/game.c:log_alloc (12x)
    46 ( 0.00%)      for (int i = 0; i < n_cards; i++) log_add_card(l, cards[i]);
     .           
    12 ( 0.00%)      g->good_players_mask = 0;
    12 ( 0.00%)      g->has_good_timestamp = false;
    48 ( 0.00%)      SNAP(g, ENGINE_HOOK_PASS, player_idx);
     .           
    10 ( 0.00%)      if (no_cards_left(g) && def->hand_count == 0) {
     .                   def->status = PLAYER_STATUS_OUT;
     .                   def->awaiting_attack = false;
     .                   g->elimination_order[g->num_eliminated++] = (int8_t)player_idx;
     .                   log_alloc(g, LOG_PLAYER_OUT, player_idx);
     .                   SNAP(g, ENGINE_HOOK_OUT, player_idx);
     .               }
     .           
    24 ( 0.00%)      g->defender = (int8_t)next;
    36 ( 0.00%)      GameLog *dc = log_alloc(g, LOG_DEFENDER_CHANGE, -1);
   192 ( 0.00%)  => ../../../c/src/game.c:log_alloc (12x)
    24 ( 0.00%)      dc->defender_index = g->defender;
     .           
     .               int uncovered = count_uncovered(g);
    36 ( 0.00%)      int defender_cards = g->players[g->defender].hand_count;
    24 ( 0.00%)      if (uncovered > defender_cards) {
     .                   // TS throws here (post-mutation, so the move never commits). We
     .                   // treat as fatal — abort the game — and report the reason.
     .                   g->status = GAME_STATUS_GAME_OVER;
     .                   REJECT(ENGINE_REJECT_PASS_OVERFLOW);
     .               }
     .               return true;
   208 ( 0.00%)  }
     .           
     .           // ---------- Action: pickup --------------------------------------------
     .           
     .           bool handle_pickup(Game *g, int player_idx) {
   267 ( 0.00%)      engine_last_reject = ENGINE_REJECT_NONE;
   178 ( 0.00%)      if (g->status != GAME_STATUS_PLAYING) REJECT(ENGINE_REJECT_NOT_PLAYING);
   271 ( 0.00%)      if (player_idx != g->defender) REJECT(ENGINE_REJECT_NOT_DEFENDER);
   309 ( 0.00%)      if (g->num_battles == 0) REJECT(ENGINE_REJECT_NO_TABLE_CARDS);
     .           
     .               Player *def = &g->players[player_idx];
     .               GameLog *l = log_alloc(g, LOG_PICKUP, player_idx);
     .           
     .               // TS pickup logs table cards attack-major (all attacks' cards in battle
     .               // order, defense right after its attack) — same interleaving here.
     .               //
     .               // The hand append is BOUNDED, like log_add_card beside it. A legal bout can
-- line 935 ----------------------------------------
-- line 937 ----------------------------------------
     .               // so pickup at worst doubles a hand that was already under the cap), but
     .               // this is the engine, and a caller that hands it a wide table — a corrupt
     .               // row, a hostile marshal, a bot's sampled world — used to walk straight off
     .               // the end of `hand`: 2 cards per battle, no check. It cost real time to find
     .               // (e2e/wasm_kernel_fuzz, 40 battles: 3 + 2*40 = 83 into hand[64], a wasm trap
     .               // ~19 cards past the end) because the wide table only reaches this path
     .               // through a ROLLOUT, so it read as a bot bug for hours. Drop the excess: a
     .               // truncated hand is a wrong game, but a corrupted Game is a wrong process.
   934 ( 0.00%)      for (int i = 0; i < g->num_battles; i++) {
     .                   Battle *b = &g->table_battles[i];
   139 ( 0.00%)          log_add_card(l, b->attack);
   973 ( 0.00%)          if (def->hand_count < MAX_HAND_SIZE) def->hand[def->hand_count++] = b->attack;
   278 ( 0.00%)          if (!card_is_none(b->defense)) {
     .                       log_add_card(l, b->defense);
   520 ( 0.00%)              if (def->hand_count < MAX_HAND_SIZE) def->hand[def->hand_count++] = b->defense;
     .                   }
     .               }
    63 ( 0.00%)      g->num_battles = 0;
   252 ( 0.00%)      SNAP(g, ENGINE_HOOK_PICKUP, player_idx);
     .           
   126 ( 0.00%)      refill_player_hands(g);
16,410 ( 0.02%)  => ../../../c/src/game.c:refill_player_hands (63x)
     .           
   189 ( 0.00%)      g->first_attacker = (int8_t)get_next_player_index(g, g->defender);
    63 ( 0.00%)      g->defender = (int8_t)get_next_player_index(g, g->first_attacker);
     .           
     .               GameLog *dc = log_alloc(g, LOG_DEFENDER_CHANGE, -1);
    63 ( 0.00%)      dc->defender_index = g->defender;
    63 ( 0.00%)      g->good_players_mask = 0;
   126 ( 0.00%)      g->has_good_timestamp = false;
     .               return true;
    89 ( 0.00%)  }
     .           
     .           // ---------- Action: good ----------------------------------------------
     .           
   150 ( 0.00%)  static void execute_round_transition(Game *g) {
     .               // TS emits a MAGIC_TRANSITION event with the pre-discard state.
   120 ( 0.00%)      SNAP(g, ENGINE_HOOK_MAGIC_TRANSITION, -1);
     .           
    60 ( 0.00%)      int discarded = g->num_battles * 2;
    30 ( 0.00%)      g->discard_pile_length += discarded;
     .           
     .               GameLog *l = log_alloc(g, LOG_DISCARD, -1);
   284 ( 0.00%)      for (int i = 0; i < g->num_battles; i++) {
    56 ( 0.00%)          log_add_card(l, g->table_battles[i].attack);
   112 ( 0.00%)          if (!card_is_none(g->table_battles[i].defense)) log_add_card(l, g->table_battles[i].defense);
     .               }
    30 ( 0.00%)      g->num_battles = 0;
    90 ( 0.00%)      SNAP(g, ENGINE_HOOK_TRASH, -1);
     .           
    60 ( 0.00%)      refill_player_hands(g);
 7,613 ( 0.01%)  => ../../../c/src/game.c:refill_player_hands (30x)
     .           
    60 ( 0.00%)      g->first_attacker = (int8_t)g->defender;
    30 ( 0.00%)      g->defender = (int8_t)get_next_player_index(g, g->first_attacker);
     .           
     .               GameLog *dc = log_alloc(g, LOG_DEFENDER_CHANGE, -1);
    30 ( 0.00%)      dc->defender_index = g->defender;
     .           
    30 ( 0.00%)      g->good_players_mask = 0;
    30 ( 0.00%)      g->has_good_timestamp = false;
   120 ( 0.00%)  }
     .           
 1,092 ( 0.00%)  bool handle_good(Game *g, int player_idx) {
   819 ( 0.00%)      engine_last_reject = ENGINE_REJECT_NONE;
   546 ( 0.00%)      if (g->status != GAME_STATUS_PLAYING) REJECT(ENGINE_REJECT_NOT_PLAYING);
 1,092 ( 0.00%)      if (g->players[player_idx].status != PLAYER_STATUS_IN) REJECT(ENGINE_REJECT_NOT_IN_STATUS);
   853 ( 0.00%)      if (player_idx == g->defender) REJECT(ENGINE_REJECT_IS_DEFENDER);
   522 ( 0.00%)      if (g->num_battles == 0 && player_idx == g->first_attacker) REJECT(ENGINE_REJECT_FIRST_MUST_ATTACK);
 1,616 ( 0.00%)      if (g->good_players_mask & (1u << player_idx)) REJECT(ENGINE_REJECT_ALREADY_GOOD);
     .           
   324 ( 0.00%)      g->good_players_mask |= (1u << player_idx);
     .               log_alloc(g, LOG_GOOD, player_idx);
   162 ( 0.00%)      g->players[player_idx].awaiting_attack = false;
     .           
     .               // Count attackers and check all_attackers_good.
     .               int n_attackers = 0;
     .               bool all_good = true;
 1,944 ( 0.00%)      for (int i = 0; i < g->num_players; i++) {
   972 ( 0.00%)          if (i != g->defender && g->players[i].status == PLAYER_STATUS_IN) {
   162 ( 0.00%)              n_attackers++;
   810 ( 0.00%)              if (!(g->good_players_mask & (1u << i))) all_good = false;
     .                   }
     .               }
   324 ( 0.00%)      if (n_attackers == 0) all_good = false;
     .           
   222 ( 0.00%)      bool all_covered = (g->num_battles > 0);
   618 ( 0.00%)      for (int i = 0; i < g->num_battles; i++) {
   230 ( 0.00%)          if (!!card_is_none(g->table_battles[i].defense)) { all_covered = false; break; }
     .               }
   576 ( 0.00%)      if (all_good && all_covered) execute_round_transition(g);
11,841 ( 0.01%)  => ../../../c/src/game.c:execute_round_transition (30x)
     .               return true;
 1,365 ( 0.00%)  }
     .           
     .           // One decoded action applied — the kind->handler dispatch every host used to
     .           // copy (iOS fio_apply_awire, the native server's apply_move_json, bot_drive's
     .           // own apply_move). The switch is a kernel fact, so it lives here beside the
     .           // handlers, and callers route through it instead of re-enumerating move types.
     .           // awire_decode already guarantees a cover frame's covering/attack cards pair up
     .           // n-for-n, so no arity re-check is needed. engine_last_reject holds the reason
     .           // on a false return; the caller owns any snapshot/event bookkeeping around it.
 1,662 ( 0.00%)  bool awire_apply(Game *g, int seat, const AwireAction *a) {
 6,648 ( 0.01%)      if (!g || !a || seat < 0 || seat >= g->num_players) return false;
     .               bool ok;
 9,141 ( 0.01%)      switch (a->kind) {
   789 ( 0.00%)          case AWIRE_ATTACK: ok = handle_attack(g, seat, a->cards, a->n); break;
60,018 ( 0.07%)  => ../../../c/src/game.c:handle_attack (263x)
   720 ( 0.00%)          case AWIRE_COVER:  ok = handle_cover(g, seat, a->cards, a->attacks, a->n); break;
48,559 ( 0.05%)  => ../../../c/src/game.c:handle_cover (180x)
    78 ( 0.00%)          case AWIRE_PASS:   ok = handle_pass(g, seat, a->cards, a->n); break;
 6,335 ( 0.01%)  => ../../../c/src/game.c:handle_pass (26x)
    89 ( 0.00%)          case AWIRE_PICKUP: ok = handle_pickup(g, seat); break;
29,962 ( 0.03%)  => ../../../c/src/game.c:handle_pickup (89x)
   273 ( 0.00%)          case AWIRE_GOOD:   ok = handle_good(g, seat); break;
30,187 ( 0.03%)  => ../../../c/src/game.c:handle_good (273x)
     .                   default:           return false;
     .               }
 1,928 ( 0.00%)      if (ok) game_settle_status(g);   // the kernel records its own game-over
     .               return ok;
   831 ( 0.00%)  }
     .           
     .           // Standalone entries mirroring the TS exports (see game.h).
     .           void engine_run_round_transition(Game *g) { execute_round_transition(g); }
     .           void engine_run_refill(Game *g) { refill_player_hands(g); }
     .           
     .           // ---------- should_bot_act --------------------------------------------
     .           
     .           bool should_bot_act(const Game *g, int bot_idx) {
-- line 1057 ----------------------------------------

42,037 ( 0.05%)  <counts for unidentified lines in ../../../c/src/game.c>

--------------------------------------------------------------------------------
-- Auto-annotated source: foolish_server.c
--------------------------------------------------------------------------------
Ir                 

-- line 189 ----------------------------------------
        .           // fresh /create). O(1) and never touches disk: just flips one bool in
        .           // persist.c's dirty bitmap for this slot's index and signals the
        .           // persistence thread — see persist.h's top comment for the full write-
        .           // behind model and DURABILITY.md for the design writeup + measurements.
        .           // `s - g_games` is safe pointer arithmetic: every GameSlot this function is
        .           // ever called with lives inside the g_games[MAX_GAMES] array (there is no
        .           // other way to obtain a GameSlot*), so the difference is always in range.
        .           static void game_mark_dirty(GameSlot *s) {
    4,677 ( 0.01%)      if (g_game_table) persist_mark_dirty(g_game_table, (int)(s - g_games));
      182 ( 0.00%)  => /home/user/foolish/server/impls/native/persist.c:persist_mark_dirty (26x)
        .           }
        .           
        .           // --------------------------------------------------------------------------
        .           // GameSlot <-> durable blob. serialize_slot/deserialize_slot are the ONLY
        .           // place that knows this on-disk layout; everything above (state_put/
        .           // state_get) is the kernel's own exact round-trip codec for `Game` — this
        .           // just wraps it with the lobby/identity fields view.c's codec deliberately
        .           // never carries (game.h: identity lives with the host, never in the state
-- line 205 ----------------------------------------
-- line 220 ----------------------------------------
        .           // PERSIST_GAME_BLOB_CAP gives real margin, same discipline as VIEW_CACHE_CAP.
        .           // --------------------------------------------------------------------------
        .           #define PERSIST_GAME_BLOB_VERSION 1
        .           #define PERSIST_GAME_BLOB_CAP 2048
        .           
        .           // Returns bytes written, or -1 if it wouldn't fit in `cap` (never happens at
        .           // PERSIST_GAME_BLOB_CAP given the worst case above — defensive, same
        .           // "correctness over the optimization" stance state_put_cached takes).
       18 ( 0.00%)  static int serialize_slot(const GameSlot *s, unsigned char *buf, int cap) {
        .               unsigned char state[1 + 65536];   // state_put's own documented cap (h_state uses the same 65536)
        8 ( 0.00%)      int state_len = state_put(&s->game, VIEW_UNMASKED, state);
    1,096 ( 0.00%)  => /home/user/foolish/server/impls/native/../../../c/src/view.c:state_put (2x)
        .               int need = 1 + 2 + state_len + (ID_LEN + 1) * 2
        .                        + MAX_PLAYERS * (ID_LEN + 1) + MAX_PLAYERS * 24 + MAX_PLAYERS;
        8 ( 0.00%)      if (state_len < 0 || need > cap) return -1;
        .               unsigned char *q = buf;
        2 ( 0.00%)      *q++ = PERSIST_GAME_BLOB_VERSION;
        2 ( 0.00%)      *q++ = (unsigned char)(state_len & 0xff);
        6 ( 0.00%)      *q++ = (unsigned char)((state_len >> 8) & 0xff);
       14 ( 0.00%)      memcpy(q, state, (size_t)state_len); q += state_len;
    1,104 ( 0.00%)  => ./elf/../sysdeps/x86_64/dl-trampoline.h:_dl_runtime_resolve_xsave (1x)
       24 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (2x)
        8 ( 0.00%)      memcpy(q, s->id, ID_LEN + 1); q += ID_LEN + 1;
        8 ( 0.00%)      memcpy(q, s->owner, ID_LEN + 1); q += ID_LEN + 1;
       64 ( 0.00%)      for (int i = 0; i < MAX_PLAYERS; i++) { memcpy(q, s->seat_user[i], ID_LEN + 1); q += ID_LEN + 1; }
       64 ( 0.00%)      for (int i = 0; i < MAX_PLAYERS; i++) { memcpy(q, s->seat_name[i], 24); q += 24; }
       32 ( 0.00%)      for (int i = 0; i < MAX_PLAYERS; i++) *q++ = (unsigned char)(s->seat_ready[i] ? 1 : 0);
        4 ( 0.00%)      return (int)(q - buf);
       12 ( 0.00%)  }
        .           
        .           // Inverse of serialize_slot. `s` MUST already be zeroed by the caller (same
        .           // contract h_create's fresh-slot memset follows) — this never touches
        .           // s->lock/s->cond/s->bot_running/s->version/s->view_cache* (runtime-only
        .           // fields the caller (re-)initializes separately; see game_persist_load /
        .           // h_create). Rejects (returns false, touches nothing) on a version mismatch
        .           // or a length too short for its own encoded state_len — defense in depth
        .           // against a corrupted DB row, never trusts `len` blindly (same posture
        .           // state_get's own bounds-clamping takes against a hostile/corrupt blob).
        .           static bool deserialize_slot(GameSlot *s, const unsigned char *buf, int len) {
       10 ( 0.00%)      if (len < 3 || buf[0] != PERSIST_GAME_BLOB_VERSION) return false;
        .               const unsigned char *q = buf + 1;
        2 ( 0.00%)      int state_len = q[0] | (q[1] << 8); q += 2;
        .               int fixed_tail = (ID_LEN + 1) * 2 + MAX_PLAYERS * (ID_LEN + 1) + MAX_PLAYERS * 24 + MAX_PLAYERS;
        3 ( 0.00%)      if (state_len < 0 || state_len > 65536 || 3 + state_len + fixed_tail > len) return false;
        7 ( 0.00%)      state_get(&s->game, q, /*masked=*/0);   // exact inverse of state_put(.., VIEW_UNMASKED, ..)
      518 ( 0.00%)  => /home/user/foolish/server/impls/native/../../../c/src/view.c:state_get (1x)
        .               q += state_len;
        5 ( 0.00%)      memcpy(s->id, q, ID_LEN + 1); s->id[ID_LEN] = 0; q += ID_LEN + 1;
        5 ( 0.00%)      memcpy(s->owner, q, ID_LEN + 1); s->owner[ID_LEN] = 0; q += ID_LEN + 1;
        .               for (int i = 0; i < MAX_PLAYERS; i++) {
       40 ( 0.00%)          memcpy(s->seat_user[i], q, ID_LEN + 1); s->seat_user[i][ID_LEN] = 0; q += ID_LEN + 1;
        .               }
        .               for (int i = 0; i < MAX_PLAYERS; i++) {
       40 ( 0.00%)          memcpy(s->seat_name[i], q, 24); s->seat_name[i][23] = 0; q += 24;
        .               }
       16 ( 0.00%)      for (int i = 0; i < MAX_PLAYERS; i++) s->seat_ready[i] = (*q++ != 0);
        2 ( 0.00%)      s->used = true;
        .               return true;
        1 ( 0.00%)  }
        .           
        .           // User <-> durable blob. Fixed-width (every User field already is), so no
        .           // length field is needed the way serialize_slot needs state_len.
        .           #define PERSIST_USER_BLOB_VERSION 1
        .           #define PERSIST_USER_BLOB_CAP 128
        .           
        .           static int serialize_user(const User *u, unsigned char *buf, int cap) {
        .               int need = 1 + (int)sizeof u->token + (int)sizeof u->user_id + (int)sizeof u->username;
-- line 282 ----------------------------------------
-- line 307 ----------------------------------------
        .           // decoration: a mismatch here means the wire format and the round-trip code
        .           // have drifted, and the server refuses to start rather than silently
        .           // persisting (or recovering) corrupt/lossy snapshots.
        .           static void persist_self_test(void) {
        .               // static: sizeof(GameSlot) is tens of KB (mostly the Game's MAX_LOGS-
        .               // sized log array — see game.h) and this runs once, so a static scratch
        .               // beats a fat stack frame.
        .               static GameSlot a, b;
        9 ( 0.00%)      memset(&a, 0, sizeof a);
   47,689 ( 0.05%)  => ./string/../sysdeps/x86_64/multiarch/memset-vec-unaligned-erms.S:__memset_avx2_unaligned_erms (1x)
      844 ( 0.00%)  => ./elf/../sysdeps/x86_64/dl-trampoline.h:_dl_runtime_resolve_xsave (1x)
        2 ( 0.00%)      a.used = true;
        3 ( 0.00%)      snprintf(a.id, sizeof a.id, "selftest0001");
        2 ( 0.00%)      snprintf(a.owner, sizeof a.owner, "selftest0001");
        1 ( 0.00%)      a.game.status = GAME_STATUS_PLAYING;
        .               a.game.num_players = 3;
        1 ( 0.00%)      a.game.power_suit = 2;
        .               a.game.first_attacker = 0;
        1 ( 0.00%)      a.game.defender = 1;
        .               a.game.deck_count = 5;
        2 ( 0.00%)      for (int i = 0; i < 5; i++) { a.game.deck[i].suit = (int8_t)(i % 4); a.game.deck[i].value = (int8_t)(5 + i); }
        .               a.game.num_battles = 1;
        6 ( 0.00%)      a.game.table_battles[0].attack.suit = 1; a.game.table_battles[0].attack.value = 9;
        .               a.game.table_battles[0].defense = CARD_NONE;
       24 ( 0.00%)      for (int i = 0; i < 3; i++) {
       28 ( 0.00%)          snprintf(a.seat_user[i], sizeof a.seat_user[i], "user%07d", i);
      828 ( 0.00%)  => ./elf/../sysdeps/x86_64/dl-trampoline.h:_dl_runtime_resolve_xsave (1x)
    1,808 ( 0.00%)  => ./stdio-common/./stdio-common/snprintf.c:snprintf (3x)
       24 ( 0.00%)          snprintf(a.seat_name[i], sizeof a.seat_name[i], "player-%d", i);
    1,490 ( 0.00%)  => ./stdio-common/./stdio-common/snprintf.c:snprintf (3x)
        6 ( 0.00%)          a.seat_ready[i] = (i % 2) == 0;
       21 ( 0.00%)          snprintf(a.game.players[i].name, sizeof a.game.players[i].name, "player-%d", i);
    1,490 ( 0.00%)  => ./stdio-common/./stdio-common/snprintf.c:snprintf (3x)
       21 ( 0.00%)          snprintf(a.game.players[i].player_id, sizeof a.game.players[i].player_id, "user%07d", i);
    1,808 ( 0.00%)  => ./stdio-common/./stdio-common/snprintf.c:snprintf (3x)
        3 ( 0.00%)          a.game.players[i].status = PLAYER_STATUS_IN;
       15 ( 0.00%)          a.game.players[i].strategy_key = (i == 2) ? 0 : STRATEGY_KEY_HUMAN;
        3 ( 0.00%)          a.game.players[i].hand_count = (int8_t)(2 + i);
       25 ( 0.00%)          for (int j = 0; j < a.game.players[i].hand_count; j++) {
        1 ( 0.00%)              a.game.players[i].hand[j].suit = (int8_t)((i + j) % 4);
       30 ( 0.00%)              a.game.players[i].hand[j].value = (int8_t)(6 + j);
        .                   }
        .               }
        .           
        .               static unsigned char blob1[PERSIST_GAME_BLOB_CAP], blob2[PERSIST_GAME_BLOB_CAP];
        4 ( 0.00%)      int n1 = serialize_slot(&a, blob1, sizeof blob1);
    1,795 ( 0.00%)  => foolish_server.c:serialize_slot (1x)
        2 ( 0.00%)      if (n1 < 0) { fprintf(stderr, "persist self-test: FAIL (serialize_slot didn't fit)\n"); exit(1); }
        6 ( 0.00%)      memset(&b, 0, sizeof b);
   47,697 ( 0.05%)  => ./string/../sysdeps/x86_64/multiarch/memset-vec-unaligned-erms.S:__memset_avx2_unaligned_erms (1x)
        6 ( 0.00%)      if (!deserialize_slot(&b, blob1, n1)) {
      654 ( 0.00%)  => foolish_server.c:deserialize_slot (1x)
        .                   fprintf(stderr, "persist self-test: FAIL (deserialize_slot rejected a round-trip blob)\n"); exit(1);
        .               }
        5 ( 0.00%)      int n2 = serialize_slot(&b, blob2, sizeof blob2);
      687 ( 0.00%)  => foolish_server.c:serialize_slot (1x)
       12 ( 0.00%)      if (n2 != n1 || memcmp(blob1, blob2, (size_t)n1) != 0) {
      831 ( 0.00%)  => ./elf/../sysdeps/x86_64/dl-trampoline.h:_dl_runtime_resolve_xsave (1x)
       77 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memcmp-avx2-movbe.S:__memcmp_avx2_movbe (1x)
        .                   fprintf(stderr, "persist self-test: FAIL (serialize->deserialize->serialize not byte-identical, "
        .                                    "n1=%d n2=%d)\n", n1, n2);
        .                   exit(1);
        .               }
       11 ( 0.00%)      fprintf(stderr, "persist self-test: OK (games: %d-byte round-trip byte-identical)\n", n1);
      920 ( 0.00%)  => ./elf/../sysdeps/x86_64/dl-trampoline.h:_dl_runtime_resolve_xsave (1x)
      889 ( 0.00%)  => ./stdio-common/./stdio-common/fprintf.c:fprintf (1x)
        .           }
        .           
        .           // --------------------------------------------------------------------------
        .           // Locking (T2a introduced the two-tier scheme below, replacing the single
        .           // global g_lock; Stage 5, SERVER_SCALING.md "Stage 5 — parallel bot compute",
        .           // DROPPED the THIRD lock T2a added on top of it — read on for why that is now
        .           // safe). Two tiers remain:
        .           //
-- line 365 ----------------------------------------
-- line 462 ----------------------------------------
        .           // writeup, how to run with certs, and the measured overhead. The one
        .           // non-uniform spot the seam comment (and SERVER_SCALING.md/DURABILITY.md's
        .           // "Seams left" sections) called out ahead of time: ws_send_frame's unmasked
        .           // server path used writev() to send a header+payload in one syscall;
        .           // OpenSSL has no vector write, so ws.c's TLS branch concatenates into one
        .           // buffer instead (see its comment) — everything else was a drop-in swap,
        .           // exactly as predicted.
        .           // --------------------------------------------------------------------------
      168 ( 0.00%)  static ssize_t io_read(Conn *c, void *buf, size_t n)  { return conn_read(c, buf, n); }
    4,510 ( 0.00%)  => /home/user/foolish/server/impls/native/conn.c:conn_read (42x)
      165 ( 0.00%)  static ssize_t io_write(Conn *c, const void *buf, size_t n) { return conn_write(c, buf, n); }
    2,241 ( 0.00%)  => /home/user/foolish/server/impls/native/conn.c:conn_write (32x)
        .           
        .           // --------------------------------------------------------------------------
        .           // Small utilities
        .           // --------------------------------------------------------------------------
        .           
        .           // POSIX doesn't guarantee libc rand() is thread-safe (glibc's implementation
        .           // shares unlocked global state across callers), and every worker-pool size
        .           // in this file (game/meta/create) is now runtime-configurable — see "Work-
        .           // queue thread routing" below — so no call site here gets to assume it's the
        .           // only thread reaching it. next_rand() gives every thread its own rand_r
        .           // state instead: seeded once per thread from the clock + thread id, no lock
        .           // needed because nothing is shared.
        .           static _Thread_local unsigned int t_rand_seed;
        .           static _Thread_local bool t_rand_seeded = false;
        .           static unsigned int next_rand(void) {
      286 ( 0.00%)      if (!t_rand_seeded) {
       35 ( 0.00%)          t_rand_seed = (unsigned int)((uintptr_t)pthread_self() ^ (uintptr_t)time(NULL) ^ (uintptr_t)getpid());
      857 ( 0.00%)  => ./elf/../sysdeps/x86_64/dl-trampoline.h:_dl_runtime_resolve_xsave (1x)
        6 ( 0.00%)  => ./time/../sysdeps/unix/sysv/linux/time.c:time_syscall (1x)
        4 ( 0.00%)  => ./posix/../sysdeps/unix/syscall-template.S:getpid (1x)
        3 ( 0.00%)  => ./nptl/./nptl/pthread_self.c:pthread_self (1x)
        3 ( 0.00%)          t_rand_seeded = true;
        .               }
      347 ( 0.00%)      return (unsigned int)rand_r(&t_rand_seed);
      855 ( 0.00%)  => ./elf/../sysdeps/x86_64/dl-trampoline.h:_dl_runtime_resolve_xsave (1x)
      285 ( 0.00%)  => ./stdlib/./stdlib/rand_r.c:rand_r (15x)
        .           }
        .           
      135 ( 0.00%)  static void gen_id(char *out, int n) {
        .               static const char hex[] = "0123456789abcdef";
       45 ( 0.00%)      unsigned long v = (atomic_fetch_add_explicit(&g_seq, 1, memory_order_relaxed) + 1)
      105 ( 0.00%)                         ^ ((unsigned long)next_rand() << 8) ^ (unsigned long)time(NULL);
       90 ( 0.00%)  => ./time/../sysdeps/unix/sysv/linux/time.c:time_syscall (15x)
    2,730 ( 0.00%)      for (int i = 0; i < n; i++) { out[i] = hex[v & 0xf]; v = v * 6364136223846793005UL + 1442695040888963407UL; v >>= 3; }
       15 ( 0.00%)      out[n] = 0;
      120 ( 0.00%)  }
        .           
        .           // Minimal JSON scrapers — enough for our flat request bodies. Not a parser.
      572 ( 0.00%)  static bool json_str(const char *body, const char *key, char *out, int cap) {
      260 ( 0.00%)      char pat[64]; snprintf(pat, sizeof pat, "\"%s\"", key);
   23,076 ( 0.03%)  => ./stdio-common/./stdio-common/snprintf.c:snprintf (52x)
      264 ( 0.00%)      const char *p = body ? strstr(body, pat) : NULL;
    4,406 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/strstr-sse2-unaligned.S:__strstr_sse2_unaligned (52x)
      874 ( 0.00%)  => ./elf/../sysdeps/x86_64/dl-trampoline.h:_dl_runtime_resolve_xsave (1x)
      104 ( 0.00%)      if (!p) return false;
      476 ( 0.00%)      p = strchr(p + strlen(pat), ':'); if (!p) return false;
    1,756 ( 0.00%)  => ./elf/../sysdeps/x86_64/dl-trampoline.h:_dl_runtime_resolve_xsave (2x)
    1,092 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/strchr-avx2.S:__strchr_avx2 (52x)
      728 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/strlen-avx2.S:__strlen_avx2 (52x)
      624 ( 0.00%)      p++; while (*p == ' ' || *p == '"') p++;
    5,706 ( 0.01%)      int i = 0; while (*p && *p != '"' && *p != ',' && *p != '}' && i < cap - 1) out[i++] = *p++;
      208 ( 0.00%)      out[i] = 0; return i > 0;
      416 ( 0.00%)  }
        .           
        .           // --------------------------------------------------------------------------
        .           // token -> User* / game_id -> GameSlot* : fixed-size open-addressing hash
        .           // maps (PROFILE_HOTPATH.md T1 report item 2 — these two were O(MAX_USERS)/
        .           // O(MAX_GAMES) linear strcmp scans on EVERY authenticated request, 1.08M
        .           // strcmp calls in one 20s hammer run). Both g_users/g_games are fixed
        .           // static arrays whose elements never move or get freed for the life of the
        .           // process (a POC store — no delete), so raw pointers into them are safe to
-- line 519 ----------------------------------------
-- line 526 ----------------------------------------
        .           #define TOKEN_HT_SIZE 1024   // power of two, > 2x MAX_USERS
        .           #define GAME_HT_SIZE   512   // power of two, > 2x MAX_GAMES
        .           
        .           static User     *g_token_ht[TOKEN_HT_SIZE];
        .           static GameSlot *g_game_ht[GAME_HT_SIZE];
        .           
        .           static unsigned long hash_str(const char *s) {
        .               unsigned long h = 1469598103934665603UL;   // FNV-1a, 64-bit offset basis
   11,892 ( 0.01%)      while (*s) { h ^= (unsigned char)*s++; h *= 1099511628211UL; }
        .               return h;
        .           }
        .           
        .           static void token_ht_insert(User *u) {
        .               unsigned long h = hash_str(u->token) & (TOKEN_HT_SIZE - 1);
       24 ( 0.00%)      while (g_token_ht[h]) h = (h + 1) & (TOKEN_HT_SIZE - 1);
        6 ( 0.00%)      g_token_ht[h] = u;
        .           }
        .           static void game_ht_insert(GameSlot *s) {
        .               unsigned long h = hash_str(s->id) & (GAME_HT_SIZE - 1);
       12 ( 0.00%)      while (g_game_ht[h]) h = (h + 1) & (GAME_HT_SIZE - 1);
        3 ( 0.00%)      g_game_ht[h] = s;
        .           }
        .           
        .           // Both lookups below: caller MUST hold g_registry_lock.
        .           static User *user_by_token(const char *token) {
      119 ( 0.00%)      if (!token || !*token) return NULL;
       32 ( 0.00%)      unsigned long h = hash_str(token) & (TOKEN_HT_SIZE - 1);
        .               for (int probes = 0; probes < TOKEN_HT_SIZE; probes++) {
       93 ( 0.00%)          User *u = g_token_ht[h];
       64 ( 0.00%)          if (!u) return NULL;   // insert-only table: an empty slot ends the probe chain
      285 ( 0.00%)          if (u->used && strcmp(u->token, token) == 0) return u;
      976 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/strcmp-avx2.S:__strcmp_avx2 (26x)
        .                   h = (h + 1) & (TOKEN_HT_SIZE - 1);
        .               }
        .               return NULL;
        .           }
        .           static GameSlot *game_by_id(const char *id) {
       87 ( 0.00%)      if (!id || !*id) return NULL;
       29 ( 0.00%)      unsigned long h = hash_str(id) & (GAME_HT_SIZE - 1);
        .               for (int probes = 0; probes < GAME_HT_SIZE; probes++) {
       81 ( 0.00%)          GameSlot *s = g_game_ht[h];
       58 ( 0.00%)          if (!s) return NULL;
      255 ( 0.00%)          if (s->used && strcmp(s->id, id) == 0) return s;
      603 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/strcmp-avx2.S:__strcmp_avx2 (23x)
        .                   h = (h + 1) & (GAME_HT_SIZE - 1);
        .               }
        .               return NULL;
        .           }
        .           // Whether a seat is a bot: the kernel's own per-seat fact now (strategy_key),
        .           // not a server-side is_ai array. A human seat is STRATEGY_KEY_HUMAN; a bot holds
        .           // its roster index. Caller MUST hold `s`'s own lock (reads s->game).
       70 ( 0.00%)  static bool seat_is_bot(const Game *g, int i) { return g->players[i].strategy_key != STRATEGY_KEY_HUMAN; }
        .           
        .           // Caller MUST hold `s`'s own lock (reads s->game and s->seat_user).
        .           static int seat_of(GameSlot *s, const char *user_id) {
      158 ( 0.00%)      for (int i = 0; i < s->game.num_players; i++)
      210 ( 0.00%)          if (!seat_is_bot(&s->game, i) && strcmp(s->seat_user[i], user_id) == 0) return i;
      420 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/strcmp-avx2.S:__strcmp_avx2 (21x)
        .               return -1;
        .           }
        .           
        .           // The bot game-loop, one thread per game — a TRAMPOLINE, not a blocking hook.
        .           // Each pass drives exactly ONE kernel cycle and RETURNS from the kernel; the
        .           // server then decides how to wait. Same split as supabase (which `await`s a
        .           // setTimeout) and the phone (Task.sleep): the KERNEL owns the cycle and the
        .           // delay value (bot_drive + bot_pacing_ms); the host owns how it waits. The
-- line 588 ----------------------------------------
-- line 590 ----------------------------------------
        .           //
        .           // The lock is `s->lock` — this game's own, not a process-wide one (T2a) — held
        .           // while touching the game and RELEASED during the pacing sleep, so bots think
        .           // + throw in over time while other requests for OTHER games (and, thanks to
        .           // the per-game lock, even other requests for THIS game between cycles) keep
        .           // serving. When no bot can act (a human is owed) the thread waits on `cond`
        .           // until /action or /ws signals it — pairing the wait with `s->lock`, same as
        .           // every other access to this slot.
       36 ( 0.00%)  static void *bot_thread(void *arg) {
        .               GameSlot *s = arg;
       16 ( 0.00%)      pthread_mutex_lock(&s->lock);
      171 ( 0.00%)  => ./nptl/./nptl/pthread_mutex_lock.c:pthread_mutex_lock@@GLIBC_2.2.5 (4x)
    1,968 ( 0.00%)      while (s->used && s->game.status == GAME_STATUS_PLAYING) {
    1,452 ( 0.00%)          uint32_t hmask = game_human_mask(&s->game);   // pure per-Game field read
   17,908 ( 0.02%)  => /home/user/foolish/server/impls/native/../../../c/src/game.c:game_human_mask (484x)
        .                   BotDriveOut drv;
        .                   // No g_kernel_lock (Stage 5, see "Locking" above): bot_drive's
        .                   // scratch state is now _Thread_local, so this game's bot_thread
        .                   // never shares it with another game's — s->lock (already held for
        .                   // this whole cycle) is the only serialization this needs.
    3,388 ( 0.00%)          bot_drive(&s->game, hmask, BOT_DRIVE_MAX_ACTIONS, 0, 0, &drv);   // ONE cycle, then returns
   54,692 ( 0.06%)  => /home/user/foolish/server/impls/native/../../../c/src/bot_drive.c:bot_drive (484x)
        .                   // A bot's move (or the game ending) changes the board exactly like a
        .                   // human's /action does — the /ws state cache must not stay stale
        .                   // just because no HTTP handler touched this slot this time.
    1,936 ( 0.00%)          if (drv.n > 0 || drv.ended >= 0) {
        .                       s->version++; game_mark_dirty(s);
        .                       // Stage 6: the one cross-thread epoll seam — see
        .                       // epoll_notify_game_changed's own doc (near the epoll worker
        .                       // machinery, end of file) for why this specific call site is it.
        .                       epoll_notify_game_changed(s);
        .                   }
        .                   // Stage 4 instrumentation (see g_bot_decisions/g_octogen_decisions'
        .                   // doc above): count every action this cycle actually applied, and —
-- line 620 ----------------------------------------
-- line 631 ----------------------------------------
        .                                   s->game.players[aseat].strategy_key == g_octogen_strat)
        .                                   oct++;
        .                           }
        .                           if (oct > 0) atomic_fetch_add_explicit(&g_octogen_decisions, oct, memory_order_relaxed);
        .                       }
        .                   }
        .           
        .                   if (drv.ended >= 0) break;   // the kernel already flipped g->status to GAME_OVER
      968 ( 0.00%)          if (drv.stop == BOT_STOP_NO_ELIGIBLE) {              // a human's move is owed
    1,452 ( 0.00%)              pthread_cond_wait(&s->cond, &s->lock);            // sleep until /action or /ws wakes us
  140,983 ( 0.15%)  => ./nptl/./nptl/pthread_cond_wait.c:pthread_cond_wait@@GLIBC_2.3.2 (484x)
        .                       continue;
        .                   }
        .           
        .                   // A visible cycle landed — the kernel prices the wait; the host owns the
        .                   // loop and the sleep (the trampoline). Lock released while we wait.
        .                   int delay = bot_cycle_delay_ms(&s->game, hmask, &drv);
        .                   if (delay > 0) {
        .                       pthread_mutex_unlock(&s->lock);
        .                       usleep((useconds_t)delay * 1000);
        .                       pthread_mutex_lock(&s->lock);
        .                   }
        .               }
        4 ( 0.00%)      s->bot_running = false;
       12 ( 0.00%)      pthread_mutex_unlock(&s->lock);
      148 ( 0.00%)  => ./nptl/./nptl/pthread_mutex_unlock.c:pthread_mutex_unlock@@GLIBC_2.2.5 (4x)
       36 ( 0.00%)      return NULL;
        .           }
        .           
        .           // Spawn the game-loop for a freshly dealt game (idempotent). Caller MUST
        .           // hold s->lock: bot_thread's very first action is to lock it too, so this
        .           // just races the parent's own unlock (harmless — see bot_thread's doc).
        .           static void start_bot_loop(GameSlot *s) {
        8 ( 0.00%)      if (s->bot_running) return;
        4 ( 0.00%)      s->bot_running = true;
        .               pthread_t t;
       44 ( 0.00%)      if (pthread_create(&t, NULL, bot_thread, s) == 0) pthread_detach(t);
  420,632 ( 0.46%)  => ./nptl/./nptl/pthread_create.c:pthread_create@@GLIBC_2.34 (4x)
       52 ( 0.00%)  => ./nptl/./nptl/pthread_detach.c:pthread_detach@@GLIBC_2.34 (4x)
        .               else s->bot_running = false;
       24 ( 0.00%)  }
        .           
        .           // --------------------------------------------------------------------------
        .           // STAGE 2: persist.c callbacks. Snapshot functions run ONLY on the
        .           // persistence thread (see persist.h's PersistSnapshotFn doc) — each takes
        .           // its OWN short-held domain lock just long enough to copy the live data,
        .           // releases it, THEN does the (slower, structured) serialize work with no
        .           // lock held at all: exactly the "briefly take the lock, memcpy, release,
        .           // serialize unlocked" split the design calls for, so a slow disk never
-- line 675 ----------------------------------------
-- line 790 ----------------------------------------
        .           } Req;
        .           
        .           // Finds the blank line ending the header block ("\r\n\r\n") by a plain byte
        .           // scan instead of strstr (PROFILE_HOTPATH.md T1 report item 3: strstr's
        .           // two-way-search internals showed up as real cost even for this 4-byte
        .           // literal). Returns a pointer just PAST the blank line, or NULL if not seen
        .           // yet in the bytes read so far.
        .           static char *find_headers_end(char *buf, int n) {
   27,639 ( 0.03%)      for (int i = 0; i + 3 < n; i++)
   11,930 ( 0.01%)          if (buf[i] == '\r' && buf[i + 1] == '\n' && buf[i + 2] == '\r' && buf[i + 3] == '\n')
        .                       return buf + i + 4;
        .               return NULL;
        .           }
        .           
        .           // Hand-rolled request-line + header parser — replaces both
        .           // `sscanf(buf, "%7s %255s", method, path)` (2.1% of all instructions under
        .           // load, __vfscanf_internal) and the `strcasestr` header scan (content-length:
        .           // + authorization:, another ~3% combined) with one single pass over the
        .           // header block using strchr-style manual scanning and fixed-width
        .           // `strncasecmp` on each line's already-located header name. Same semantics:
        .           // method/path/query split on the request line, Content-Length, a Bearer
        .           // token, and (new) whatever the WebSocket upgrade needs.
        .           static void parse_request_line_and_headers(char *buf, char *hdr_end, Req *r) {
      378 ( 0.00%)      memset(r, 0, sizeof *r);
    2,394 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memset-vec-unaligned-erms.S:__memset_avx2_unaligned_erms (42x)
        .               char *p = buf;
        .           
        .               char *s = p;
    1,632 ( 0.00%)      while (p < hdr_end && *p != ' ' && *p != '\r' && *p != '\n') p++;
      210 ( 0.00%)      int len = (int)(p - s); if (len > (int)sizeof r->method - 1) len = (int)sizeof r->method - 1;
      336 ( 0.00%)      memcpy(r->method, s, (size_t)len); r->method[len] = 0;
      660 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (42x)
      294 ( 0.00%)      if (p < hdr_end && *p == ' ') p++;
        .           
        .               s = p;
    2,142 ( 0.00%)      while (p < hdr_end && *p != ' ' && *p != '?' && *p != '\r' && *p != '\n') p++;
      294 ( 0.00%)      len = (int)(p - s); if (len > (int)sizeof r->path - 1) len = (int)sizeof r->path - 1;
      294 ( 0.00%)      memcpy(r->path, s, (size_t)len); r->path[len] = 0;
      636 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (42x)
      126 ( 0.00%)      if (p < hdr_end && *p == '?') {
        .                   p++; s = p;
    1,020 ( 0.00%)          while (p < hdr_end && *p != ' ' && *p != '\r' && *p != '\n') p++;
       36 ( 0.00%)          len = (int)(p - s); if (len > (int)sizeof r->query - 1) len = (int)sizeof r->query - 1;
       54 ( 0.00%)          memcpy(r->query, s, (size_t)len); r->query[len] = 0;
       66 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (6x)
        .               }
        .               // Skip the rest of the request line (HTTP version) up to its CRLF.
    3,066 ( 0.00%)      while (p < hdr_end && *p != '\n') p++;
       42 ( 0.00%)      if (p < hdr_end) p++;
        .           
        .               bool saw_upgrade_websocket = false, saw_connection_upgrade = false;
      496 ( 0.00%)      while (p < hdr_end) {
        .                   char *line_start = p;
        .                   char *colon = NULL;
   50,746 ( 0.06%)          while (p < hdr_end && *p != '\r' && *p != '\n') { if (!colon && *p == ':') colon = p; p++; }
        .                   char *line_end = p;
      824 ( 0.00%)          if (p < hdr_end && *p == '\r') p++;
    1,236 ( 0.00%)          if (p < hdr_end && *p == '\n') p++;
      740 ( 0.00%)          if (!colon || line_start == colon) continue;   // blank/malformed line — skip
        .           
        .                   int name_len = (int)(colon - line_start);
        .                   const char *val = colon + 1;
    2,952 ( 0.00%)          while (val < line_end && (*val == ' ' || *val == '\t')) val++;
        .                   int val_len = (int)(line_end - val);
        .           
    1,011 ( 0.00%)          if (name_len == 14 && strncasecmp(line_start, "content-length", 14) == 0) {
    1,769 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/strcmp-avx2.S:__strncasecmp_avx2 (29x)
        .                       int cl = 0;
      725 ( 0.00%)              for (const char *q = val; q < line_end && *q >= '0' && *q <= '9'; q++) cl = cl * 10 + (*q - '0');
       58 ( 0.00%)              r->content_length = cl;
      224 ( 0.00%)          } else if (name_len == 13 && strncasecmp(line_start, "authorization", 13) == 0) {
    1,952 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/strcmp-avx2.S:__strncasecmp_avx2 (32x)
      224 ( 0.00%)              if (val_len > 7 && strncasecmp(val, "Bearer ", 7) == 0) {
    2,048 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/strcmp-avx2.S:__strncasecmp_avx2 (32x)
       32 ( 0.00%)                  const char *tok = val + 7;
      128 ( 0.00%)                  int tlen = val_len - 7; if (tlen > (int)sizeof r->token - 1) tlen = (int)sizeof r->token - 1;
      192 ( 0.00%)                  memcpy(r->token, tok, (size_t)tlen); r->token[tlen] = 0;
      384 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (32x)
        .                       }
       42 ( 0.00%)          } else if (name_len == 7 && strncasecmp(line_start, "upgrade", 7) == 0) {
      384 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/strcmp-avx2.S:__strncasecmp_avx2 (6x)
        .                       for (int i = 0; i + 9 <= val_len; i++)
       42 ( 0.00%)                  if (strncasecmp(val + i, "websocket", 9) == 0) { saw_upgrade_websocket = true; break; }
      516 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/strcmp-avx2.S:__strncasecmp_avx2 (6x)
      228 ( 0.00%)          } else if (name_len == 10 && strncasecmp(line_start, "connection", 10) == 0) {
      994 ( 0.00%)  => ./elf/../sysdeps/x86_64/dl-trampoline.h:_dl_runtime_resolve_xsave (1x)
    1,769 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/strcmp-avx2.S:__strncasecmp_avx2 (42x)
        .                       for (int i = 0; i + 7 <= val_len; i++)
       42 ( 0.00%)                  if (strncasecmp(val + i, "upgrade", 7) == 0) { saw_connection_upgrade = true; break; }
      384 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/strcmp-avx2.S:__strncasecmp_avx2 (6x)
       36 ( 0.00%)          } else if (name_len == 17 && strncasecmp(line_start, "sec-websocket-key", 17) == 0) {
      252 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/strcmp-avx2.S:__strncasecmp_avx2 (6x)
       18 ( 0.00%)              int klen = val_len; if (klen > (int)sizeof r->ws_key - 1) klen = (int)sizeof r->ws_key - 1;
       48 ( 0.00%)              memcpy(r->ws_key, val, (size_t)klen); r->ws_key[klen] = 0;
       66 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (6x)
        .                   }
        .               }
      144 ( 0.00%)      r->is_ws_upgrade = saw_upgrade_websocket && saw_connection_upgrade && r->ws_key[0];
        .           }
        .           
        .           // Reads a full HTTP request (headers, then body up to Content-Length) off
        .           // `conn` into caller-owned `buf` (>= cap bytes), and parses it into `r`. `r->
        .           // body`/`r->body_len` end up pointing INTO `buf` — the caller must keep
        .           // `buf` alive for as long as `r` is used (see WorkItem/WsSpawnArg below,
        .           // which both carry the buffer alongside the parsed Req for exactly this
        .           // reason). Returns false on a malformed/empty request (caller should just
        .           // close the conn and free buf).
        .           #define REQ_BUF_CAP (1 << 16)
        .           static bool read_and_parse_request(Conn *conn, char *buf, int cap, Req *r) {
        .               int total = 0, n;
        .               char *hdr_end = NULL;
      294 ( 0.00%)      while ((n = (int)io_read(conn, buf + total, (size_t)(cap - 1 - total))) > 0) {
      126 ( 0.00%)          total += n; buf[total] = 0;
        .                   hdr_end = find_headers_end(buf, total);
        .                   if (!hdr_end) continue;
        .                   break;
        .               }
        .               if (!hdr_end) return false;   // malformed / empty request
        .           
        .               parse_request_line_and_headers(buf, hdr_end, r);
        .               // Keep reading if the body (by Content-Length) hasn't fully arrived yet.
        .               int have = total - (int)(hdr_end - buf);
      210 ( 0.00%)      while (have < r->content_length && (n = (int)io_read(conn, buf + total, (size_t)(cap - 1 - total))) > 0) {
        .                   total += n; buf[total] = 0;
        .                   have = total - (int)(hdr_end - buf);
        .               }
       42 ( 0.00%)      r->body = hdr_end;
       42 ( 0.00%)      r->body_len = total - (int)(hdr_end - buf);   // real byte count (body may be binary awire)
        .               return true;
        .           }
        .           
        .           static void respond(Conn *conn, int code, const char *json) {
        .               const char *msg = code == 200 ? "OK" : code == 400 ? "Bad Request"
        .                               : code == 401 ? "Unauthorized" : code == 404 ? "Not Found" : "Error";
        .               char hdr[512];
      328 ( 0.00%)      int n = snprintf(hdr, sizeof hdr,
   37,524 ( 0.04%)  => ./stdio-common/./stdio-common/snprintf.c:snprintf (32x)
        .                   "HTTP/1.1 %d %s\r\nContent-Type: application/json\r\n"
        .                   "Access-Control-Allow-Origin: *\r\nAccess-Control-Allow-Headers: *\r\n"
        .                   "Content-Length: %zu\r\nConnection: close\r\n\r\n",
      102 ( 0.00%)          code, msg, strlen(json));
      728 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/strlen-avx2.S:__strlen_avx2 (32x)
       36 ( 0.00%)      io_write(conn, hdr, n);
      102 ( 0.00%)      io_write(conn, json, strlen(json));
      728 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/strlen-avx2.S:__strlen_avx2 (32x)
        .           }
        .           
        .           // Raw bytes (the packed kernel wire) — no JSON. The client decodes with its own
        .           // kernel-wire reader (MaskedView etc.).
        .           static void respond_bin(Conn *conn, int code, const unsigned char *data, int len) {
        .               char hdr[256];
        .               int n = snprintf(hdr, sizeof hdr,
        .                   "HTTP/1.1 %d OK\r\nContent-Type: application/octet-stream\r\n"
-- line 924 ----------------------------------------
-- line 933 ----------------------------------------
        .           // "Locking" above): take g_registry_lock, find/allocate the User*/GameSlot*
        .           // (copying out any User field it still needs as a local — the User itself
        .           // is only safe to dereference while g_registry_lock is held), take the
        .           // GameSlot's own `lock`, release g_registry_lock, do the game work, release
        .           // the GameSlot lock, THEN respond (I/O never happens with either lock held).
        .           // --------------------------------------------------------------------------
        .           
        .           static void h_signup(Req *r, Conn *conn) {
       18 ( 0.00%)      char uname[24] = {0};
       42 ( 0.00%)      if (!json_str(r->body, "username", uname, sizeof uname)) { respond(conn, 400, "{\"error\":\"username\"}"); return; }
    7,856 ( 0.01%)  => foolish_server.c:json_str (6x)
       18 ( 0.00%)      pthread_mutex_lock(&g_registry_lock);
      216 ( 0.00%)  => ./nptl/./nptl/pthread_mutex_lock.c:pthread_mutex_lock@@GLIBC_2.2.5 (6x)
        .               User *u = NULL;
   15,450 ( 0.02%)      for (int i = 0; i < MAX_USERS; i++) if (g_users[i].used && !strcmp(g_users[i].username, uname)) { u = &g_users[i]; break; }
      390 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/strcmp-avx2.S:__strcmp_avx2 (15x)
       69 ( 0.00%)      if (!u) for (int i = 0; i < MAX_USERS; i++) if (!g_users[i].used) {
       64 ( 0.00%)          u = &g_users[i]; u->used = true; snprintf(u->username, sizeof u->username, "%s", uname);
    2,196 ( 0.00%)  => ./stdio-common/./stdio-common/snprintf.c:snprintf (6x)
       24 ( 0.00%)          gen_id(u->user_id, ID_LEN); break;
    2,817 ( 0.00%)  => foolish_server.c:gen_id (6x)
        .               }
        .               // Fresh session token, indexed, and marked dirty for the persistence
        .               // thread (Stage 2) — a signup/signin the DB never learns about would
        .               // strand that user's token past a crash, same durability need as a
        .               // game's state (see game_mark_dirty's doc for the write-behind model).
        .               if (u) {
       24 ( 0.00%)          gen_id(u->token, 32);
    2,088 ( 0.00%)  => foolish_server.c:gen_id (6x)
        .                   token_ht_insert(u);
       48 ( 0.00%)          if (g_user_table) persist_mark_dirty(g_user_table, (int)(u - g_users));
       42 ( 0.00%)  => /home/user/foolish/server/impls/native/persist.c:persist_mark_dirty (6x)
        .               }
        .               char out[160];
       66 ( 0.00%)      if (u) snprintf(out, sizeof out, "{\"token\":\"%s\",\"user_id\":\"%s\",\"username\":\"%s\"}", u->token, u->user_id, u->username);
    5,293 ( 0.01%)  => ./stdio-common/./stdio-common/snprintf.c:snprintf (6x)
       18 ( 0.00%)      pthread_mutex_unlock(&g_registry_lock);
      156 ( 0.00%)  => ./nptl/./nptl/pthread_mutex_unlock.c:pthread_mutex_unlock@@GLIBC_2.2.5 (6x)
        .               if (u) respond(conn, 200, out); else respond(conn, 400, "{\"error\":\"full\"}");
        .           }
        .           
        .           static void h_create(Req *r, Conn *conn) {
        9 ( 0.00%)      pthread_mutex_lock(&g_registry_lock);
      108 ( 0.00%)  => ./nptl/./nptl/pthread_mutex_lock.c:pthread_mutex_lock@@GLIBC_2.2.5 (3x)
        .               User *u = user_by_token(r->token);
        .               if (!u) { pthread_mutex_unlock(&g_registry_lock); respond(conn, 401, "{\"error\":\"auth\"}"); return; }
       27 ( 0.00%)      char user_id[ID_LEN + 1]; snprintf(user_id, sizeof user_id, "%s", u->user_id);
    1,104 ( 0.00%)  => ./stdio-common/./stdio-common/snprintf.c:snprintf (3x)
       21 ( 0.00%)      char username[24]; snprintf(username, sizeof username, "%s", u->username);
    1,098 ( 0.00%)  => ./stdio-common/./stdio-common/snprintf.c:snprintf (3x)
        .           
        .               GameSlot *s = NULL;
       22 ( 0.00%)      for (int i = 0; i < MAX_GAMES; i++) if (!g_games[i].used) { s = &g_games[i]; break; }
        .               if (!s) { pthread_mutex_unlock(&g_registry_lock); respond(conn, 400, "{\"error\":\"full\"}"); return; }
       17 ( 0.00%)      memset(s, 0, sizeof *s);
  143,091 ( 0.16%)  => ./string/../sysdeps/x86_64/multiarch/memset-vec-unaligned-erms.S:__memset_avx2_unaligned_erms (3x)
       21 ( 0.00%)      pthread_mutex_init(&s->lock, NULL);
      135 ( 0.00%)  => ./nptl/./nptl/pthread_mutex_init.c:pthread_mutex_init@@GLIBC_2.2.5 (3x)
       15 ( 0.00%)      pthread_cond_init(&s->cond, NULL);
       30 ( 0.00%)  => ./nptl/./nptl/pthread_cond_init.c:pthread_cond_init@@GLIBC_2.3.2 (3x)
        3 ( 0.00%)      s->used = true;
        .               // s->version starts at 0 (memset); view_cache_version must start at a
        .               // value that can NEVER equal it, or state_put_cached's very first call
        .               // for a seat would see version==cache_version (both zeroed) and return
        .               // the also-zeroed, never-computed view_cache_len (0 bytes) instead of
        .               // actually serializing — a silent "client gets an empty state" bug. The
        .               // +1 covers the shared spectator cache slot too (index MAX_PLAYERS —
        .               // Stage 4, SPECTATOR_CACHE_IDX).
       12 ( 0.00%)      for (int i = 0; i < MAX_PLAYERS + 1; i++) s->view_cache_version[i] = (uint32_t)-1;
       15 ( 0.00%)      gen_id(s->id, ID_LEN);
      534 ( 0.00%)  => foolish_server.c:gen_id (3x)
        .               // LOCK ORDER: registry, then this fresh slot's own lock — never the
        .               // reverse (see g_registry_lock's declaration). No other thread can find
        .               // this slot before game_ht_insert runs, so taking s->lock here is
        .               // uncontended; it's only here for symmetry with every other handler's
        .               // registry->game handoff, so nothing outside this function ever touches
        .               // a GameSlot without its lock held.
        9 ( 0.00%)      pthread_mutex_lock(&s->lock);
      108 ( 0.00%)  => ./nptl/./nptl/pthread_mutex_lock.c:pthread_mutex_lock@@GLIBC_2.2.5 (3x)
        .               game_ht_insert(s);
        9 ( 0.00%)      pthread_mutex_unlock(&g_registry_lock);   // registry work done; the rest is s->lock-only
       78 ( 0.00%)  => ./nptl/./nptl/pthread_mutex_unlock.c:pthread_mutex_unlock@@GLIBC_2.2.5 (3x)
        .           
       27 ( 0.00%)      snprintf(s->owner, sizeof s->owner, "%s", user_id);
    1,104 ( 0.00%)  => ./stdio-common/./stdio-common/snprintf.c:snprintf (3x)
        .               // Seat 0 = creator. Identity lives here; the kernel state is dealt at start.
        3 ( 0.00%)      Game *g = &s->game; g->num_players = 1; g->status = GAME_STATUS_WAITING;
       24 ( 0.00%)      snprintf(s->seat_user[0], ID_LEN + 1, "%s", user_id);
    1,104 ( 0.00%)  => ./stdio-common/./stdio-common/snprintf.c:snprintf (3x)
       27 ( 0.00%)      snprintf(s->seat_name[0], 24, "%s", username);
    1,098 ( 0.00%)  => ./stdio-common/./stdio-common/snprintf.c:snprintf (3x)
       24 ( 0.00%)      snprintf(g->players[0].name, 24, "%s", username);
    1,098 ( 0.00%)  => ./stdio-common/./stdio-common/snprintf.c:snprintf (3x)
       24 ( 0.00%)      snprintf(g->players[0].player_id, 24, "%s", user_id);
    1,104 ( 0.00%)  => ./stdio-common/./stdio-common/snprintf.c:snprintf (3x)
        3 ( 0.00%)      g->players[0].status = PLAYER_STATUS_IDLE;
        3 ( 0.00%)      g->players[0].strategy_key = STRATEGY_KEY_HUMAN;   // the creator is a human seat
        .               game_mark_dirty(s);
       24 ( 0.00%)      char out[80]; snprintf(out, sizeof out, "{\"game_id\":\"%s\"}", s->id);
    1,323 ( 0.00%)  => ./stdio-common/./stdio-common/snprintf.c:snprintf (3x)
        6 ( 0.00%)      pthread_mutex_unlock(&s->lock);
       78 ( 0.00%)  => ./nptl/./nptl/pthread_mutex_unlock.c:pthread_mutex_unlock@@GLIBC_2.2.5 (3x)
        .               respond(conn, 200, out);
        .           }
        .           
        .           static void h_meta(Req *r, Conn *conn) {
       92 ( 0.00%)      char type[16] = {0}, gid[ID_LEN + 1] = {0};
      115 ( 0.00%)      json_str(r->body, "type", type, sizeof type);
   15,884 ( 0.02%)  => foolish_server.c:json_str (23x)
      115 ( 0.00%)      json_str(r->body, "game_id", gid, sizeof gid);
   17,871 ( 0.02%)  => foolish_server.c:json_str (23x)
        .           
       69 ( 0.00%)      pthread_mutex_lock(&g_registry_lock);
      828 ( 0.00%)  => ./nptl/./nptl/pthread_mutex_lock.c:pthread_mutex_lock@@GLIBC_2.2.5 (23x)
       23 ( 0.00%)      User *u = user_by_token(r->token);
        .               GameSlot *s = game_by_id(gid);
       46 ( 0.00%)      if (!u) { pthread_mutex_unlock(&g_registry_lock); respond(conn, 401, "{\"error\":\"auth\"}"); return; }
       46 ( 0.00%)      if (!s) { pthread_mutex_unlock(&g_registry_lock); respond(conn, 404, "{\"error\":\"no game\"}"); return; }
      184 ( 0.00%)      char user_id[ID_LEN + 1]; snprintf(user_id, sizeof user_id, "%s", u->user_id);
    8,464 ( 0.01%)  => ./stdio-common/./stdio-common/snprintf.c:snprintf (23x)
      161 ( 0.00%)      char username[24]; snprintf(username, sizeof username, "%s", u->username);
    8,418 ( 0.01%)  => ./stdio-common/./stdio-common/snprintf.c:snprintf (23x)
       92 ( 0.00%)      pthread_mutex_lock(&s->lock);
      828 ( 0.00%)  => ./nptl/./nptl/pthread_mutex_lock.c:pthread_mutex_lock@@GLIBC_2.2.5 (23x)
       69 ( 0.00%)      pthread_mutex_unlock(&g_registry_lock);
      598 ( 0.00%)  => ./nptl/./nptl/pthread_mutex_unlock.c:pthread_mutex_unlock@@GLIBC_2.2.5 (23x)
        .           
       23 ( 0.00%)      Game *g = &s->game;
      115 ( 0.00%)      if (!strcmp(type, "join")) {
       15 ( 0.00%)          if (seat_of(s, user_id) < 0 && g->num_players < MAX_PLAYERS && g->status == GAME_STATUS_WAITING) {
        6 ( 0.00%)              int i = g->num_players++;
       30 ( 0.00%)              snprintf(s->seat_user[i], ID_LEN + 1, "%s", user_id);
    1,104 ( 0.00%)  => ./stdio-common/./stdio-common/snprintf.c:snprintf (3x)
       30 ( 0.00%)              snprintf(s->seat_name[i], 24, "%s", username);
    1,098 ( 0.00%)  => ./stdio-common/./stdio-common/snprintf.c:snprintf (3x)
       33 ( 0.00%)              snprintf(g->players[i].name, 24, "%s", username);
    1,098 ( 0.00%)  => ./stdio-common/./stdio-common/snprintf.c:snprintf (3x)
       24 ( 0.00%)              snprintf(g->players[i].player_id, 24, "%s", user_id);
    1,104 ( 0.00%)  => ./stdio-common/./stdio-common/snprintf.c:snprintf (3x)
        3 ( 0.00%)              g->players[i].status = PLAYER_STATUS_IDLE;
        3 ( 0.00%)              g->players[i].strategy_key = STRATEGY_KEY_HUMAN;   // a human seat
        .                   }
       60 ( 0.00%)      } else if (!strcmp(type, "add-bot")) {
        .                   char skey[24] = {0}; if (!json_str(r->body, "strategy", skey, sizeof skey)) snprintf(skey, sizeof skey, "random");
        .                   if (g->num_players < MAX_PLAYERS && g->status == GAME_STATUS_WAITING) {
        .                       int i = g->num_players++;
        .                       s->seat_ready[i] = true;
        .                       int ridx = bot_roster_find(skey);
        .                       if (ridx < 0) ridx = bot_roster_find("random");
        .                       const BotRosterEntry *entry = bot_roster_at(ridx);
        .                       snprintf(s->seat_name[i], 24, "%%%s %d", skey, i);
-- line 1047 ----------------------------------------
-- line 1064 ----------------------------------------
        .                       // own `.strat` happens to be 9) or, for a few names, matching a
        .                       // DIFFERENT roster entry's `.strat` and silently running THAT
        .                       // bot's brain instead (e.g. "gunpowder"'s index (6) equals
        .                       // "blackpowder"'s STRAT_BLACKPOWDER (6) — a gunpowder seat would
        .                       // have silently played blackpowder). The roster entry's OWN
        .                       // `.strat` field is the actual kernel brain id; use it.
        .                       g->players[i].strategy_key = entry ? (int8_t)entry->strat : (int8_t)STRAT_RANDOM;
        .                   }
      100 ( 0.00%)      } else if (!strcmp(type, "start")) {
        .                   int me = seat_of(s, user_id);
       52 ( 0.00%)          if (me >= 0) { s->seat_ready[me] = true; g->players[me].status = PLAYER_STATUS_READY; }
        .                   // Deal once every seated human is ready (bots are always ready) and 2+ seated.
       26 ( 0.00%)          bool all = g->num_players >= 2;
      286 ( 0.00%)          for (int i = 0; i < g->num_players; i++) if (!seat_is_bot(g, i) && !s->seat_ready[i]) all = false;
       34 ( 0.00%)          if (all && g->status == GAME_STATUS_WAITING) {
    1,024 ( 0.00%)              unsigned char seed[32]; for (int i = 0; i < 32; i++) seed[i] = (unsigned char)(next_rand() ^ (i * 131 + (int)g_seq));
        .                       // No g_kernel_lock (Stage 5, see "Locking" above): the deal RNG
        .                       // state game_set_deal_seed_bytes/game_seat_and_deal touch is
        .                       // _Thread_local, and every other kernel static reachable from
        .                       // game_seat_and_deal's apply/refill path is too — s->lock
        .                       // (already held here) is the only serialization this needs.
        8 ( 0.00%)              game_set_deal_seed_bytes(seed, 32);
      156 ( 0.00%)  => /home/user/foolish/server/impls/native/../../../c/src/game.c:game_set_deal_seed_bytes (4x)
        .                       // Seats were wired in the lobby (strategy_key per seat), so pass NULL:
        .                       // the kernel owns marking them + the deal (+ g->status = PLAYING).
       16 ( 0.00%)              game_seat_and_deal(g, NULL, g->num_players);
   37,016 ( 0.04%)  => /home/user/foolish/server/impls/native/../../../c/src/game.c:game_seat_and_deal (4x)
        8 ( 0.00%)              start_bot_loop(s);             // the game-loop paces bot play from here
  420,776 ( 0.46%)  => foolish_server.c:start_bot_loop (4x)
        .                   }
       28 ( 0.00%)      } else if (!strcmp(type, "continue")) {
        .                   // Reset the kernel to the lobby for a rematch (identities kept, re-dealt
        .                   // on start). The host owns only this lobby transition; game-over was the
        .                   // kernel's to declare.
        7 ( 0.00%)          g->status = GAME_STATUS_WAITING;
       98 ( 0.00%)          for (int i = 0; i < g->num_players; i++) {
        .                       bool ai = seat_is_bot(g, i);
       14 ( 0.00%)              s->seat_ready[i] = ai;
       14 ( 0.00%)              g->players[i].status = ai ? PLAYER_STATUS_READY : PLAYER_STATUS_IDLE;
        .                   }
        .               }
        .               // Every branch above either mutates the roster/lobby state or is a no-op
        .               // (an already-seated join, a re-ready), and bumping on a no-op is
        .               // harmless (worst case: one extra state_put on the next /ws poll) — see
        .               // GameSlot.version's doc. Unconditional beats re-deriving "did this
        .               // branch actually change anything" per-branch for a rarely-called path.
       23 ( 0.00%)      s->version++;
        .               game_mark_dirty(s);
      230 ( 0.00%)      char out[80]; snprintf(out, sizeof out, "{\"game_id\":\"%s\",\"status\":%d}", s->id, g->status);
   17,163 ( 0.02%)  => ./stdio-common/./stdio-common/snprintf.c:snprintf (23x)
       46 ( 0.00%)      pthread_mutex_unlock(&s->lock);
      609 ( 0.00%)  => ./nptl/./nptl/pthread_mutex_unlock.c:pthread_mutex_unlock@@GLIBC_2.2.5 (23x)
        .               respond(conn, 200, out);
        .           }
        .           
        .           static void h_action(Req *r, Conn *conn) {
        .               // game_id rides the query string (like /state); the body IS the packed awire
        .               // frame, so it can be binary. /action?game_id=..
        .               char gid[ID_LEN + 1] = {0};
        .               const char *gp = strstr(r->query, "game_id=");
-- line 1118 ----------------------------------------
-- line 1206 ----------------------------------------
        .           // so a load tool (foolish_hammer's --mode=ws summary) or an external script
        .           // (bot_stress.sh) can poll it before/after a timed run and compute
        .           // decisions/sec from the delta. See g_bot_decisions/g_octogen_decisions'
        .           // doc above for exactly what each counts. Answered inline off the
        .           // dispatcher (see main()'s accept loop), same as /health — cheap atomic
        .           // loads, no lock, no reason to pay a work-queue round trip.
        .           static void h_stats(Req *r, Conn *conn) {
        .               (void)r;
        2 ( 0.00%)      unsigned long bd = atomic_load_explicit(&g_bot_decisions, memory_order_relaxed);
        2 ( 0.00%)      unsigned long od = atomic_load_explicit(&g_octogen_decisions, memory_order_relaxed);
        .               char out[128];
       14 ( 0.00%)      snprintf(out, sizeof out, "{\"bot_decisions\":%lu,\"octogen_decisions\":%lu}", bd, od);
    1,714 ( 0.00%)  => ./stdio-common/./stdio-common/snprintf.c:snprintf (2x)
        .               respond(conn, 200, out);
        .           }
        .           
        .           // Serialize a masked view, reusing the cached bytes from GameSlot when
        .           // nothing has changed since they were computed (PROFILE_HOTPATH.md "T1c" —
        .           // see GameSlot's `version`/`view_cache*` fields above for the invariant).
        .           // `cache_idx` selects WHICH cached slot to use/fill: 0 <= cache_idx <
        .           // num_players <= MAX_PLAYERS for a seated client's own cache, or
-- line 1225 ----------------------------------------
-- line 1230 ----------------------------------------
        .           // state_put viewer value (that's VIEW_SPECTATOR, -1). MUST be called with
        .           // s->lock held (same contract as a bare state_put call here) and
        .           // `cache_idx`/`viewer` MUST already be known valid — ws_conn_thread
        .           // validates both at handshake time before ever calling in (a seated
        .           // client's own seat, or the fixed spectator pair); this function does not
        .           // re-check, so it is not safe to point at an unvalidated/attacker-
        .           // controlled index.
        .           static int state_put_cached(GameSlot *s, int cache_idx, int viewer, unsigned char *out) {
  405,820 ( 0.44%)      if (s->view_cache_version[cache_idx] != s->version) {
        .                   // Serialize straight into a scratch buffer wider than
        .                   // VIEW_CACHE_CAP first: state_put's real worst case fits well
        .                   // inside VIEW_CACHE_CAP today (see that constant's comment), but if
        .                   // a future kernel change ever grew a cap enough to overflow it,
        .                   // this falls back to "always recompute, never cache" for that slot
        .                   // instead of truncating a state update — correctness over the
        .                   // optimization.
        .                   unsigned char scratch[1 + 65536];
    3,222 ( 0.00%)          int n = state_put(&s->game, viewer, scratch);
    3,174 ( 0.00%)  => /home/user/foolish/server/impls/native/../../../c/src/view.c:state_put (6x)
    1,638 ( 0.00%)          if (n < 0) n = 0;
    1,622 ( 0.00%)          if (n <= VIEW_CACHE_CAP) {
    6,472 ( 0.01%)              memcpy(s->view_cache[cache_idx], scratch, (size_t)n);
       72 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (6x)
      811 ( 0.00%)              s->view_cache_len[cache_idx] = n;
    1,628 ( 0.00%)              s->view_cache_version[cache_idx] = s->version;
        .                   } else {
        .                       memcpy(out, scratch, (size_t)n);
        .                       return n;
        .                   }
        .               }
  100,648 ( 0.11%)      int n = s->view_cache_len[cache_idx];
  405,852 ( 0.44%)      memcpy(out, s->view_cache[cache_idx], (size_t)n);
        .               return n;
        .           }
        .           
        .           // --------------------------------------------------------------------------
        .           // /ws — the WebSocket hot loop (item 1/2 of PROFILE_HOTPATH.md's "where to
        .           // speed up" list). One handshake, then ONE persistent connection carries
        .           // every move + state push for that seat's whole session — no pthread_create
        .           // per action.
-- line 1267 ----------------------------------------
-- line 1332 ----------------------------------------
        .           // Validates a /ws (or /ws?spectator=1) upgrade against the registry + this
        .           // game's roster — same checks this function's callers always made. Returns
        .           // NULL on any auth/seat failure (caller responds 401 and closes); on success
        .           // returns the owning GameSlot* with *out_seat/*out_spectator/*out_cache_idx/
        .           // *out_viewer resolved for the rest of the connection's life (see
        .           // state_put_cached's doc for why cache_idx and viewer are two different
        .           // values for a spectator).
        .           static GameSlot *ws_handshake_validate(Req *r, int *out_seat, bool *out_spectator,
       78 ( 0.00%)                                          int *out_cache_idx, int *out_viewer) {
       12 ( 0.00%)      char gid[ID_LEN + 1] = {0}; int seat = -1;
       30 ( 0.00%)      const char *gp = strstr(r->query, "game_id=");
      516 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/strstr-sse2-unaligned.S:__strstr_sse2_unaligned (6x)
      630 ( 0.00%)      if (gp) { gp += 8; int i = 0; while (gp[i] && gp[i] != '&' && i < ID_LEN) { gid[i] = gp[i]; i++; } gid[i] = 0; }
       24 ( 0.00%)      const char *sp = strstr(r->query, "seat=");
      408 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/strstr-sse2-unaligned.S:__strstr_sse2_unaligned (6x)
       48 ( 0.00%)      if (sp) seat = (int)strtol(sp + 5, NULL, 10);
      648 ( 0.00%)  => ./stdlib/../stdlib/strtol.c:__isoc23_strtol (6x)
        .               bool spectator = false;
       24 ( 0.00%)      const char *spq = strstr(r->query, "spectator=");
      282 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/strstr-sse2-unaligned.S:__strstr_sse2_unaligned (6x)
       12 ( 0.00%)      if (spq) { spq += 10; spectator = (*spq == '1'); }
        .           
       18 ( 0.00%)      pthread_mutex_lock(&g_registry_lock);
      216 ( 0.00%)  => ./nptl/./nptl/pthread_mutex_lock.c:pthread_mutex_lock@@GLIBC_2.2.5 (6x)
        6 ( 0.00%)      User *u = user_by_token(r->token);
        .               GameSlot *s = game_by_id(gid);
       12 ( 0.00%)      if (!u || !s) { pthread_mutex_unlock(&g_registry_lock); return NULL; }
       24 ( 0.00%)      pthread_mutex_lock(&s->lock);
      216 ( 0.00%)  => ./nptl/./nptl/pthread_mutex_lock.c:pthread_mutex_lock@@GLIBC_2.2.5 (6x)
       18 ( 0.00%)      pthread_mutex_unlock(&g_registry_lock);
      156 ( 0.00%)  => ./nptl/./nptl/pthread_mutex_unlock.c:pthread_mutex_unlock@@GLIBC_2.2.5 (6x)
        .               // Spectators: a real Bearer token is still required (same user_by_token
        .               // check every /ws client passes — this is "no SEAT membership", not "no
        .               // auth"), but ownership of a specific seat is not — any authenticated
        .               // user may watch any EXISTING game. Seated clients are exactly as
        .               // before: must own the seat they asked for.
        .               bool ok;
       12 ( 0.00%)      if (spectator) {
        .                   ok = true;
        .               } else {
       48 ( 0.00%)          char user_id[ID_LEN + 1]; snprintf(user_id, sizeof user_id, "%s", u->user_id);
    2,208 ( 0.00%)  => ./stdio-common/./stdio-common/snprintf.c:snprintf (6x)
       36 ( 0.00%)          ok = seat >= 0 && seat < s->game.num_players && seat_of(s, user_id) == seat;
        .               }
       18 ( 0.00%)      pthread_mutex_unlock(&s->lock);
      156 ( 0.00%)  => ./nptl/./nptl/pthread_mutex_unlock.c:pthread_mutex_unlock@@GLIBC_2.2.5 (6x)
        6 ( 0.00%)      if (!ok) return NULL;
        .           
       24 ( 0.00%)      *out_seat = seat; *out_spectator = spectator;
       12 ( 0.00%)      *out_cache_idx = spectator ? SPECTATOR_CACHE_IDX : seat;
       12 ( 0.00%)      *out_viewer    = spectator ? VIEW_SPECTATOR : seat;
        .               return s;
       54 ( 0.00%)  }
        .           
        .           // Encodes the 101 Switching Protocols response, THEN the immediate
        .           // post-handshake state push (current masked view, ok=0 — "here's where
        .           // things stand", not a move confirmation) into `conn` (a real fd, blocking —
        .           // ws_conn_thread; or a Stage 6 buffered Conn — the epoll dispatcher, see
        .           // conn.h). `accept` is the already-computed Sec-WebSocket-Accept value
        .           // (callers check ws_accept_from_key's own failure mode separately, since the
        .           // two paths respond to a bad key differently — see below). On success fills
        .           // *out_wc (mask_outgoing=0: server frames are never masked) and returns
        .           // true; false means "close the connection, nothing more to send."
        .           static bool ws_send_handshake_and_push(WsConn *out_wc, Conn conn, const char *accept,
       78 ( 0.00%)                                          GameSlot *s, int cache_idx, int viewer) {
        .               char resp[256];
       30 ( 0.00%)      int n = snprintf(resp, sizeof resp,
    2,808 ( 0.00%)  => ./stdio-common/./stdio-common/snprintf.c:snprintf (6x)
        .                   "HTTP/1.1 101 Switching Protocols\r\nUpgrade: websocket\r\nConnection: Upgrade\r\n"
        .                   "Sec-WebSocket-Accept: %s\r\n\r\n", accept);
       42 ( 0.00%)      if (n <= 0 || conn_write(&conn, resp, (size_t)n) != n) return false;
      378 ( 0.00%)  => /home/user/foolish/server/impls/native/conn.c:conn_write (6x)
        .           
       48 ( 0.00%)      ws_conn_init(out_wc, conn, /*mask_outgoing=*/0);
       60 ( 0.00%)  => /home/user/foolish/server/impls/native/ws.c:ws_conn_init (6x)
        .               // [ok:u8][state_put bytes] — sized for state_put's documented worst case
        .               // (h_state uses the same 65536 cap).
        .               unsigned char msg[1 + 65536];
       24 ( 0.00%)      pthread_mutex_lock(&s->lock);
      216 ( 0.00%)  => ./nptl/./nptl/pthread_mutex_lock.c:pthread_mutex_lock@@GLIBC_2.2.5 (6x)
       18 ( 0.00%)      int slen = s->used ? state_put_cached(s, cache_idx, viewer, msg + 1) : 0;
       18 ( 0.00%)      pthread_mutex_unlock(&s->lock);
      156 ( 0.00%)  => ./nptl/./nptl/pthread_mutex_unlock.c:pthread_mutex_unlock@@GLIBC_2.2.5 (6x)
       12 ( 0.00%)      msg[0] = 0;
       48 ( 0.00%)      return ws_send_frame(out_wc, WS_OP_BIN, msg, slen + 1) >= 0;
      978 ( 0.00%)  => /home/user/foolish/server/impls/native/ws.c:ws_send_frame (6x)
       54 ( 0.00%)  }
        .           
        .           // Applies one received WS application message (a real awire move, or an
        .           // empty/rejected/spectator-sent poll) and produces the reply payload — the
        .           // exact per-message body ws_conn_thread's loop always ran. `in`/`mlen` is
        .           // the just-received message (mlen==0 is a plain poll); `msg` is
        .           // caller-owned, >= 1+65536 bytes. Returns the total reply length (>= 1),
        .           // [ok:u8][state_put_cached bytes]. MUST be called with NO lock held — takes
        .           // and releases s->lock itself, exactly once. `out_version` (may be NULL) is
        .           // set to `s->version` as observed under that SAME lock acquisition, right
        .           // before it releases — Stage 6's epoll worker uses this to record exactly
        .           // which version this connection's peer was just brought up to date with,
        .           // without a second, unlocked (racy) read of s->version afterward.
        .           static int ws_service_message(GameSlot *s, int seat, bool spectator, int cache_idx, int viewer,
1,420,118 ( 1.56%)                                 const unsigned char *in, int mlen, unsigned char *msg, uint32_t *out_version) {
        .               bool applied = false;
        .               int slen;
  405,748 ( 0.44%)      pthread_mutex_lock(&s->lock);
3,651,732 ( 4.00%)  => ./nptl/./nptl/pthread_mutex_lock.c:pthread_mutex_lock@@GLIBC_2.2.5 (101,437x)
  202,874 ( 0.22%)      if (s->used) {
        .                   // Spectators MAY NOT submit moves (Stage 4): `!spectator` keeps ANY
        .                   // frame a spectator sends — empty or a well-formed move alike —
        .                   // from ever reaching awire_decode/awire_apply. It is silently
        .                   // treated as "send me the current state", same as a seated client's
        .                   // empty poll frame; `applied` stays false and the reply's ok byte is
        .                   // always 0.
  104,773 ( 0.11%)          if (!spectator && mlen > 0 && s->game.status == GAME_STATUS_PLAYING) {
        .                       AwireAction a;
    3,324 ( 0.00%)              if (awire_decode(in, mlen, &a)) {
   44,779 ( 0.05%)  => /home/user/foolish/server/impls/native/../../../c/src/awire.c:awire_decode (831x)
        .                           // No g_kernel_lock (Stage 5, see h_action's identical
        .                           // pattern and the "Locking" doc above) — s->lock, held for
        .                           // this whole call, is enough.
    2,493 ( 0.00%)                  bool applied_now = awire_apply(&s->game, seat, &a);
  220,383 ( 0.24%)  => /home/user/foolish/server/impls/native/../../../c/src/game.c:awire_apply (831x)
    1,662 ( 0.00%)                  if (applied_now) {
        .                               applied = true;
      495 ( 0.00%)                      s->version++;   // this seat's move can change every seat's view
        .                               game_mark_dirty(s);
    1,485 ( 0.00%)                      pthread_cond_signal(&s->cond);   // same wakeup /action gives the bot game-loop
   61,173 ( 0.07%)  => ./nptl/./nptl/pthread_cond_signal.c:pthread_cond_signal@@GLIBC_2.3.2 (495x)
        .                           }
        .                       }
        .                   }
        .                   // PROFILE_HOTPATH.md "T1c": on a pure poll (mlen==0 or an
        .                   // illegal/rejected move, or ANY frame from a spectator) this view
        .                   // did NOT change, so state_put_cached memcpy's the bytes computed
        .                   // last time instead of re-running the kernel's full masked
        .                   // serialization.
  202,874 ( 0.22%)          slen = state_put_cached(s, cache_idx, viewer, msg + 1);
        .               } else {
        .                   slen = 0;
        .               }
  405,748 ( 0.44%)      if (out_version) *out_version = s->version;
  304,311 ( 0.33%)      pthread_mutex_unlock(&s->lock);
2,637,846 ( 2.89%)  => ./nptl/./nptl/pthread_mutex_unlock.c:pthread_mutex_unlock@@GLIBC_2.2.5 (101,437x)
  202,874 ( 0.22%)      msg[0] = applied ? 1 : 0;
1,014,370 ( 1.11%)      return slen + 1;
        .           }
        .           
        .           static void *ws_conn_thread(void *argp) {
        .               WsSpawnArg *sa = argp;
        .               Conn *conn = &sa->conn;
        .               Req *r = &sa->req;
        .           
        .               int seat; bool spectator; int cache_idx, viewer;
-- line 1463 ----------------------------------------
-- line 1548 ----------------------------------------
        .               WorkItem *buf;    // ring buffer, WQ_CAP entries
        .               int head, tail, count;
        .               pthread_mutex_t mtx;
        .               pthread_cond_t  not_empty;
        .               pthread_cond_t  not_full;
        .           } WorkQueue;
        .           
        .           static void wq_init(WorkQueue *q) {
       30 ( 0.00%)      q->buf = calloc(WQ_CAP, sizeof(WorkItem));
    1,958 ( 0.00%)  => ./malloc/./malloc/malloc.c:calloc (6x)
       12 ( 0.00%)      q->head = q->tail = q->count = 0;
       28 ( 0.00%)      pthread_mutex_init(&q->mtx, NULL);
      963 ( 0.00%)  => ./elf/../sysdeps/x86_64/dl-trampoline.h:_dl_runtime_resolve_xsave (1x)
      270 ( 0.00%)  => ./nptl/./nptl/pthread_mutex_init.c:pthread_mutex_init@@GLIBC_2.2.5 (6x)
       28 ( 0.00%)      pthread_cond_init(&q->not_empty, NULL);
    1,084 ( 0.00%)  => ./elf/../sysdeps/x86_64/dl-trampoline.h:_dl_runtime_resolve_xsave (1x)
       60 ( 0.00%)  => ./nptl/./nptl/pthread_cond_init.c:pthread_cond_init@@GLIBC_2.3.2 (6x)
       24 ( 0.00%)      pthread_cond_init(&q->not_full, NULL);
       60 ( 0.00%)  => ./nptl/./nptl/pthread_cond_init.c:pthread_cond_init@@GLIBC_2.3.2 (6x)
        .           }
        .           
        .           // Blocking push (backpressure): if a pool's queue is full, the dispatcher
        .           // waits rather than dropping the connection or growing unboundedly. Fine
        .           // for this POC's bounded load; a production version would size WQ_CAP for
        .           // the target burst or shed load with a 503 instead of blocking the accept
        .           // loop.
        .           static void wq_push(WorkQueue *q, const WorkItem *item) {
       27 ( 0.00%)      pthread_mutex_lock(&q->mtx);
      324 ( 0.00%)  => ./nptl/./nptl/pthread_mutex_lock.c:pthread_mutex_lock@@GLIBC_2.2.5 (9x)
       27 ( 0.00%)      while (q->count == WQ_CAP) pthread_cond_wait(&q->not_full, &q->mtx);
       81 ( 0.00%)      q->buf[q->tail] = *item;
      907 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (9x)
       90 ( 0.00%)      q->tail = (q->tail + 1) % WQ_CAP;
        9 ( 0.00%)      q->count++;
       31 ( 0.00%)      pthread_cond_signal(&q->not_empty);
      919 ( 0.00%)  => ./elf/../sysdeps/x86_64/dl-trampoline.h:_dl_runtime_resolve_xsave (1x)
    1,125 ( 0.00%)  => ./nptl/./nptl/pthread_cond_signal.c:pthread_cond_signal@@GLIBC_2.3.2 (9x)
       31 ( 0.00%)      pthread_mutex_unlock(&q->mtx);
      970 ( 0.00%)  => ./elf/../sysdeps/x86_64/dl-trampoline.h:_dl_runtime_resolve_xsave (1x)
      234 ( 0.00%)  => ./nptl/./nptl/pthread_mutex_unlock.c:pthread_mutex_unlock@@GLIBC_2.2.5 (9x)
        .           }
        .           
        .           static void wq_pop(WorkQueue *q, WorkItem *out) {
       44 ( 0.00%)      pthread_mutex_lock(&q->mtx);
      961 ( 0.00%)  => ./elf/../sysdeps/x86_64/dl-trampoline.h:_dl_runtime_resolve_xsave (1x)
      360 ( 0.00%)  => ./nptl/./nptl/pthread_mutex_lock.c:pthread_mutex_lock@@GLIBC_2.2.5 (10x)
      101 ( 0.00%)      while (q->count == 0) pthread_cond_wait(&q->not_empty, &q->mtx);
    2,660 ( 0.00%)  => ./nptl/./nptl/pthread_cond_wait.c:pthread_cond_wait@@GLIBC_2.3.2 (10x)
      926 ( 0.00%)  => ./elf/../sysdeps/x86_64/dl-trampoline.h:_dl_runtime_resolve_xsave (1x)
       63 ( 0.00%)      *out = q->buf[q->head];
      909 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (9x)
       81 ( 0.00%)      q->head = (q->head + 1) % WQ_CAP;
       27 ( 0.00%)      q->count--;
       18 ( 0.00%)      pthread_cond_signal(&q->not_full);
      207 ( 0.00%)  => ./nptl/./nptl/pthread_cond_signal.c:pthread_cond_signal@@GLIBC_2.3.2 (9x)
       27 ( 0.00%)      pthread_mutex_unlock(&q->mtx);
      333 ( 0.00%)  => ./nptl/./nptl/pthread_mutex_unlock.c:pthread_mutex_unlock@@GLIBC_2.2.5 (9x)
        .           }
        .           
        .           static WorkQueue g_auth_create_q;
        .           static WorkQueue g_meta_q;
        .           static WorkQueue g_game_q[MAX_GAME_WORKERS];
        .           
      320 ( 0.00%)  static void route(Req *r, Conn *conn) {
      160 ( 0.00%)      if (!strcmp(r->method, "OPTIONS")) { respond(conn, 200, "{}"); return; }
      739 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/strcmp-avx2.S:__strcmp_avx2 (32x)
      224 ( 0.00%)      if (!strcmp(r->path, "/health")) { respond(conn, 200, "{\"ok\":true}"); return; }
      739 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/strcmp-avx2.S:__strcmp_avx2 (32x)
      348 ( 0.00%)      if (!strcmp(r->path, "/auth/signup") || !strcmp(r->path, "/auth/signin")) { h_signup(r, conn); return; }
    1,292 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/strcmp-avx2.S:__strcmp_avx2 (58x)
      156 ( 0.00%)      if (!strcmp(r->path, "/create")) { h_create(r, conn); return; }
      553 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/strcmp-avx2.S:__strcmp_avx2 (26x)
      138 ( 0.00%)      if (!strcmp(r->path, "/meta"))   { h_meta(r, conn); return; }
      713 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/strcmp-avx2.S:__strcmp_avx2 (23x)
        .               if (!strcmp(r->path, "/action")) { h_action(r, conn); return; }
        .               if (!strcmp(r->path, "/state"))  { h_state(r, conn); return; }
        .               if (!strcmp(r->path, "/status")) { h_status(r, conn); return; }
        .               if (!strcmp(r->path, "/stats"))  { h_stats(r, conn); return; }
        .               respond(conn, 404, "{\"error\":\"route\"}");
      256 ( 0.00%)  }
        .           
        .           // Picks which pool's queue a one-shot request belongs on. Called by the
        .           // dispatcher only for requests it hasn't already answered inline (OPTIONS,
        .           // /health) or handed to a dedicated thread (/ws) — see main()'s accept loop.
        .           static WorkQueue *classify_queue(Req *r) {
        .               if (!strcmp(r->path, "/auth/signup") || !strcmp(r->path, "/auth/signin") || !strcmp(r->path, "/create"))
        .                   return &g_auth_create_q;
        .               bool is_meta = !strcmp(r->path, "/meta");
-- line 1611 ----------------------------------------
-- line 1617 ----------------------------------------
        .                   if (gp) { gp += 8; int i = 0; while (gp[i] && gp[i] != '&' && i < ID_LEN) { gid[i] = gp[i]; i++; } gid[i] = 0; }
        .                   unsigned long h = gid[0] ? hash_str(gid) : 0;
        .                   return &g_game_q[h % (unsigned long)g_n_game_workers];
        .               }
        .               // unrecognized route -> route() 404s it; any pool can carry it.
        .               return g_n_meta_workers > 0 ? &g_meta_q : &g_game_q[0];
        .           }
        .           
       14 ( 0.00%)  static void *worker_thread(void *arg) {
        .               WorkQueue *q = arg;
        .               for (;;) {
        .                   WorkItem item;
        .                   wq_pop(q, &item);
       36 ( 0.00%)          route(&item.req, &item.conn);
  207,631 ( 0.23%)  => foolish_server.c:route (9x)
       18 ( 0.00%)          conn_close(&item.conn);
      639 ( 0.00%)  => /home/user/foolish/server/impls/native/conn.c:conn_close (9x)
       27 ( 0.00%)          free(item.raw_buf);
    3,030 ( 0.00%)  => ./malloc/./malloc/malloc.c:free (9x)
        .               }
        .               return NULL;
        .           }
        .           
        .           // ==========================================================================
        .           // STAGE 6 — epoll-per-shard connection I/O (SERVER_SCALING.md "Stage 6").
        .           //
        .           // T2a (SERVER_SCALING.md "Deliverable 2 — WS design: (B), not (A)") deferred
-- line 1640 ----------------------------------------
-- line 1788 ----------------------------------------
        .           // --tls server (which never populates g_workers) never touches it.
        .           static bool g_epoll_active = false;
        .           
        .           // Same shard hash classify_queue's game-worker routing has always used
        .           // (hash_str(game_id) % g_n_game_workers) — reused here so a game's HTTP
        .           // requests, its /ws connections, AND its bot_thread's push notifications all
        .           // agree on exactly one owning worker.
        .           static int game_worker_index(const char *game_id) {
       18 ( 0.00%)      unsigned long h = (game_id && game_id[0]) ? hash_str(game_id) : 0;
      174 ( 0.00%)      return (int)(h % (unsigned long)g_n_game_workers);
        .           }
        .           
        .           // The one cross-thread seam (SERVER_SCALING.md "Stage 6"): bot_thread (its
        .           // own per-game trampoline thread) just mutated `s` — a bot's move landed, or
        .           // the game ended — and bumped s->version under s->lock, same as every other
        .           // mutation site in this file. Under the OLD thread-per-connection /ws
        .           // design that was enough by itself: every live connection was blocked in
        .           // its OWN thread's ws_recv_message, so the NEXT client poll would simply
-- line 1805 ----------------------------------------
-- line 1854 ----------------------------------------
        .           //                   again.
        .           //   WSF_NEED_MORE — no complete message yet; *consumed == n (every input
        .           //                   byte was used). Caller should wait for more EPOLLIN.
        .           //   WSF_ERROR     — protocol violation, an oversized message (> WS_IN_CAP),
        .           //                   or a CLOSE frame. Caller must tear the connection down;
        .           //                   *consumed is NOT reliably set on every WSF_ERROR return
        .           //                   path (every caller ignores it in this case — the
        .           //                   connection is going away regardless).
2,536,825 ( 2.78%)  static int wsasync_feed(EConn *ec, const unsigned char *in, int n, int *consumed) {
        .               int off = 0;
        .               for (;;) {
        .                   switch (ec->phase) {
        .                   case WSP_HDR2: {
  710,059 ( 0.78%)              int take = 2 - ec->hdr_have; if (take > n - off) take = n - off;
1,217,244 ( 1.33%)              if (take > 0) { memcpy(ec->hdrbuf + ec->hdr_have, in + off, (size_t)take); ec->hdr_have += take; off += take; }
1,825,866 ( 2.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (101,437x)
  202,874 ( 0.22%)              if (ec->hdr_have < 2) { *consumed = off; return WSF_NEED_MORE; }
  405,748 ( 0.44%)              ec->fin    = (ec->hdrbuf[0] >> 7) & 1;
  507,185 ( 0.56%)              ec->op     = ec->hdrbuf[0] & 0x0F;
  101,437 ( 0.11%)              ec->masked = (ec->hdrbuf[1] >> 7) & 1;
  202,874 ( 0.22%)              int64_t len7 = ec->hdrbuf[1] & 0x7F;
  101,437 ( 0.11%)              ec->hdr_have = 0;
  101,437 ( 0.11%)              ec->frame_validated = false;   // a brand-new frame starts here — see WSP_PAYLOAD's doc
  405,748 ( 0.44%)              if (len7 == 126)      { ec->hdr_need = 2; ec->phase = WSP_EXTLEN; }
        .                       else if (len7 == 127) { ec->hdr_need = 8; ec->phase = WSP_EXTLEN; }
  710,059 ( 0.78%)              else { ec->frame_len = len7; ec->phase = ec->masked ? WSP_MASK : WSP_PAYLOAD; }
        .                       continue;
        .                   }
        .                   case WSP_EXTLEN: {
        .                       int take = ec->hdr_need - ec->hdr_have; if (take > n - off) take = n - off;
        .                       if (take > 0) { memcpy(ec->hdrbuf + ec->hdr_have, in + off, (size_t)take); ec->hdr_have += take; off += take; }
        .                       if (ec->hdr_have < ec->hdr_need) { *consumed = off; return WSF_NEED_MORE; }
        .                       int64_t len = 0;
        .                       for (int i = 0; i < ec->hdr_need; i++) len = (len << 8) | ec->hdrbuf[i];
        .                       if (len < 0) return WSF_ERROR;   // top bit set on the 8-byte form is a protocol violation per spec
        .                       ec->frame_len = len;
        .                       ec->hdr_have = 0;
        .                       ec->phase = ec->masked ? WSP_MASK : WSP_PAYLOAD;
        .                       continue;
        .                   }
        .                   case WSP_MASK: {
  710,059 ( 0.78%)              int take = 4 - ec->hdr_have; if (take > n - off) take = n - off;
1,217,244 ( 1.33%)              if (take > 0) { memcpy(ec->mkey + ec->hdr_have, in + off, (size_t)take); ec->hdr_have += take; off += take; }
1,521,555 ( 1.67%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (101,437x)
  202,874 ( 0.22%)              if (ec->hdr_have < 4) { *consumed = off; return WSF_NEED_MORE; }
  101,437 ( 0.11%)              ec->hdr_have = 0;
  202,874 ( 0.22%)              ec->phase = WSP_PAYLOAD;
        .                       continue;
        .                   }
        .                   case WSP_PAYLOAD: {
  405,856 ( 0.44%)              bool ctrl = (ec->op == WS_OP_PING || ec->op == WS_OP_PONG || ec->op == WS_OP_CLOSE);
        .                       // Frame-header validation (new-vs-continuation bookkeeping, the
        .                       // oversized check) MUST run EXACTLY ONCE per frame. wsasync_feed
        .                       // can be called many times while a single frame's payload
        .                       // trickles in across several non-blocking reads (WSF_NEED_MORE
        .                       // below, resumed on the next EPOLLIN) — including calls that
        .                       // land here with ZERO payload bytes yet available (the header
        .                       // and mask arrived, but the payload hasn't started at all) — so
        .                       // neither "first call" nor "payload_got == 0" reliably means
-- line 1910 ----------------------------------------
-- line 1912 ----------------------------------------
        .                       // that fully arrives with 0 payload bytes in the SAME read, then
        .                       // the payload trickles in on a LATER read, hits this case again
        .                       // with payload_got still 0 — see SERVER_SCALING.md "Stage 6").
        .                       // `ec->frame_validated` is the actual per-FRAME signal: reset to
        .                       // false exactly once, when WSP_HDR2 starts parsing this frame's
        .                       // 2-byte header, and set true here the first time validation
        .                       // actually runs for it — correct regardless of how the
        .                       // header/mask/payload bytes happen to split across reads.
  202,946 ( 0.22%)              if (!ec->frame_validated) {
  101,437 ( 0.11%)                  ec->frame_validated = true;
  101,437 ( 0.11%)                  if (ctrl && ec->frame_len > 125) return WSF_ERROR;   // control frames are never fragmented/oversized (RFC 6455 5.5)
        .                           if (!ctrl) {
  202,874 ( 0.22%)                      if (ec->op != WS_OP_CONT && ec->op != WS_OP_TEXT && ec->op != WS_OP_BIN) return WSF_ERROR;
  202,874 ( 0.22%)                      if (ec->op != WS_OP_CONT) {
  101,437 ( 0.11%)                          if (ec->msg_opcode != -1) return WSF_ERROR;   // a new message started before the last one finished
  202,874 ( 0.22%)                          ec->msg_opcode = ec->op;
        .                               } else if (ec->msg_opcode == -1) {
        .                                   return WSF_ERROR;   // continuation with nothing to continue
        .                               }
  507,185 ( 0.56%)                      if (ec->msg_total + ec->frame_len > (int64_t)WS_IN_CAP) return WSF_ERROR;   // oversized for this buffer
        .                           }
        .                       }
  101,581 ( 0.11%)              unsigned char *dst = ctrl ? ec->ctrl_buf : ec->msg_buf + ec->msg_total;
  710,311 ( 0.78%)              int take = (int)(ec->frame_len - ec->payload_got); if (take > n - off) take = n - off;
  212,954 ( 0.23%)              if (take > 0) { memcpy(dst + ec->payload_got, in + off, (size_t)take); ec->payload_got += take; off += take; }
   14,304 ( 0.02%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (834x)
  205,448 ( 0.23%)              if (ec->payload_got < ec->frame_len) { *consumed = off; return WSF_NEED_MORE; }
        .           
  528,099 ( 0.58%)              if (ec->masked) for (int64_t i = 0; i < ec->frame_len; i++) dst[i] ^= ec->mkey[i & 3];
  101,437 ( 0.11%)              int op = ec->op; int64_t flen = ec->frame_len;
  304,311 ( 0.33%)              ec->payload_got = 0; ec->hdr_have = 0; ec->phase = WSP_HDR2;   // ready for the next frame either way
        .           
  101,437 ( 0.11%)              if (ctrl) {
        .                           if (op == WS_OP_PING) {
        .                               if (econn_reserve_out(ec, (int)flen + 14)) {
        .                                   ws_send_frame(&ec->wc_out, WS_OP_PONG, ec->ctrl_buf, flen);
        .                                   econn_commit_out(ec);
        .                               }
        .                               continue;   // more frames may remain in this same chunk
        .                           }
        .                           if (op == WS_OP_PONG) continue;
-- line 1951 ----------------------------------------
-- line 1953 ----------------------------------------
        .                           if (econn_reserve_out(ec, 16)) {
        .                               unsigned char payload[2] = { 0x03, 0xE8 };   // 1000, network byte order
        .                               ws_send_frame(&ec->wc_out, WS_OP_CLOSE, payload, 2);
        .                               econn_commit_out(ec);
        .                           }
        .                           *consumed = off;
        .                           return WSF_ERROR;
        .                       }
  101,437 ( 0.11%)              ec->msg_total += (int)flen;
  304,311 ( 0.33%)              if (ec->fin) {
  101,437 ( 0.11%)                  *consumed = off;
  304,311 ( 0.33%)                  ec->last_msg_len = ec->msg_total;
  304,311 ( 0.33%)                  ec->msg_total = 0; ec->msg_opcode = -1;
        .                           return WSF_MESSAGE;
        .                       }
        .                       continue;   // more fragments to come
        .                   }
        .                   }
        .               }
  811,784 ( 0.89%)  }
        .           
        .           // --------------------------------------------------------------------------
        .           // EConn output buffering + epoll bookkeeping — all single-threaded per
        .           // worker (see "Threading" above), so none of this needs a lock.
        .           // --------------------------------------------------------------------------
        .           
        .           // Prepares to append `need` more bytes to ec->wbuf: compacts away any
        .           // already-flushed prefix first (slides the unflushed [woff,wlen) tail down
-- line 1980 ----------------------------------------
-- line 1983 ----------------------------------------
        .           // ec->wc_out with a buffered Conn (conn.h) over the (now-compacted) tail —
        .           // callers append through `ec->wc_out` (ws_send_frame et al.), then MUST call
        .           // econn_commit_out to publish the new length. Returns false (appends
        .           // nothing) iff there truly isn't `need` bytes of room even after compacting
        .           // — a real backpressure case (this connection's peer has stopped reading
        .           // entirely) — callers drop that specific send rather than risk writing a
        .           // partial/corrupt frame into the buffer.
        .           static bool econn_reserve_out(EConn *ec, int need) {
  304,359 ( 0.33%)      if (ec->woff > 0) {
        .                   int rem = ec->wlen - ec->woff;
        .                   if (rem > 0) memmove(ec->wbuf, ec->wbuf + ec->woff, (size_t)rem);
        .                   ec->wlen = rem; ec->woff = 0;
        .               }
  405,812 ( 0.44%)      if (ec->wlen + need > ec->wbuf_cap) return false;
  101,453 ( 0.11%)      Conn buffered; conn_init_buffered(&buffered, ec->wbuf, ec->wbuf_cap);
  101,453 ( 0.11%)      buffered.buf_len = ec->wlen;   // resume appending after whatever's already queued
  811,624 ( 0.89%)      ws_conn_init(&ec->wc_out, buffered, /*mask_outgoing=*/0);   // server frames are never masked
1,014,530 ( 1.11%)  => /home/user/foolish/server/impls/native/ws.c:ws_conn_init (101,453x)
        .               return true;
        .           }
  202,906 ( 0.22%)  static void econn_commit_out(EConn *ec) { ec->wlen = ec->wc_out.conn.buf_len; }
        .           
        .           static void econn_set_epollout(Worker *w, EConn *ec, bool want) {
  202,964 ( 0.22%)      if (want == ec->want_epollout) return;
       29 ( 0.00%)      ec->want_epollout = want;
       87 ( 0.00%)      uint32_t events = (uint32_t)((ec->kind == ECONN_WS ? EPOLLIN : 0) | (want ? EPOLLOUT : 0));
       58 ( 0.00%)      struct epoll_event ev = { .events = events, .data.ptr = ec };
      174 ( 0.00%)      epoll_ctl(w->epfd, EPOLL_CTL_MOD, ec->fd, &ev);
      203 ( 0.00%)  => ./misc/../sysdeps/unix/syscall-template.S:epoll_ctl (29x)
        .           }
        .           
        .           static void econn_close(Worker *w, EConn *ec) {
      138 ( 0.00%)      epoll_ctl(w->epfd, EPOLL_CTL_DEL, ec->fd, NULL);
      161 ( 0.00%)  => ./misc/../sysdeps/unix/syscall-template.S:epoll_ctl (23x)
       69 ( 0.00%)      close(ec->fd);
    1,219 ( 0.00%)  => ./io/../sysdeps/unix/sysv/linux/close.c:close (23x)
       46 ( 0.00%)      if (ec->kind == ECONN_WS) {
        .                   if (ec->prev) ec->prev->next = ec->next; else if (w->ws_head == ec) w->ws_head = ec->next;
        .                   if (ec->next) ec->next->prev = ec->prev;
        .               }
       69 ( 0.00%)      free(ec->wbuf);
    6,429 ( 0.01%)  => ./malloc/./malloc/malloc.c:free (23x)
       69 ( 0.00%)      free(ec);
    6,048 ( 0.01%)  => ./malloc/./malloc/malloc.c:free (23x)
        .           }
        .           
        .           // Non-blocking flush of ec->wbuf[woff..wlen). Returns false iff `ec` was
        .           // closed (a real I/O error, or — ECONN_ONE_SHOT with close_after_flush — a
        .           // completed flush): the caller must not touch `ec` again after a false
        .           // return. On EAGAIN, arms EPOLLOUT and returns true (still open, not yet
        .           // fully drained) — the next EPOLLOUT event resumes the flush.
  811,856 ( 0.89%)  static bool econn_try_flush(Worker *w, EConn *ec) {
  913,338 ( 1.00%)      while (ec->woff < ec->wlen) {
  811,856 ( 0.89%)          ssize_t wr = write(ec->fd, ec->wbuf + ec->woff, (size_t)(ec->wlen - ec->woff));
5,784,474 ( 6.34%)  => ./io/../sysdeps/unix/sysv/linux/write.c:write (101,482x)
  202,964 ( 0.22%)          if (wr < 0) {
        .                       if (errno == EAGAIN || errno == EWOULDBLOCK) { econn_set_epollout(w, ec, true); return true; }
        .                       if (errno == EINTR) continue;
        .                       econn_close(w, ec); return false;
        .                   }
  101,482 ( 0.11%)          if (wr == 0) { econn_close(w, ec); return false; }
  202,964 ( 0.22%)          ec->woff += (int)wr;
        .               }
  101,482 ( 0.11%)      ec->woff = ec->wlen = 0;
        .               econn_set_epollout(w, ec, false);
  203,010 ( 0.22%)      if (ec->kind == ECONN_ONE_SHOT && ec->close_after_flush) { econn_close(w, ec); return false; }
        .               return true;
  710,374 ( 0.78%)  }
        .           
        .           // Pushes fresh state to every active /ws connection THIS worker owns whose
        .           // cached view is stale (ec->last_pushed_version != its game's current
        .           // s->version), skipping `skip` and, when `game` is non-NULL, every OTHER
        .           // game too. ONE call site: the eventfd-triggered path, woken by
        .           // epoll_notify_game_changed from bot_thread on a DIFFERENT thread — since
        .           // the eventfd carries no payload (which game changed), this is called with
        .           // game=NULL, skip=NULL and checks every /ws connection this worker owns.
-- line 2051 ----------------------------------------
-- line 2065 ----------------------------------------
        .           // human can't otherwise notice without polling); this keeps it scoped to
        .           // exactly that — human-to-human visibility stays the same as every earlier
        .           // stage: a seat learns about another seat's move on its own next round trip.
        .           // Same state_put_cached discipline every other reply in this file uses —
        .           // this is a pure fan-out of that same cached-view read, not a new
        .           // serialization path.
        .           static void worker_push_stale(Worker *w, GameSlot *game, EConn *skip) {
        .               static _Thread_local unsigned char scratch[1 + 65536];   // this worker's own thread only — never shared
      238 ( 0.00%)      for (EConn *ec = w->ws_head; ec; ec = ec->next) {
        .                   if (ec == skip) continue;
        .                   if (game && ec->slot != game) continue;
       37 ( 0.00%)          GameSlot *s = ec->slot;
      148 ( 0.00%)          pthread_mutex_lock(&s->lock);
    1,332 ( 0.00%)  => ./nptl/./nptl/pthread_mutex_lock.c:pthread_mutex_lock@@GLIBC_2.2.5 (37x)
       37 ( 0.00%)          uint32_t v = s->version;
       37 ( 0.00%)          bool stale = ec->last_pushed_version != v;
        .                   int slen = 0;
      117 ( 0.00%)          if (stale) { scratch[0] = 0; slen = s->used ? state_put_cached(s, ec->cache_idx, ec->viewer, scratch + 1) : 0; }
      111 ( 0.00%)          pthread_mutex_unlock(&s->lock);
      962 ( 0.00%)  => ./nptl/./nptl/pthread_mutex_unlock.c:pthread_mutex_unlock@@GLIBC_2.2.5 (37x)
        .                   if (!stale) continue;
       16 ( 0.00%)          int mtotal = slen + 1;
      128 ( 0.00%)          if (econn_reserve_out(ec, mtotal + 14) && ws_send_frame(&ec->wc_out, WS_OP_BIN, scratch, mtotal) >= 0) {
    2,608 ( 0.00%)  => /home/user/foolish/server/impls/native/ws.c:ws_send_frame (16x)
        .                       econn_commit_out(ec);
       32 ( 0.00%)              ec->last_pushed_version = v;
       32 ( 0.00%)              if (!econn_try_flush(w, ec)) continue;   // may have closed ec on a real write error — nothing left to do for it
    1,600 ( 0.00%)  => foolish_server.c:econn_try_flush (16x)
        .                   }
        .               }
        .           }
        .           
        .           // Drains fds handed off by the dispatcher (epoll_dispatch_ws/
        .           // epoll_dispatch_oneshot) into this worker's own epoll set. Each arrives
        .           // with its FIRST reply/handshake already encoded into wbuf (the dispatcher
        .           // built it via the buffered-Conn trick before handing the fd over) — this
        .           // just registers it for EPOLLOUT (to flush that) and, for a /ws connection,
        .           // EPOLLIN too (to read whatever the client sends next) and links it into
        .           // ws_head.
        .           static void drain_handoff_queue(Worker *w) {
       81 ( 0.00%)      pthread_mutex_lock(&w->handoff_mtx);
      972 ( 0.00%)  => ./nptl/./nptl/pthread_mutex_lock.c:pthread_mutex_lock@@GLIBC_2.2.5 (27x)
       27 ( 0.00%)      EConn *head = w->handoff_head;
       54 ( 0.00%)      w->handoff_head = w->handoff_tail = NULL;
       81 ( 0.00%)      pthread_mutex_unlock(&w->handoff_mtx);
      702 ( 0.00%)  => ./nptl/./nptl/pthread_mutex_unlock.c:pthread_mutex_unlock@@GLIBC_2.2.5 (27x)
      139 ( 0.00%)      while (head) {
       58 ( 0.00%)          EConn *ec = head; head = head->next; ec->next = NULL;
       87 ( 0.00%)          if (ec->wlen <= ec->woff) { econn_close(w, ec); continue; }   // nothing to send at all — shouldn't happen, defensive
      116 ( 0.00%)          uint32_t events = (uint32_t)((ec->kind == ECONN_WS ? EPOLLIN : 0) | EPOLLOUT);
       29 ( 0.00%)          ec->want_epollout = true;
       58 ( 0.00%)          struct epoll_event ev = { .events = events, .data.ptr = ec };
      232 ( 0.00%)          if (epoll_ctl(w->epfd, EPOLL_CTL_ADD, ec->fd, &ev) < 0) { close(ec->fd); free(ec->wbuf); free(ec); continue; }
      203 ( 0.00%)  => ./misc/../sysdeps/unix/syscall-template.S:epoll_ctl (29x)
       58 ( 0.00%)          if (ec->kind == ECONN_WS) {
       18 ( 0.00%)              ec->prev = NULL; ec->next = w->ws_head;
       16 ( 0.00%)              if (w->ws_head) w->ws_head->prev = ec;
       12 ( 0.00%)              w->ws_head = ec;
        .                   }
        .               }
        .           }
        .           
        .           // EPOLLIN on a live /ws connection: drain what's available non-blockingly,
        .           // feed it through wsasync_feed, and for each complete message, apply it
        .           // (ws_service_message — the SAME function ws_conn_thread uses) and reply.
        .           static void handle_ws_readable(Worker *w, EConn *ec) {
        .               for (;;) {
        .                   unsigned char tmp[8192];
  505,230 ( 0.55%)          ssize_t r = read(ec->fd, tmp, sizeof tmp);
5,759,622 ( 6.31%)  => ./io/../sysdeps/unix/sysv/linux/read.c:read (101,046x)
  202,092 ( 0.22%)          if (r < 0) {
        .                       if (errno == EAGAIN || errno == EWOULDBLOCK) return;
        .                       if (errno == EINTR) continue;
        .                       econn_close(w, ec); return;
        .                   }
  101,046 ( 0.11%)          if (r == 0) { econn_close(w, ec); return; }   // peer closed
        .                   int off = 0;
  505,976 ( 0.55%)          while (off < (int)r) {
  101,473 ( 0.11%)              int consumed = 0;
  811,784 ( 0.89%)              int rc = wsasync_feed(ec, tmp + off, (int)r - off, &consumed);
22,367,186 (24.51%)  => foolish_server.c:wsasync_feed (101,473x)
        .                       off += consumed;
  203,018 ( 0.22%)              if (rc == WSF_MESSAGE) {
        .                           unsigned char reply[1 + 65536];
        .                           uint32_t v;
1,318,681 ( 1.45%)                  int mtotal = ws_service_message(ec->slot, ec->seat, ec->spectator, ec->cache_idx, ec->viewer,
14,306,124 (15.68%)  => foolish_server.c:ws_service_message (101,437x)
  101,437 ( 0.11%)                                                   ec->msg_buf, ec->last_msg_len, reply, &v);
  811,496 ( 0.89%)                  if (econn_reserve_out(ec, mtotal + 14) && ws_send_frame(&ec->wc_out, WS_OP_BIN, reply, mtotal) >= 0) {
16,502,355 (18.09%)  => /home/user/foolish/server/impls/native/ws.c:ws_send_frame (101,437x)
        .                               econn_commit_out(ec);
  202,874 ( 0.22%)                      ec->last_pushed_version = v;
        .                           }
  202,874 ( 0.22%)                  if (!econn_try_flush(w, ec)) return;   // closed (real write error) — ec is gone
10,143,700 (11.12%)  => foolish_server.c:econn_try_flush (101,437x)
        .                           // Deliberately NOT fanning this out to the game's OTHER
        .                           // connections here (unlike the bot_thread/eventfd path
        .                           // below) — see worker_push_stale's doc for why: at
        .                           // multi-seat scale, proactively pushing on every HUMAN
        .                           // move creates a thundering herd (each push wakes every
        .                           // other seat's client, which immediately re-checks
        .                           // eligibility and often re-submits, racing the others) that
        .                           // measurably hurt the submitted-vs-applied ratio without
-- line 2155 ----------------------------------------
-- line 2163 ----------------------------------------
        .                           // NEVER re-derive that by re-reading `ec` afterward, which
        .                           // would be a use-after-free the moment it already freed it.
        .                           if (econn_try_flush(w, ec)) econn_close(w, ec);
        .                           return;
        .                       } else {
        .                           break;   // WSF_NEED_MORE
        .                       }
        .                   }
  303,138 ( 0.33%)          if ((size_t)r < sizeof tmp) return;   // short read: likely drained for now — next EPOLLIN will confirm
        .               }
        .           }
        .           
       80 ( 0.00%)  static void *epoll_worker_main(void *arg) {
        .               Worker *w = arg;
        .               struct epoll_event events[EPOLL_MAX_EVENTS];
        .               for (;;) {
  509,374 ( 0.56%)          int n = epoll_wait(w->epfd, events, EPOLL_MAX_EVENTS, -1);
5,093,580 ( 5.58%)  => ./misc/../sysdeps/unix/sysv/linux/epoll_wait.c:epoll_wait (84,895x)
      918 ( 0.00%)  => ./elf/../sysdeps/x86_64/dl-trampoline.h:_dl_runtime_resolve_xsave (1x)
  169,782 ( 0.19%)          if (n < 0) { if (errno == EINTR) continue; break; }
        .                   bool woke = false;
  473,106 ( 0.52%)          for (int i = 0; i < n; i++) {
  404,432 ( 0.44%)              if (events[i].data.ptr == NULL) {   // the wake_evfd itself — see epoll_notify_game_changed / worker_handoff_push
      135 ( 0.00%)                  uint64_t v; ssize_t rd = read(w->wake_evfd, &v, sizeof v); (void)rd;
    1,539 ( 0.00%)  => ./io/../sysdeps/unix/sysv/linux/read.c:read (27x)
        .                           woke = true;
        .                           continue;
        .                       }
        .                       EConn *ec = events[i].data.ptr;
  101,081 ( 0.11%)              uint32_t evb = events[i].events;
  202,162 ( 0.22%)              if (evb & (EPOLLHUP | EPOLLERR)) { econn_close(w, ec); continue; }
  202,266 ( 0.22%)              if (evb & EPOLLOUT) { if (!econn_try_flush(w, ec)) continue; }
   17,768 ( 0.02%)  => foolish_server.c:econn_try_flush (29x)
  505,230 ( 0.55%)              if (evb & EPOLLIN) { if (ec->kind == ECONN_WS) handle_ws_readable(w, ec); }
        .                   }
  169,782 ( 0.19%)          if (woke) {
        .                       drain_handoff_queue(w);
        .                       worker_push_stale(w, NULL, NULL);   // don't know which game(s) a bot_thread notification was for — check them all
        .                   }
        .               }
        .               return NULL;
        .           }
        .           
        .           // Pushes a freshly-prepared EConn (its first reply/handshake already
        .           // encoded into wbuf) onto `w`'s handoff queue and wakes it — the ONE way an
        .           // fd crosses from the dispatcher thread to its owning worker thread.
        .           static void worker_handoff_push(Worker *w, EConn *ec) {
      232 ( 0.00%)      pthread_mutex_lock(&w->handoff_mtx);
    1,044 ( 0.00%)  => ./nptl/./nptl/pthread_mutex_lock.c:pthread_mutex_lock@@GLIBC_2.2.5 (29x)
        6 ( 0.00%)      ec->next = NULL;
      174 ( 0.00%)      if (w->handoff_tail) w->handoff_tail->next = ec; else w->handoff_head = ec;
       29 ( 0.00%)      w->handoff_tail = ec;
       87 ( 0.00%)      pthread_mutex_unlock(&w->handoff_mtx);
      754 ( 0.00%)  => ./nptl/./nptl/pthread_mutex_unlock.c:pthread_mutex_unlock@@GLIBC_2.2.5 (29x)
       29 ( 0.00%)      uint64_t one = 1;
      145 ( 0.00%)      ssize_t wr = write(w->wake_evfd, &one, sizeof one);
    1,653 ( 0.00%)  => ./io/../sysdeps/unix/sysv/linux/write.c:write (29x)
        .               (void)wr;
        .           }
        .           
        .           // --------------------------------------------------------------------------
        .           // Dispatcher-side handlers (Stage 6). Called from main()'s accept loop, on
        .           // the dispatcher thread, AFTER read_and_parse_request has already fully
        .           // read this request (headers + body) — exactly the same blocking read every
        .           // earlier stage did here. `conn`'s fd is still in its default BLOCKING mode
-- line 2220 ----------------------------------------
-- line 2223 ----------------------------------------
        .           // --------------------------------------------------------------------------
        .           
        .           // /action, /state, /status, /meta — game_id-sharded one-shot HTTP. Builds
        .           // the response with the UNCHANGED route()/h_action/h_state/h_status/h_meta
        .           // handlers (a buffered Conn — conn.h — captures their output into memory
        .           // instead of a real write()), then hands the connection to its shard's
        .           // worker purely to flush that response non-blockingly and close.
        .           static void epoll_dispatch_oneshot(Conn *conn, Req *r, char *raw_buf) {
       92 ( 0.00%)      EConn *ec = calloc(1, sizeof *ec);
  202,630 ( 0.22%)  => ./malloc/./malloc/malloc.c:calloc (23x)
       46 ( 0.00%)      if (!ec) { conn_close(conn); free(raw_buf); return; }
        .               ec->kind = ECONN_ONE_SHOT;
       46 ( 0.00%)      ec->fd = conn->fd;
       23 ( 0.00%)      ec->wbuf_cap = ONESHOT_WBUF_CAP;
       92 ( 0.00%)      ec->wbuf = malloc((size_t)ec->wbuf_cap);
    5,142 ( 0.01%)  => ./malloc/./malloc/malloc.c:malloc (23x)
       46 ( 0.00%)      if (!ec->wbuf) { free(ec); conn_close(conn); free(raw_buf); return; }
        .           
        .               Conn buffered; conn_init_buffered(&buffered, ec->wbuf, ec->wbuf_cap);
       69 ( 0.00%)      route(r, &buffered);   // h_action/h_state/h_status/h_meta/h_stats — completely unchanged
  585,923 ( 0.64%)  => foolish_server.c:route (23x)
       46 ( 0.00%)      ec->wlen = buffered.buf_len;
       23 ( 0.00%)      ec->close_after_flush = true;
       69 ( 0.00%)      free(raw_buf);
    4,734 ( 0.01%)  => ./malloc/./malloc/malloc.c:free (23x)
        .           
      142 ( 0.00%)      int fl = fcntl(ec->fd, F_GETFL, 0);
      826 ( 0.00%)  => ./elf/../sysdeps/x86_64/dl-trampoline.h:_dl_runtime_resolve_xsave (1x)
      943 ( 0.00%)  => ./io/../sysdeps/unix/sysv/linux/fcntl64.c:fcntl (23x)
      207 ( 0.00%)      if (fl >= 0) fcntl(ec->fd, F_SETFL, fl | O_NONBLOCK);
      943 ( 0.00%)  => ./io/../sysdeps/unix/sysv/linux/fcntl64.c:fcntl (23x)
        .           
       46 ( 0.00%)      char gid[ID_LEN + 1] = {0};
       92 ( 0.00%)      const char *gp = strstr(r->query, "game_id=");
    1,081 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/strstr-sse2-unaligned.S:__strstr_sse2_unaligned (23x)
       46 ( 0.00%)      if (gp) { gp += 8; int i = 0; while (gp[i] && gp[i] != '&' && i < ID_LEN) { gid[i] = gp[i]; i++; } gid[i] = 0; }
        .               worker_handoff_push(&g_workers[game_worker_index(gid)], ec);
        .           }
        .           
        .           // GET /ws (seated or ?spectator=1) — validates + completes the handshake
        .           // HERE (ws_handshake_validate/ws_send_handshake_and_push — the exact checks
        .           // and wire bytes ws_conn_thread has always produced), then hands the
        .           // now-upgraded connection to its shard's worker. No thread is spawned.
        .           static void epoll_dispatch_ws(Conn *conn, Req *r, char *raw_buf) {
        .               int seat; bool spectator; int cache_idx, viewer;
       36 ( 0.00%)      GameSlot *s = ws_handshake_validate(r, &seat, &spectator, &cache_idx, &viewer);
    8,981 ( 0.01%)  => foolish_server.c:ws_handshake_validate (6x)
       12 ( 0.00%)      if (!s) { respond(conn, 401, "{\"error\":\"ws auth\"}"); conn_close(conn); free(raw_buf); return; }
        .               char accept[64];
       36 ( 0.00%)      if (!ws_accept_from_key(r->ws_key, accept, sizeof accept)) {
   65,556 ( 0.07%)  => /home/user/foolish/server/impls/native/ws.c:ws_accept_from_key (6x)
        .                   respond(conn, 400, "{\"error\":\"ws key\"}"); conn_close(conn); free(raw_buf); return;
        .               }
        .           
       24 ( 0.00%)      EConn *ec = calloc(1, sizeof *ec);
   53,194 ( 0.06%)  => ./malloc/./malloc/malloc.c:calloc (6x)
       12 ( 0.00%)      if (!ec) { conn_close(conn); free(raw_buf); return; }
        6 ( 0.00%)      ec->kind = ECONN_WS;
       12 ( 0.00%)      ec->fd = conn->fd;
       72 ( 0.00%)      ec->slot = s; ec->seat = seat; ec->spectator = spectator; ec->cache_idx = cache_idx; ec->viewer = viewer;
        6 ( 0.00%)      ec->phase = WSP_HDR2; ec->msg_opcode = -1;
        6 ( 0.00%)      ec->wbuf_cap = WS_WBUF_CAP;
       24 ( 0.00%)      ec->wbuf = malloc((size_t)ec->wbuf_cap);
    1,871 ( 0.00%)  => ./malloc/./malloc/malloc.c:malloc (6x)
       12 ( 0.00%)      if (!ec->wbuf) { free(ec); conn_close(conn); free(raw_buf); return; }
        .           
        .               Conn buffered; conn_init_buffered(&buffered, ec->wbuf, ec->wbuf_cap);
        .               WsConn wc;
       78 ( 0.00%)      if (!ws_send_handshake_and_push(&wc, buffered, accept, s, cache_idx, viewer)) {
    8,514 ( 0.01%)  => foolish_server.c:ws_send_handshake_and_push (6x)
        .                   free(ec->wbuf); free(ec); conn_close(conn); free(raw_buf); return;
        .               }
       12 ( 0.00%)      ec->wlen = wc.conn.buf_len; ec->woff = 0;
       24 ( 0.00%)      pthread_mutex_lock(&s->lock);
      216 ( 0.00%)  => ./nptl/./nptl/pthread_mutex_lock.c:pthread_mutex_lock@@GLIBC_2.2.5 (6x)
       12 ( 0.00%)      ec->last_pushed_version = s->version;
       18 ( 0.00%)      pthread_mutex_unlock(&s->lock);
      156 ( 0.00%)  => ./nptl/./nptl/pthread_mutex_unlock.c:pthread_mutex_unlock@@GLIBC_2.2.5 (6x)
        .           
        .               // Any bytes the client pipelined past the upgrade request (the same
        .               // case ws_conn_prime primes into WsConn.pending for the thread-per-
        .               // connection path) — feed them straight into this connection's async
        .               // parser now, before the fd is even handed to its worker, so a
        .               // pipelined first move/poll is never lost.
       24 ( 0.00%)      if (r->body_len > 0) {
        .                   int off = 0, n = r->body_len; const unsigned char *body = (const unsigned char *)r->body;
        .                   while (off < n) {
        .                       int consumed = 0;
        .                       int rc = wsasync_feed(ec, body + off, n - off, &consumed);
        .                       off += consumed;
        .                       if (rc == WSF_MESSAGE) {
        .                           unsigned char reply[1 + 65536]; uint32_t v;
        .                           int mtotal = ws_service_message(s, seat, spectator, cache_idx, viewer, ec->msg_buf, ec->last_msg_len, reply, &v);
-- line 2300 ----------------------------------------
-- line 2302 ----------------------------------------
        .                               econn_commit_out(ec);
        .                               ec->last_pushed_version = v;
        .                           }
        .                       } else if (rc != WSF_NEED_MORE) {
        .                           break;   // a malformed pipelined frame — let the worker's own error handling take it from here once connected
        .                       }
        .                   }
        .               }
       18 ( 0.00%)      free(raw_buf);
    1,270 ( 0.00%)  => ./malloc/./malloc/malloc.c:free (6x)
        .           
       36 ( 0.00%)      int fl = fcntl(ec->fd, F_GETFL, 0);
      246 ( 0.00%)  => ./io/../sysdeps/unix/sysv/linux/fcntl64.c:fcntl (6x)
       54 ( 0.00%)      if (fl >= 0) fcntl(ec->fd, F_SETFL, fl | O_NONBLOCK);
      246 ( 0.00%)  => ./io/../sysdeps/unix/sysv/linux/fcntl64.c:fcntl (6x)
        .           
        .               worker_handoff_push(&g_workers[game_worker_index(s->id)], ec);
        .           }
        .           
        .           // --------------------------------------------------------------------------
        .           // STAGE 3: TLS listener state. `g_tls_ctx` is built ONCE in main() — before
        .           // the accept loop, before any worker/connection thread exists — off
        .           // tls_server_ctx_create (conn.c), then only ever READ afterward (every
-- line 2321 ----------------------------------------
-- line 2328 ----------------------------------------
        .           // once per accepted connection.
        .           // --------------------------------------------------------------------------
        .           static SSL_CTX *g_tls_ctx = NULL;
        .           
        .           // --------------------------------------------------------------------------
        .           // Connection handling — the dispatcher (T2a Deliverable 2)
        .           // --------------------------------------------------------------------------
        .           
        8 ( 0.00%)  int main(int argc, char **argv) {
        .               int port = 8099;
        .               // Default: DB ON (see DURABILITY.md — "Stage 2: persistence"). --no-db
        .               // opts all the way out (pure in-memory, e.g. for tests/benchmarks that
        .               // don't want a stray file); --db=PATH points at a specific file instead
        .               // of the default; --persist-interval-ms tunes the write-behind period
        .               // (50-100ms is the documented sweet spot — see persist.h).
        .               const char *db_path = "./foolish.db";
        .               int persist_interval_ms = 75;
-- line 2344 ----------------------------------------
-- line 2345 ----------------------------------------
        .               // Stage 3: TLS is OFF by default (plaintext, byte-for-byte the same as
        .               // every earlier stage) — `--tls --cert=PATH --key=PATH` turns the WHOLE
        .               // listen socket over to TLS (HTTPS + WSS); there is no mixed
        .               // plaintext+TLS listener in this design (see TLS.md for why a single
        .               // `--tls`-flips-the-listener design was chosen over a second
        .               // `--tls-port`).
        .               bool want_tls = false;
        .               const char *cert_path = NULL, *key_path = NULL;
       12 ( 0.00%)      for (int i = 1; i < argc; i++) {
       28 ( 0.00%)          if (!strncmp(argv[i], "--game-workers=", 15)) {
      889 ( 0.00%)  => ./elf/../sysdeps/x86_64/dl-trampoline.h:_dl_runtime_resolve_xsave (1x)
       72 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/strcmp-avx2.S:__strncmp_avx2 (3x)
        1 ( 0.00%)              int nw = atoi(argv[i] + 15);
        5 ( 0.00%)              if (nw > 0 && nw <= MAX_GAME_WORKERS) g_n_game_workers = nw;
       14 ( 0.00%)          } else if (!strncmp(argv[i], "--meta-workers=", 15)) {
       50 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/strcmp-avx2.S:__strncmp_avx2 (2x)
        .                       int nw = atoi(argv[i] + 15);
        .                       if (nw >= 0 && nw <= MAX_META_WORKERS) g_n_meta_workers = nw;   // 0 = fold /meta onto the game pool
       14 ( 0.00%)          } else if (!strncmp(argv[i], "--create-workers=", 17)) {
       50 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/strcmp-avx2.S:__strncmp_avx2 (2x)
        .                       int nw = atoi(argv[i] + 17);
        .                       if (nw > 0 && nw <= MAX_CREATE_WORKERS) g_n_create_workers = nw;
       16 ( 0.00%)          } else if (!strcmp(argv[i], "--no-db")) {
      847 ( 0.00%)  => ./elf/../sysdeps/x86_64/dl-trampoline.h:_dl_runtime_resolve_xsave (1x)
       40 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/strcmp-avx2.S:__strcmp_avx2 (2x)
        .                       db_path = NULL;
        7 ( 0.00%)          } else if (!strncmp(argv[i], "--db=", 5)) {
       25 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/strcmp-avx2.S:__strncmp_avx2 (1x)
        .                       db_path = argv[i] + 5;
        7 ( 0.00%)          } else if (!strncmp(argv[i], "--persist-interval-ms=", 22)) {
       25 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/strcmp-avx2.S:__strncmp_avx2 (1x)
        .                       int v = atoi(argv[i] + 22);
        .                       if (v > 0) persist_interval_ms = v;
        6 ( 0.00%)          } else if (!strcmp(argv[i], "--tls")) {
       20 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/strcmp-avx2.S:__strcmp_avx2 (1x)
        .                       want_tls = true;
        7 ( 0.00%)          } else if (!strncmp(argv[i], "--cert=", 7)) {
       25 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/strcmp-avx2.S:__strncmp_avx2 (1x)
        .                       cert_path = argv[i] + 7;
        7 ( 0.00%)          } else if (!strncmp(argv[i], "--key=", 6)) {
       25 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/strcmp-avx2.S:__strncmp_avx2 (1x)
        .                       key_path = argv[i] + 6;
        .                   } else {
        .                       int p = atoi(argv[i]);
        3 ( 0.00%)              if (p > 0) port = p;
        .                   }
        .               }
       22 ( 0.00%)      srand((unsigned)(time(NULL) ^ getpid()));   // only ever called here, before any worker thread exists
    2,510 ( 0.00%)  => ./elf/../sysdeps/x86_64/dl-trampoline.h:_dl_runtime_resolve_xsave (3x)
    6,232 ( 0.01%)  => ./stdlib/./stdlib/random.c:srand (1x)
        6 ( 0.00%)  => ./time/../sysdeps/unix/sysv/linux/time.c:time_syscall (1x)
        4 ( 0.00%)  => ./posix/../sysdeps/unix/syscall-template.S:getpid (1x)
        .               // Stage 4: resolve "octogen"'s STRAT_* brain id ONCE, before any
        .               // bot_thread can run — g_octogen_strat is read-only from every thread
        .               // after this line, so no lock is needed (same posture g_tls_ctx's doc
        .               // takes). Via bot_roster_at(...)->strat, NOT bot_roster_find's own
        .               // return value directly — that's a roster ARRAY INDEX, a different
        .               // number from the STRAT_* id seats actually carry (see h_meta's add-bot
        .               // branch for the full story; this counter needs to compare against the
        .               // SAME value strategy_key holds). -1 (an unknown key, or a somehow-
        .               // absent roster entry) just means the per-strategy octogen counter never
        .               // increments — g_bot_decisions still counts every bot's applied actions
        .               // regardless.
        .               {
        2 ( 0.00%)          int ridx = bot_roster_find("octogen");
      208 ( 0.00%)  => /home/user/foolish/server/impls/native/../../../c/src/bot_roster.c:bot_roster_find (1x)
        4 ( 0.00%)          const BotRosterEntry *e = ridx >= 0 ? bot_roster_at(ridx) : NULL;
       11 ( 0.00%)  => /home/user/foolish/server/impls/native/../../../c/src/bot_roster.c:bot_roster_at (1x)
        4 ( 0.00%)          g_octogen_strat = e ? e->strat : -1;
        .               }
        .               // Long-lived /ws connections mean a peer can vanish (crash, network
        .               // reset, the load client's own `timeout` cutting it off) between our
        .               // read and our next write; the default SIGPIPE action is to kill the
        .               // WHOLE PROCESS on that write. Ignore it — write() already reports the
        .               // same failure as -1/EPIPE, which every write path here already checks
        .               // (and, under TLS, conn_read/conn_write (conn.c) translate the
        .               // equivalent SSL_ERROR_SYSCALL the same way — see their doc).
        8 ( 0.00%)      signal(SIGPIPE, SIG_IGN);
      862 ( 0.00%)  => ./elf/../sysdeps/x86_64/dl-trampoline.h:_dl_runtime_resolve_xsave (1x)
      120 ( 0.00%)  => ./signal/../sysdeps/posix/signal.c:signal (1x)
        .           
        .               // Stage 3: build the shared server SSL_CTX ONCE, before any worker pool
        .               // or the accept loop starts (same "set up before any other thread can
        .               // touch it" posture persist_start takes for crash recovery, just for a
        .               // different piece of startup state) — see g_tls_ctx's own doc above. A
        .               // REQUESTED (--tls passed) but failed TLS setup is fatal, never a
        .               // silent downgrade to plaintext: same posture Stage 2 takes for a
        .               // requested-but-failed --db (see persist_start's doc) — serving
        .               // plaintext when the operator asked for TLS would be a silent security
        .               // regression, worse than refusing to start.
        2 ( 0.00%)      if (want_tls) {
        .                   if (!cert_path || !key_path) {
        .                       fprintf(stderr, "fatal: --tls requires --cert=PATH --key=PATH\n");
        .                       return 1;
        .                   }
        .                   g_tls_ctx = tls_server_ctx_create(cert_path, key_path);
        .                   if (!g_tls_ctx) {
        .                       fprintf(stderr, "fatal: TLS setup failed (--cert=%s --key=%s) — "
        .                                        "check the cert/key are valid PEM and the key matches the cert\n",
-- line 2424 ----------------------------------------
-- line 2432 ----------------------------------------
        .               // itself), then registering the two durable tables and starting
        .               // persistence — BEFORE any worker pool or the accept loop exists, so
        .               // crash recovery's synchronous load (inside persist_start) runs with no
        .               // other thread able to touch g_users[]/g_games[] yet. A requested
        .               // (non-NULL) --db that fails to open/configure is fatal: silently
        .               // downgrading a requested durability guarantee to "pretend it's fine"
        .               // would be worse than refusing to start.
        .               persist_self_test();
        7 ( 0.00%)      g_game_table = persist_register_table("games", MAX_GAMES, PERSIST_GAME_BLOB_CAP,
   24,022 ( 0.03%)  => /home/user/foolish/server/impls/native/persist.c:persist_register_table (1x)
        .                                                      game_persist_snapshot, game_persist_load);
        8 ( 0.00%)      g_user_table = persist_register_table("users", MAX_USERS, PERSIST_USER_BLOB_CAP,
   23,564 ( 0.03%)  => /home/user/foolish/server/impls/native/persist.c:persist_register_table (1x)
        .                                                      user_persist_snapshot, user_persist_load);
        5 ( 0.00%)      if (!persist_start(db_path, persist_interval_ms)) {
       25 ( 0.00%)  => /home/user/foolish/server/impls/native/persist.c:persist_start (1x)
        .                   fprintf(stderr, "fatal: persistence failed to start (--db=%s)\n", db_path ? db_path : "(null)");
        .                   return 1;
        .               }
        .           
        .               wq_init(&g_auth_create_q);
        .               wq_init(&g_meta_q);
       22 ( 0.00%)      for (int i = 0; i < g_n_game_workers; i++) wq_init(&g_game_q[i]);
        .           
        .               // /auth/* and /create stay on their own threaded worker pool in BOTH
        .               // modes (task-granted: "the dispatcher and create/auth worker can stay
        .               // threads") — no game_id to shard by, never the hot path (see
        .               // SERVER_SCALING.md's Deliverable 2 sweep — neither pool was ever close
        .               // to contended).
        .               pthread_t wt;
        5 ( 0.00%)      for (int i = 0; i < g_n_create_workers; i++)
       19 ( 0.00%)          if (pthread_create(&wt, NULL, worker_thread, &g_auth_create_q) == 0) pthread_detach(wt);
  105,205 ( 0.12%)  => ./nptl/./nptl/pthread_create.c:pthread_create@@GLIBC_2.34 (1x)
    2,032 ( 0.00%)  => ./elf/../sysdeps/x86_64/dl-trampoline.h:_dl_runtime_resolve_xsave (2x)
       13 ( 0.00%)  => ./nptl/./nptl/pthread_detach.c:pthread_detach@@GLIBC_2.34 (1x)
        .           
        2 ( 0.00%)      if (g_tls_ctx) {
        .                   // Stage 6 TLS fallback: non-blocking OpenSSL's WANT_READ/WANT_WRITE
        .                   // state machine is out of this stage's budget (see
        .                   // SERVER_SCALING.md "Stage 6" for the honest writeup) — a --tls
        .                   // server keeps the ENTIRE pre-Stage-6 design, byte-for-byte:
        .                   // thread-per-/ws-connection + the typed HTTP work-queue pools.
        .                   for (int i = 0; i < g_n_meta_workers; i++)
        .                       if (pthread_create(&wt, NULL, worker_thread, &g_meta_q) == 0) pthread_detach(wt);
        .                   for (int i = 0; i < g_n_game_workers; i++)
        .                       if (pthread_create(&wt, NULL, worker_thread, &g_game_q[i]) == 0) pthread_detach(wt);
        .               } else {
        .                   // Stage 6: plaintext runs epoll-per-shard instead of the typed game/
        .                   // meta queues + thread-per-/ws-connection — `--game-workers=N` now
        .                   // ALSO sizes the epoll shard count (same knob, see
        .                   // SERVER_SCALING.md). g_game_q/g_meta_q/worker_thread stay fully
        .                   // intact above (unused in this mode) purely for the --tls fallback.
       22 ( 0.00%)          for (int i = 0; i < g_n_game_workers; i++) {
        .                       Worker *w = &g_workers[i];
        4 ( 0.00%)              w->idx = i;
       20 ( 0.00%)              w->epfd = epoll_create1(0);
      859 ( 0.00%)  => ./elf/../sysdeps/x86_64/dl-trampoline.h:_dl_runtime_resolve_xsave (1x)
       24 ( 0.00%)  => ./misc/../sysdeps/unix/syscall-template.S:epoll_create1 (4x)
       24 ( 0.00%)              w->wake_evfd = eventfd(0, EFD_NONBLOCK);
      855 ( 0.00%)  => ./elf/../sysdeps/x86_64/dl-trampoline.h:_dl_runtime_resolve_xsave (1x)
       24 ( 0.00%)  => ./misc/../sysdeps/unix/syscall-template.S:eventfd (4x)
       16 ( 0.00%)              if (w->epfd < 0 || w->wake_evfd < 0) {
        .                           fprintf(stderr, "fatal: epoll worker %d setup failed (epoll_create1/eventfd)\n", i);
        .                           return 1;
        .                       }
       16 ( 0.00%)              pthread_mutex_init(&w->handoff_mtx, NULL);
      180 ( 0.00%)  => ./nptl/./nptl/pthread_mutex_init.c:pthread_mutex_init@@GLIBC_2.2.5 (4x)
       12 ( 0.00%)              w->handoff_head = w->handoff_tail = NULL;
        .                       w->ws_head = NULL;
        8 ( 0.00%)              struct epoll_event ev = { .events = EPOLLIN, .data.ptr = NULL };   // NULL data.ptr marks the wake_evfd itself — see epoll_worker_main
       36 ( 0.00%)              if (epoll_ctl(w->epfd, EPOLL_CTL_ADD, w->wake_evfd, &ev) < 0) {
      851 ( 0.00%)  => ./elf/../sysdeps/x86_64/dl-trampoline.h:_dl_runtime_resolve_xsave (1x)
       28 ( 0.00%)  => ./misc/../sysdeps/unix/syscall-template.S:epoll_ctl (4x)
        .                           fprintf(stderr, "fatal: epoll worker %d setup failed (epoll_ctl)\n", i);
        .                           return 1;
        .                       }
       32 ( 0.00%)              if (pthread_create(&wt, NULL, epoll_worker_main, w) != 0) {
  420,328 ( 0.46%)  => ./nptl/./nptl/pthread_create.c:pthread_create@@GLIBC_2.34 (4x)
        .                           fprintf(stderr, "fatal: epoll worker %d thread create failed\n", i);
        .                           return 1;
        .                       }
       12 ( 0.00%)              pthread_detach(wt);
       52 ( 0.00%)  => ./nptl/./nptl/pthread_detach.c:pthread_detach@@GLIBC_2.34 (4x)
        .                   }
        2 ( 0.00%)          g_epoll_active = true;
        .               }
        .           
       10 ( 0.00%)      int srv = socket(AF_INET, SOCK_STREAM, 0);
      814 ( 0.00%)  => ./elf/../sysdeps/x86_64/dl-trampoline.h:_dl_runtime_resolve_xsave (1x)
        6 ( 0.00%)  => ./socket/../sysdeps/unix/syscall-template.S:socket (1x)
       12 ( 0.00%)      int opt = 1; setsockopt(srv, SOL_SOCKET, SO_REUSEADDR, &opt, sizeof opt);
      908 ( 0.00%)  => ./elf/../sysdeps/x86_64/dl-trampoline.h:_dl_runtime_resolve_xsave (1x)
        7 ( 0.00%)  => ./socket/../sysdeps/unix/sysv/linux/setsockopt.c:setsockopt (1x)
        1 ( 0.00%)      struct sockaddr_in addr = {0};
        3 ( 0.00%)      addr.sin_family = AF_INET; addr.sin_addr.s_addr = INADDR_ANY; addr.sin_port = htons(port);
       10 ( 0.00%)      if (bind(srv, (struct sockaddr *)&addr, sizeof addr) < 0) { perror("bind"); return 1; }
      864 ( 0.00%)  => ./elf/../sysdeps/x86_64/dl-trampoline.h:_dl_runtime_resolve_xsave (1x)
        6 ( 0.00%)  => ./socket/../sysdeps/unix/syscall-template.S:bind (1x)
        8 ( 0.00%)      listen(srv, 64);
      891 ( 0.00%)  => ./elf/../sysdeps/x86_64/dl-trampoline.h:_dl_runtime_resolve_xsave (1x)
        6 ( 0.00%)  => ./socket/../sysdeps/unix/syscall-template.S:listen (1x)
       10 ( 0.00%)      fprintf(stderr, "foolish native server (kernel-driven, in-memory + SQLite write-behind%s) on :%d "
    2,989 ( 0.00%)  => ./stdio-common/./stdio-common/fprintf.c:fprintf (1x)
        .                       "(game-workers=%d meta-workers=%d create-workers=%d db=%s interval=%dms)\n",
        4 ( 0.00%)              g_tls_ctx ? " + TLS (https/wss)" : "",
        4 ( 0.00%)              port, g_n_game_workers, g_n_meta_workers, g_n_create_workers,
        4 ( 0.00%)              db_path ? db_path : "off (--no-db)", persist_interval_ms);
        .           
        .               // The dispatcher: accept, (Stage 3) do the TLS handshake if this
        .               // listener is running TLS, read+parse the request, then either hand it
        .               // to a dedicated thread (/ws — a persistent connection, see
        .               // ws_conn_thread's doc for why it stays off the typed queues) or
        .               // enqueue it onto the right typed worker pool (classify_queue). This is
        .               // the accept loop itself acting as dispatcher (no separate
        .               // reader-thread pool) — see SERVER_SCALING.md for why that's an
        .               // acceptable, correctness-first choice here rather than a scalability
        .               // bottleneck in practice.
        .               for (;;) {
      219 ( 0.00%)          int fd = accept(srv, NULL, NULL);
      840 ( 0.00%)  => ./elf/../sysdeps/x86_64/dl-trampoline.h:_dl_runtime_resolve_xsave (1x)
    2,422 ( 0.00%)  => ./socket/../sysdeps/unix/sysv/linux/accept.c:accept (43x)
       84 ( 0.00%)          if (fd < 0) continue;
        .                   // A one-shot HTTP request/response barely notices Nagle's algorithm,
        .                   // but a persistent /ws connection does many small back-and-forth
        .                   // writes+reads — without TCP_NODELAY, Nagle batching interacting
        .                   // with the peer's delayed ACKs turns each round trip into tens of
        .                   // milliseconds instead of tens of microseconds.
      336 ( 0.00%)          int one = 1; setsockopt(fd, IPPROTO_TCP, TCP_NODELAY, &one, sizeof one);
      294 ( 0.00%)  => ./socket/../sysdeps/unix/sysv/linux/setsockopt.c:setsockopt (42x)
        .           
        .                   // Stage 3: SSL_accept happens HERE, on this connection's own
        .                   // servicing path, before any HTTP parsing — a fresh SSL* per
        .                   // connection, off the one shared g_tls_ctx (see its doc above). A
        .                   // failed/abandoned handshake (a port scanner, a plaintext probe
        .                   // against a TLS listener, a client that hangs up mid-handshake) is
        .                   // just a dropped connection, same as a malformed plaintext request
        .                   // below — never fatal to the process.
        .                   Conn conn;
      126 ( 0.00%)          if (g_tls_ctx) {
        .                       if (!conn_tls_accept(&conn, g_tls_ctx, fd)) { close(fd); continue; }
        .                   } else {
        .                       conn_init_plain(&conn, fd);
        .                   }
        .           
      130 ( 0.00%)          char *buf = malloc(REQ_BUF_CAP);
      871 ( 0.00%)  => ./elf/../sysdeps/x86_64/dl-trampoline.h:_dl_runtime_resolve_xsave (1x)
    9,834 ( 0.01%)  => ./malloc/./malloc/malloc.c:malloc (42x)
       84 ( 0.00%)          if (!buf) { conn_close(&conn); continue; }
        .                   Req r;
        .                   if (!read_and_parse_request(&conn, buf, REQ_BUF_CAP, &r)) { conn_close(&conn); free(buf); continue; }
        .           
      150 ( 0.00%)          if (r.is_ws_upgrade && !strcmp(r.method, "GET") && !strcmp(r.path, "/ws")) {
       12 ( 0.00%)              if (g_tls_ctx) {
        .                           // Stage 6 TLS fallback: dedicated per-connection thread
        .                           // (design B, see ws_conn_thread's doc) — this thread lives
        .                           // for the client's whole session, so the pthread_create
        .                           // cost is paid once per client instead of once per action.
        .                           // See PROFILE_HOTPATH.md T1b.
        .                           WsSpawnArg *sa = malloc(sizeof *sa);
        .                           if (!sa) { conn_close(&conn); free(buf); continue; }
        .                           sa->conn = conn; sa->req = r; sa->raw_buf = buf;
-- line 2563 ----------------------------------------
-- line 2571 ----------------------------------------
        .                           epoll_dispatch_ws(&conn, &r, buf);
        .                       }
        .                       continue;
        .                   }
        .           
        .                   // Cheap, store-free routes: answer inline instead of paying a queue
        .                   // round trip for them, in EITHER mode (no game_id, never worth
        .                   // sharding to an epoll worker just to flush a few bytes).
      108 ( 0.00%)          if (!strcmp(r.method, "OPTIONS")) { respond(&conn, 200, "{}"); conn_close(&conn); free(buf); continue; }
      144 ( 0.00%)          if (!strcmp(r.path, "/health"))  { respond(&conn, 200, "{\"ok\":true}"); conn_close(&conn); free(buf); continue; }
      238 ( 0.00%)          if (!strcmp(r.path, "/stats"))   { h_stats(&r, &conn); conn_close(&conn); free(buf); continue; }
        .           
       64 ( 0.00%)          if (g_tls_ctx) {
        .                       // Stage 6 TLS fallback: unchanged typed HTTP work-queue pools.
        .                       WorkItem item; item.conn = conn; item.req = r; item.raw_buf = buf;
        .                       wq_push(classify_queue(&r), &item);
        .                       continue;
        .                   }
        .                   // Stage 6 plaintext: /auth/signup, /auth/signin, /create have no
        .                   // game_id to shard by — stay on the threaded create/auth pool
        .                   // (started above, both modes). Everything else here
        .                   // (/action,/state,/status,/meta) is game_id-sharded — hand it to
        .                   // its shard's epoll worker instead of the (now TLS-only) typed
        .                   // game/meta queues.
      579 ( 0.00%)          if (!strcmp(r.path, "/auth/signup") || !strcmp(r.path, "/auth/signin") || !strcmp(r.path, "/create")) {
       81 ( 0.00%)              WorkItem item; item.conn = conn; item.req = r; item.raw_buf = buf;
      801 ( 0.00%)  => ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:__memcpy_avx_unaligned_erms (9x)
        .                       wq_push(&g_auth_create_q, &item);
        .                   } else {
        .                       epoll_dispatch_oneshot(&conn, &r, buf);
        .                   }
        .               }
        .           }

6,132,445 ( 6.72%)  <counts for unidentified lines in foolish_server.c>

--------------------------------------------------------------------------------
-- Auto-annotated source: ./conn.h
--------------------------------------------------------------------------------
Ir               

-- line 37 ----------------------------------------
      .           typedef struct {
      .               int fd;
      .               SSL *ssl;
      .               unsigned char *buf_out;   // NULL unless this Conn is in buffered (Stage 6) mode
      .               int buf_len;
      .               int buf_cap;
      .           } Conn;
      .           
    210 ( 0.00%)  static inline void conn_init_plain(Conn *c, int fd) { c->fd = fd; c->ssl = NULL; c->buf_out = NULL; c->buf_len = 0; c->buf_cap = 0; }
      .           
      .           // Stage 6: wrap a caller-owned `out[0..cap)` scratch buffer as a Conn. Every
      .           // conn_write (and everything built on it — respond/respond_bin/ws_send_frame)
      .           // appends to `out` instead of touching a socket; conn_read must never be
      .           // called on the result. `out_len(out)`-equivalent is just the caller reading
      .           // back the Conn's own `buf_len` after the encoding calls finish.
      .           static inline void conn_init_buffered(Conn *c, unsigned char *out, int cap) {
405,957 ( 0.44%)      c->fd = -1; c->ssl = NULL; c->buf_out = out; c->buf_len = 0; c->buf_cap = cap;
      .           }
101,459 ( 0.11%)  static inline bool conn_is_buffered(const Conn *c) { return c->buf_out != NULL; }
      .           
      .           // Single-call, EINTR-transparent read/write — the SAME contract a bare
      .           // read()/write() on a blocking fd has (a short result is normal, not an
      .           // error; 0 means the peer is done / cleanly closed; <0 means a real error,
      .           // never crashes the process — SIGPIPE is separately ignored, see
      .           // foolish_server.c's/foolish_hammer.c's main()). Dispatches to read()/
      .           // write() when c->ssl is NULL (byte-for-byte the same behavior a plain `int
      .           // fd` had before Stage 3 — see TLS.md's "plaintext is unchanged" claim),
-- line 63 ----------------------------------------

--------------------------------------------------------------------------------
-- Auto-annotated source: ../../../c/src/view.c
--------------------------------------------------------------------------------
Ir              

     .           #include "view.h"
     .           #include "../wasm/wire.h"
     .           
     .           // Whether the hand of seat `seat` is visible to `viewer`.
     .           static int hand_visible(int viewer, int seat) {
 3,256 ( 0.00%)      return viewer == VIEW_UNMASKED || viewer == seat;
     .           }
     .           
     .           int state_put(const Game *g, int viewer, unsigned char *out) {
 9,993 ( 0.01%)      const int unmasked = (viewer == VIEW_UNMASKED);
     .               unsigned char *q = out;
 1,626 ( 0.00%)      *q++ = (unsigned char)g->status;
 1,626 ( 0.00%)      *q++ = (unsigned char)g->num_players;
 1,626 ( 0.00%)      *q++ = (unsigned char)g->power_suit;
 1,626 ( 0.00%)      *q++ = (unsigned char)g->first_attacker;
 1,626 ( 0.00%)      *q++ = (unsigned char)g->defender;
 1,626 ( 0.00%)      *q++ = (unsigned char)(g->discard_pile_length & 0xff);
 1,626 ( 0.00%)      *q++ = (unsigned char)((g->discard_pile_length >> 8) & 0xff);
 1,626 ( 0.00%)      *q++ = (unsigned char)(g->has_flipped ? 1 : 0);
     .               // Canonical no-flip byte: after the flipped trump is drawn the kernel
     .               // keeps the stale card in g->flipped (gated by has_flipped) — writing it
     .               // would make byte-equal states serialize differently depending on
     .               // whether they came from a marshal round-trip (which normalizes to the
     .               // {0,0} placeholder -> WIRE_CARD_HIDDEN) or stayed kernel-resident.
     .               // Decoders ignore this byte when has_flipped is 0.
 3,018 ( 0.00%)      *q++ = g->has_flipped ? wire_from_card(g->flipped) : (unsigned char)WIRE_CARD_HIDDEN;
 1,626 ( 0.00%)      *q++ = (unsigned char)(g->good_players_mask & 0xff);
 1,626 ( 0.00%)      *q++ = (unsigned char)((g->good_players_mask >> 8) & 0xff);
 1,626 ( 0.00%)      *q++ = (unsigned char)((g->good_players_mask >> 16) & 0xff);
 1,626 ( 0.00%)      *q++ = (unsigned char)((g->good_players_mask >> 24) & 0xff);
 1,626 ( 0.00%)      *q++ = (unsigned char)(g->has_good_timestamp ? 1 : 0);
 1,626 ( 0.00%)      *q++ = (unsigned char)(g->deck_count & 0xff);
 2,439 ( 0.00%)      *q++ = (unsigned char)((g->deck_count >> 8) & 0xff);
28,574 ( 0.03%)      for (int i = 0; i < g->deck_count; i++) {
13,484 ( 0.01%)          *q++ = unmasked ? wire_from_card(g->deck[i]) : (unsigned char)WIRE_CARD_HIDDEN;
     .               }
 7,874 ( 0.01%)      *q++ = (unsigned char)g->num_battles;
 7,761 ( 0.01%)      for (int i = 0; i < g->num_battles; i++) {
     .                   const Battle *b = &g->table_battles[i];
 2,454 ( 0.00%)          *q++ = wire_from_card(b->attack);
 2,454 ( 0.00%)          *q++ = wire_from_card(b->defense);
     .               }
15,238 ( 0.02%)      for (int i = 0; i < g->num_players; i++) {
 1,628 ( 0.00%)          const Player *pl = &g->players[i];
     .                   const int visible = hand_visible(viewer, i);
 4,884 ( 0.01%)          *q++ = (unsigned char)pl->status;
 4,890 ( 0.01%)          *q++ = (unsigned char)((visible && pl->awaiting_attack) ? 1 : 0);
 4,884 ( 0.01%)          *q++ = (unsigned char)pl->hand_count;
44,144 ( 0.05%)          for (int j = 0; j < pl->hand_count; j++) {
35,635 ( 0.04%)              *q++ = visible ? wire_from_card(pl->hand[j]) : (unsigned char)WIRE_CARD_HIDDEN;
     .                   }
     .               }
 1,626 ( 0.00%)      *q++ = (unsigned char)g->num_eliminated;
 1,788 ( 0.00%)      for (int i = 0; i < g->num_eliminated; i++) *q++ = (unsigned char)g->elimination_order[i];
 5,718 ( 0.01%)      return (int)(q - out);
     .           }
     .           
     .           // Masked decode: WIRE_CARD_HIDDEN becomes the {0,1} placeholder the browser
     .           // marshal always used for redacted cards; everything else clamps like the
     .           // legacy path.
     .           static Card card_from_wire_masked(unsigned char b) {
     .               if (b == WIRE_CARD_HIDDEN) { Card c; c.suit = 0; c.value = 1; return c; }
     .               return card_from_wire_state(b);
-- line 63 ----------------------------------------
-- line 64 ----------------------------------------
     .           }
     .           
     .           void state_get(Game *g, const unsigned char *p, int masked) {
     .               const unsigned char *q = p;
     .               // No full-struct memset: the Game is ~200 KB (mostly log capacity) and
     .               // every read in the kernel is bounded by the counts set below. Every
     .               // count is clamped to its array capacity — the kernel must never corrupt
     .               // memory on a malformed/corrupt input (see docs/SECURITY_WASM_BOUNDARY.md).
     2 ( 0.00%)      g->status = (int8_t)*q++;
     .               g->num_players = (int8_t)*q++;
     4 ( 0.00%)      if (g->num_players < 0) g->num_players = 0;
     3 ( 0.00%)      if (g->num_players > MAX_PLAYERS) g->num_players = MAX_PLAYERS;
     2 ( 0.00%)      g->power_suit = (int8_t)*q++;
     2 ( 0.00%)      g->first_attacker = (int8_t)*q++;
     2 ( 0.00%)      g->defender = (int8_t)*q++;
     2 ( 0.00%)      g->discard_pile_length = (int16_t)(q[0] | (q[1] << 8)); q += 2;
     2 ( 0.00%)      g->has_flipped = (*q++ != 0);
     .               // When there is no flip (TS flipped === null), preserve the exact {0,0}
     .               // bytes the old 2-byte wire left in g->flipped — semtex-family belief
     .               // code reads it unguarded and {0,0} acts as a harmless never-matches pin.
     .               {
     .                   unsigned char fw = *q++;
     1 ( 0.00%)          if (g->has_flipped) g->flipped = card_from_wire_state(fw);
     .                   else { g->flipped.suit = 0; g->flipped.value = 0; }
     .               }
     2 ( 0.00%)      g->good_players_mask = (uint32_t)q[0] | ((uint32_t)q[1] << 8)
     .                   | ((uint32_t)q[2] << 16) | ((uint32_t)q[3] << 24);
     .               q += 4;
     2 ( 0.00%)      g->has_good_timestamp = (*q++ != 0);
     2 ( 0.00%)      g->deck_count = (int16_t)(q[0] | (q[1] << 8)); q += 2;
     1 ( 0.00%)      if (g->deck_count < 0) g->deck_count = 0;
     5 ( 0.00%)      if (g->deck_count > MAX_DECK) g->deck_count = MAX_DECK;
    25 ( 0.00%)      for (int i = 0; i < g->deck_count; i++) {
     5 ( 0.00%)          unsigned char b = *q++;
    40 ( 0.00%)          g->deck[i] = masked ? card_from_wire_masked(b) : card_from_wire_state(b);
     .               }
     3 ( 0.00%)      g->num_battles = (int8_t)*q++;
     5 ( 0.00%)      if (g->num_battles < 0) g->num_battles = 0;
     3 ( 0.00%)      if (g->num_battles > MAX_BATTLES) g->num_battles = MAX_BATTLES;
     8 ( 0.00%)      for (int i = 0; i < g->num_battles; i++) {
     .                   Battle *b = &g->table_battles[i];
     1 ( 0.00%)          b->attack = card_from_wire_state(*q++);
     1 ( 0.00%)          unsigned char db = *q++;
     3 ( 0.00%)          b->defense = (db == WIRE_CARD_NONE) ? CARD_NONE : card_from_wire_state(db);
     .               }
    37 ( 0.00%)      for (int i = 0; i < g->num_players; i++) {
     3 ( 0.00%)          Player *pl = &g->players[i];
     6 ( 0.00%)          pl->status = (int8_t)*q++;
     6 ( 0.00%)          pl->awaiting_attack = (*q++ != 0);
     3 ( 0.00%)          pl->hand_count = (int8_t)*q++;
    15 ( 0.00%)          if (pl->hand_count < 0) pl->hand_count = 0;
     6 ( 0.00%)          if (pl->hand_count > MAX_HAND_SIZE) pl->hand_count = MAX_HAND_SIZE;
    36 ( 0.00%)          for (int j = 0; j < pl->hand_count; j++) {
     9 ( 0.00%)              unsigned char b = *q++;
    27 ( 0.00%)              pl->hand[j] = masked ? card_from_wire_masked(b) : card_from_wire_state(b);
     .                   }
     .               }
     .               g->num_eliminated = (int8_t)*q++;
     5 ( 0.00%)      if (g->num_eliminated < 0) g->num_eliminated = 0;
     3 ( 0.00%)      if (g->num_eliminated > MAX_PLAYERS) g->num_eliminated = MAX_PLAYERS;
     1 ( 0.00%)      for (int i = 0; i < g->num_eliminated; i++) g->elimination_order[i] = (int8_t)*q++;
     1 ( 0.00%)      g->num_logs = 0;
     .               // The resident game always has a full-size log array; only sampled-world
     .               // slots ever set log_cap (see game.h). Re-pin defensively per marshal.
     1 ( 0.00%)      g->log_cap = 0;
     .               g->log_virt = 0;
     7 ( 0.00%)  }

31,951 ( 0.04%)  <counts for unidentified lines in ../../../c/src/view.c>

--------------------------------------------------------------------------------
-- Auto-annotated source: ../../../c/src/../wasm/wire.h
--------------------------------------------------------------------------------
Ir              

-- line 9 ----------------------------------------
     .           #define CNITRO_WASM_WIRE_H
     .           
     .           #include "card.h"
     .           
     .           #define WIRE_CARD_HIDDEN 0xFEu
     .           #define WIRE_CARD_NONE   0xFFu
     .           
     .           static inline unsigned char wire_from_card(Card c) {
 8,012 ( 0.01%)      if (card_is_none(c)) return (unsigned char)WIRE_CARD_NONE;
51,946 ( 0.06%)      if (c.suit < 0 || c.value < 1) return (unsigned char)WIRE_CARD_HIDDEN;
50,316 ( 0.06%)      return (unsigned char)(c.suit * 13 + (c.value - 1));
     .           }
     .           
     .           // State/move/action cards are always real cards; hostile ids clamp into the
     .           // representable space (the job clamp_card did on the old 2-byte wire — the
     .           // bot bitboards do `1ull << card_id`, undefined for out-of-range ids).
     .           static inline Card card_from_wire_state(unsigned char b) {
 2,566 ( 0.00%)      if (b > 51) b = 51;
 2,538 ( 0.00%)      Card c; c.suit = (int8_t)(b / 13); c.value = (int8_t)(b % 13 + 1);
     .               return c;
     .           }
     .           
     .           // Log pairs carry the sentinels.
     .           static inline Card card_from_wire_pair(unsigned char b) {
     .               if (b == WIRE_CARD_NONE) return CARD_NONE;
     .               if (b == WIRE_CARD_HIDDEN) { Card c; c.suit = -1; c.value = -1; return c; }
     .               return card_from_wire_state(b);
-- line 35 ----------------------------------------

11,726 ( 0.01%)  <counts for unidentified lines in ../../../c/src/../wasm/wire.h>

--------------------------------------------------------------------------------
The following files chosen for auto-annotation could not be found:
--------------------------------------------------------------------------------
  ./elf/../sysdeps/generic/dl-new-hash.h
  ./elf/../sysdeps/x86_64/dl-machine.h
  ./elf/./elf/dl-lookup.c
  ./elf/./elf/dl-reloc.c
  ./elf/./elf/dl-tunables.c
  ./elf/./elf/do-rel.h
  ./io/../sysdeps/unix/sysv/linux/read.c
  ./io/../sysdeps/unix/sysv/linux/write.c
  ./misc/../sysdeps/unix/sysv/linux/epoll_wait.c
  ./nptl/../nptl/descr.h
  ./nptl/./nptl/cancellation.c
  ./nptl/./nptl/pthread_cond_wait.c
  ./nptl/./nptl/pthread_mutex_lock.c
  ./nptl/./nptl/pthread_mutex_unlock.c
  ./string/../sysdeps/x86_64/multiarch/../multiarch/memset-vec-unaligned-erms.S
  ./string/../sysdeps/x86_64/multiarch/../multiarch/strcmp-sse2.S
  ./string/../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S
  ./string/../sysdeps/x86_64/multiarch/memset-vec-unaligned-erms.S

--------------------------------------------------------------------------------
Ir                  
--------------------------------------------------------------------------------
48,640,934 (53.31%)  events annotated

