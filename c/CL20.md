# CL-20 — octogen's successor

CL-20 (HNIW, the rung above octogen's HMX on the explosives ladder) is
octogen's engine forked verbatim plus the levers below. Registered as
`cl20`/`cl` (C-only arena/research bot, like torpex and novichok;
`cl20_oracle`/`clo` is the 6x-worlds audit variant). With every lever off it
is decision-identical to octogen (paired 0/0/N at every player count), so
octogen's validated dominance matrix (OCTOGEN.md, SEMTEX.md) transfers, plus
whatever each lever adds. Same legitimacy contract: public information only,
no LLM, nothing computed outside one `chooseMove` call.

All numbers are paired same-deal deltas vs an **octogen** control
(`cnitro_eval --control=octogen`): Δ = hero mean finish − control mean
finish, negative = cl20 better, `±` = SE of the paired delta, `b/w/e` =
deals cl20 finished better / worse / equal. Seeds are fresh per experiment.

## Where octogen's headroom is (the compute probe)

Before touching the bot, the 6x-worlds oracle (`octogen_oracle`) was paired
against octogen to see where more search still pays (200 pairs/cell, seeds
610001):

| tables | pc2 | pc3 | pc4 | pc6 |
|---|---|---|---|---|
| cordite | −0.025 ± 0.043 | −0.100 ± 0.069 | +0.060 ± 0.090 | **−0.280 ± 0.109** |
| handwritten | — | −0.030 ± 0.049 | −0.075 ± 0.064 | +0.000 ± 0.105 |
| random | — | — | −0.015 ± 0.042 | +0.000 ± 0.081 |

Heads-up and 3–4 players are search-saturated (as SEMTEX.md found); the
6-player MC mirror is still **compute-limited**. 2x worlds at pc6 recovers
about half of it (−0.130 ± 0.117), wider candidate survival alone none
(+0.035 ± 0.109) — the binding resource is worlds, not breadth.

## Measurement infrastructure added

- `cnitro_elo` now schedules every game up front and plays them in parallel
  (`make OMP=1`), then applies the sequential K=32 Elo in schedule order
  (bit-identical trajectory to the old serial loop), and finally fits an
  order-free **Bradley-Terry maximum-likelihood rating** over the same
  pairwise seat outcomes with a game-level bootstrap (`--bootstrap=N`,
  default 400): per-bot 95% intervals, the top-two gap with its interval and
  P(gap > 0), and the direct head-to-head records. SEMTEX.md showed the
  sequential arena moves ratings by >100 points on game order alone; the MLE
  is the instrument that can call a ladder position definitive.

## The nulls (measured, so nobody retreads them)

Every lever below is implemented, sound, and default-off. Cells are 200-300
pairs unless noted; Δ vs the octogen control.

1. **Policy-consistency belief constraints** (`CL_HWC`). Every decision by a
   handwritten-family seat is a deterministic function of its hand, so each
   observed move is an exact constraint on that hand: a trump first-attack
   means the whole hand is trump; a cover with Q♠ over 7♠ means no 8♠–J♠; a
   GOOD with defender capacity left means no non-trump card of any table
   value; a throw-in short of capacity means those were its only matching
   cards; a pickup facing several attacks means its greedy full cover fails.
   Constraints persist across draws as count constraints ("at most d of
   these, d = cards drawn since") and drop on any contradiction (which also
   marks the seat non-handwritten). An oracle audit (`CL_VERIFY`, reads the
   true hands only to check) found 0 violations in 3,909 constraints against
   handwritten seats. Effect: null at every player count — handwritten
   tables pc2 −0.007 ± 0.017, pc3 −0.023 ± 0.040, pc4 −0.017 ± 0.047, pc6
   +0.067 ± 0.075, pc7 −0.013 ± 0.135, pc8 −0.093 ± 0.110; cordite tables
   pc7 +0.213 ± 0.150, pc8 −0.053 ± 0.153 (pooled ≈ −0.01 ± 0.015). This
   agrees with NOVICHOK.md: even the *complete* hand cheat is worth only
   ~0.09 at pc3 handwritten, so partial hand information has a low ceiling.
2. **Adaptive final duel** (`CL_ADAPT_K`): extend the top-2 stage while the
   paired same-world difference is inside 2 SE. Null everywhere (cordite
   pc2 −0.010, pc3 +0.020, pc4 −0.060 ± 0.057, pc6 −0.010; handwritten
   pc3/4/6 within ±0.05). Extending from stage 1 at pc6: −0.145 ± 0.122.
3. **Race deliberation** (`CL_RACE`): statistical pruning of candidates on
   paired deficits, world cap 3x. cordite pc6 −0.055 ± 0.135, pc2
   −0.065 ± 0.043; handwritten pc4 **+0.095 ± 0.062**. Unreliable; off.
4. **Self-search rollouts** (`CL_SELF`): my first post-root decision inside
   the playout chosen by search over my legal moves. cordite pc2/3/4 null;
   handwritten pc4 **+0.170 ± 0.072** — the in-world best reply exploits
   sampled hidden cards it could not know (strategy fusion), the same
   failure as octogen's opponent reply tournament.
5. **Self-seat MC-defender model** (`CL_SELFPOL=2`): cordite pc4
   +0.150 ± 0.087. Off.
6. **Espresso rollouts at heads-up only** (`CL_ROLLOUT=1`, struct path):
   cordite pc2 +0.060 ± 0.046. Off.
7. **Soft voids for strategic seats** (`CL_MCVOID`): keep an MC seat's
   voids as "no cheap same-suit cover" instead of dropping them. cordite pc3
   +0.070 ± 0.038. Off.
8. **Wider candidate survival at pc6** (`CL_KEEP1=6 CL_KEEP2=3`):
   +0.035 ± 0.109. Worlds, not breadth, are the pc6 resource.
9. **Live-deck exact evaluation** (`CL_XDECK`): heads-up with ≤ 2–4 deck
   cards, value every (world × candidate) by an exact sign-only solve of the
   rest of the game in that world (the solver now supports a fixed deck
   order: deterministic draws, full child clones, deck_n + per-world salt in
   the transposition key). Infeasible: at the 14–16 cards a heads-up
   pre-endgame holds, >90% of solves abort at 100k nodes (depth and budget)
   and a decision costs ~10x; even deck-empty 10-card leaf solves at 40k
   nodes are ~8x slower per game. Kept as research plumbing.
10. **10x exact-solve budget, 36-card window** (`CL_BB_WIN=4000000
    CL_BB_AVOID=2500000 CL_SOLVE_CARDS=36`): more exact truth at heads-up,
    octogen's own hunt-4 lever pushed further. Mirror −0.010 ± 0.023
    (25/22), cordite tables −0.030 ± 0.026 (35/26), at ~3x decision cost.
    The 28-card window already captures what the solver can resolve.
11. **Violation-count profiling** (`CL_PROFILE=2`): counting proven
    handwritten-consistency violations as weak-seat evidence flags cordite
    seats 48% and robusta 43% of the time vs random 29% — MC bots deviate
    from handwritten *more* than random does. Off.

## The shipped levers

Everything below is on by default in `cl20`; each has a knob to turn it off.

### 1. Weak-seat profiling (`CL_PROFILE=1`, off in octogen)

Octogen carries semtex's fulminate-style weak-seat profiler (`OG_PROFILE`)
but ships with it off. It marks a seat "loose" on a deck-alive trump-burn
share ≥ 40–60% over ≥ 14 observed cards, or ≥ 3 deck-alive trump leads, and
rolls that seat out with the LOOSE (random-ish) model instead of
handwritten. Audited on 40-game samples per opponent, it flags **0.0%** of
handwritten seat-decisions at pc4 and pc6, 0.0% of espresso and robusta,
3% of cordite, and it is decision-identical to octogen at handwritten
tables (0/0/200 at pc3/4/6) and near-identical at cordite tables
(9/3/188, 2/1/197, 0/0/200). Against random seats — one of the eleven
arena bots — it pays every time it is measured:

| random tables | pc3 | pc4 | pc6 |
|---|---|---|---|
| seeds 650001, 200 pairs | −0.040 ± 0.032 | −0.060 ± 0.042 | −0.095 ± 0.067 |
| seeds 740001, 200 pairs (all levers) | +0.005 ± 0.021 | +0.005 ± 0.032 | −0.020 ± 0.069 |
| seeds 750001, **600 pairs**, isolated | — | **−0.020 ± 0.012** | **−0.035 ± 0.017** |

Small (it only fires on ~2% of random seat-decisions; random seats almost
never survive 14 deck-alive cards), but never negative and never a false
positive on a strong seat.

### 2. A measured model of strategic seats (`CL_MCPOL=1`)

Octogen's rollouts model every opponent as handwritten, including
opponents it has *proven* strategic (mc_tell: a pickup while holding cover,
a declined legal throw-in). `tests/mc_behavior.c` (`make build/mc_behavior`)
measures what the MC family actually does at those decisions, deck alive,
in self-play:

| bot | picks up holding a full non-trump cover | …cover needs a trump | declines a legal throw-in | first attack ≠ handwritten's |
|---|---|---|---|---|
| octogen | 15% | 31% | 32% | 54% |
| cordite | 15% | 23% | 36% | 64% |
| semtex | 22% | 32% | 38% | 63% |
| blackpowder | 23% | 26% | 39% | 89% |
| handwritten model | 0% | 0% | 0% | 0% |

`CD_POL_MC` (cordite_sim.c) is a stochastic mimic with those parameters;
cl20 rolls proven-strategic seats out with it. The ablation (heads-up,
octogen tables, seeds 720001, 300 pairs each) found the pickup component
carries the effect: pickups at the fitted 30%/15% −0.080 ± 0.023 (37/13),
at 50%/25% −0.043, the higher-lead component alone −0.040, octogen's own
MCDEF −0.033, and adding the declined-throw-in or declined-transfer
components on top of pickups did not improve on pickups alone (−0.057,
−0.053, −0.037). So the shipped model is **pickups only, 30% when the
greedy cover needs a trump, 15% otherwise** (`CL_MC_PICKT/PICKN`); the
other components stay as knobs (`CL_MC_FIRST`, `CL_MC_GOOD`,
`CL_MC_NOPASS`, all 0).

Its honest size, across every heads-up mirror cell run (octogen tables):
−0.030 (700001, 300, MCDEF), 0.000 (710001, 200, full fit), −0.080 and
−0.090 (720001, 300), −0.010 (740001, 200), −0.015 ± 0.015 (760001,
**600**): about −0.02 pooled, plus pc3 −0.095 ± 0.056 (730001, 200) and pc4
null. Against cordite tables it is a wash (pc2 +0.010, −0.010,
+0.015 ± 0.015 over 600; pc3 +0.010 ± 0.033 over 600); against blackpowder
tables −0.040 ± 0.025 (pc2) and −0.030 ± 0.067 (pc4). Never significantly
negative anywhere, so it ships as the more faithful model.

### 3. 3x worlds at 6+ players (`CL_WMUL6=3`)

The one place the compute probe kept pointing: 6-player tables. Cordite
tables pc6 (seeds 610001, 200 pairs): 2x −0.130 ± 0.117, 3x −0.085 ± 0.110,
4x −0.085 ± 0.107, 6x −0.280 ± 0.109; seeds 740001 +0.020 ± 0.133; seeds
750001 **800 pairs** −0.019 ± 0.065. Handwritten tables pc6: seeds 740001
(200) +0.145 ± 0.091; seeds 750001 (**800**) **−0.138 ± 0.046** (174/133).
Pooled, roughly −0.05 ± 0.03 at pc6; pc5 and pc8 are null even at 6x
(−0.015 ± 0.099, −0.027 ± 0.161). It costs 3x per decision at 6–8 players,
which the arena does not charge for; a production deployment would want
its own wall-clock decision (octogen's `prod` budget already sits at the
saturation knee for 2–4 players, and this changes nothing there).

## Confirmation matrix (all levers, fresh seeds 740001, 200 pairs/cell)

Δ = cl20 mean finish − octogen mean finish, paired same deals, negative =
cl20 better. Cells where the levers never fire are exactly identical
(0/0/200).

| tables | pc2 | pc3 | pc4 | pc6 |
|---|---|---|---|---|
| octogen | −0.010 ± 0.027 | +0.065 ± 0.065 | −0.005 ± 0.047 | +0.060 ± 0.130 |
| cordite | −0.040 ± 0.032 | +0.050 ± 0.063 | +0.050 ± 0.049 | +0.020 ± 0.133 |
| handwritten | 0 (identical) | 0 (identical) | 0 (identical) | +0.145 ± 0.091 |
| random | 0 (identical) | +0.005 ± 0.021 | +0.005 ± 0.032 | −0.020 ± 0.069 |
| espresso | −0.015 ± 0.011 | 0 (identical) | 0 (identical) | +0.145 ± 0.093 |
| blackpowder | +0.015 ± 0.027 | −0.075 ± 0.060 | +0.050 ± 0.044 | −0.200 ± 0.144 |

No cell is significantly different from zero; the matrix says cl20 is
never worse than octogen by a detectable margin against any field.

## The arena (the number the question was about)

`cnitro_elo`, the full eleven-bot pool (random, handwritten, espresso,
robusta, firecracker, gunpowder, blackpowder, cordite, semtex, octogen,
cl20), player counts 2–8 uniform, 20,000 games per seed, Bradley-Terry MLE
with a 400-replicate game-level bootstrap. Each seed is ~9,200 games per
bot. Results per seed (`bench_results/cl20/arena_20000_seed*.txt`):

| seed | cl20 | octogen | MLE rank | gap cl20−octogen (95% CI) | direct cl20 vs octogen |
|---|---|---|---|---|---|
| 1 | 1091.4 | 1087.5 | 1 / 2 | +4.0 (−4.4 .. +11.3) | 2094–1928 (52.1%) |
| 2 | 1088.4 | 1083.2 | 1 / 2 | +5.2 (−2.3 .. +12.1) | 2131–2076 (50.7%) |
| 3 | 1084.7 | 1086.8 | 2 / 1 | −2.1 (−8.8 .. +4.6) | 2016–2039 (49.7%) |
| 4 | 1089.7 | 1088.0 | 1 / 2 | +1.7 (−6.5 .. +9.3) | 1863–1911 (49.4%) |

ARENA_POOLED_PLACEHOLDER

## Methodology lesson: 200-pair cells cannot see these effects

Every lever in this document is worth a few hundredths of a finish
position. The paired harness's SE at 200 pairs is 0.02–0.03 heads-up and
0.09–0.14 at six players, so a 200-pair cell resolves nothing smaller than
~0.05 heads-up and ~0.25 at pc6 — and the same cell re-run on a fresh seed
set routinely moved by 2σ (the pc6 handwritten 3x cell: +0.145 ± 0.091 on
740001, −0.138 ± 0.046 on 750001, with the 200-pair number reproducing
bit-exactly under the isolated configuration, so it is deal variance, not
configuration). Several earlier ladder claims in this repository rest on
200–400-pair cells and are 1–2σ results by the same arithmetic. The
decisions above were therefore taken on 600–1,000-pair isolated cells,
and the arena result below on tens of thousands of games with a bootstrap
interval, never on a single screening cell.

## Knobs (`CL_*`, cl20 only; env overrides the C default)

Shipped: `CL_PROFILE` (1), `CL_MCPOL` (1) with `CL_MC_PICKT` (30) /
`CL_MC_PICKN` (15) / `CL_MC_FIRST` (0) / `CL_MC_GOOD` (0) / `CL_MC_NOPASS`
(0), `CL_WMUL6` (3). Research (off): `CL_HWC` (+ `CL_HWC_MOD`,
`CL_HWC_TRIES`, `CL_HWC_STATS`, `CL_VERIFY`), `CL_ADAPT_K` / `CL_ADAPT_STAGE`
/ `CL_ADAPT_C`, `CL_RACE` (+ `CL_RACE_MULT/C/CF/BLOCK/MINB`), `CL_SELF` (+
`CL_SELF_CAP`, `CL_SELF_STAGE`), `CL_SELFPOL`, `CL_MCVOID`, `CL_XDECK` (+
`CL_XDECK_CARDS/TOTAL/BUDGET/WORLDS/STATS`), `CL_PROFILE=2` (+
`CL_PROFILE_V0/VW`, `CL_PROFILE_STATS`). Octogen's ablation knobs are
inherited under the `CL_` prefix (`CL_W1/2/3`, `CL_KEEP1/2`, `CL_NO_SOLVE`,
…). With every shipped knob at 0/1 cl20 is decision-identical to octogen.

