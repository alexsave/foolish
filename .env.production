# Client-exposed env vars must use the `NEXT_PUBLIC_` prefix in Next.js.
# Supabase URL/key are provided via NEXT_PUBLIC_SUPABASE_URL / NEXT_PUBLIC_SUPABASE_KEY
# (set in the deployment environment). Web analytics are handled by the
# @vercel/analytics <Analytics /> component and need no env var.
