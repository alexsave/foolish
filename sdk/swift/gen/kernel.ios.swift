// GENERATED by tools/structgen - do not edit.
// build: ios; roots: TableView, PushEvent, ViewRules, AnimPlan, AnimFrame, AnimBeats, AnimSurfacePlan, BotDriveOut, Roster, MsgHeader
// target: arm64-apple-ios15.0

/// The layout THESE readers were generated for. A host compares it with the
/// hash the library it links was stamped with (-DSG_LAYOUT_HASH), so a stale
/// binding or a stale library is a refusal at startup and never a wrong offset.
public let SG_LAYOUT_HASH: UInt32 = 0x73a27f61

/// Why a generated reader or writer refused. Every one of them names the field
/// and the value: a payload that does not read whole is not read at all.
public enum SGLayoutError: Error, Equatable, Sendable {
    /// A count outside 0...capacity - a struct that does not describe itself.
    /// Behind a pointer there is no capacity to check, and the capacity is Int.max.
    case count(field: String, got: Int, capacity: Int)
    /// A NULL pointer with a count, so there is nothing to copy the count from.
    case null(field: String, count: Int)
    /// A value with more elements or bytes than the field holds.
    case tooLong(field: String, got: Int, capacity: Int)
}

@inline(__always) private func sgUTF8(_ p: UnsafeRawPointer, _ o: Int, _ n: Int) -> String {
    String(decoding: UnsafeRawBufferPointer(start: p + o, count: n), as: UTF8.self)
}

// constants
public let CLIENT_OK = 0
public let CLIENT_E_FORMAT = -201
public let CLIENT_E_TRAILER = -202
public let CLIENT_E_STATE = -203
public let CLIENT_E_MISMATCH = -204
public let CLIENT_E_PUSH = -205
public let CLIENT_E_IDENTITY = -206
public let CLIENT_E_ORDER = -207
public let CLIENT_E_CAP = -208
public let CLIENT_E_MOVE = -209
public let CLIENT_EDIT_KEEP = 1
public let CLIENT_EDIT_TURN = 2
public let CLIENT_EDIT_TABLE = 3
public let CLIENT_EDIT_LIFT = 4
public let CLIENT_EDIT_RETURN = 5
public let CLIENT_EDIT_LOBBY = 6
public let CLIENT_EDIT_WITHDRAW = 7
public let CLIENT_EDIT_UNDEAL = 8
public let CLIENT_CONFLICT_MAX_EVENTS = 128
public let CLIENT_CONFLICT_MAX_MOTIONS = 64
public let ANIM_EVT_MAGIC_TRANSITION = 0
public let ANIM_EVT_DEAL = 1
public let ANIM_EVT_FLIPPED = 2
public let ANIM_EVT_DEFENDER_MOVE = 3
public let ANIM_EVT_ATTACK_PASS = 4
public let ANIM_EVT_COVER = 5
public let ANIM_EVT_PICKUP = 6
public let ANIM_EVT_DISCARD = 7
public let ANIM_EVT_OUT = 8
public let ANIM_EVT_REFILL = 9
public let ANIM_EVT_CARDS_TO_TRASH = 10
public let ANIM_EVT_REVERT = 11
public let ANIM_LOC_DECK = 0
public let ANIM_LOC_HAND = 1
public let ANIM_LOC_TABLE = 2
public let ANIM_LOC_DISCARD = 3
public let ANIM_LOC_FLIPPED = 4
public let ANIM_LOC_NONE = 255
public let ANIM_DEST_TABLE = 0
public let ANIM_DEST_MY_HAND = 1
public let ANIM_DEST_POOL = 2
public let ANIM_CONFLICT_REVERT = 0
public let ANIM_CONFLICT_KEEP = 1
public let ANIM_CONFLICT_CLEAR = 2
public let ANIM_SEAT_NONE = -1
public let ANIM_STEP_NONE = -1
public let ANIM_NEVER = -1
public let ANIM_TIME_MS = 500
public let ANIM_GAP_MS = 25
public let ANIM_NO_MASK = -1
public let ANIM_BEAT_HOLDS = 1
public let ANIM_BEAT_MOVED = 2
public let ANIM_BEAT_PLACED = 4
public let ANIM_BEAT_DROPS = 8
public let ANIM_SURFACE_MAX_BEATS = 10
public let ANIM_SURFACE_ROSTER = 1
public let ANIM_SURFACE_RULES = 2
public let ANIM_SURFACE_BOARD = 3
public let ANIM_SURFACE_LOBBY = 4
public let ANIM_SURFACE_CONTROLS_LIVE = 0
public let ANIM_SURFACE_CONTROLS_HELD = 1
public let ANIM_SURFACE_HOLD_MS = 500
public let ANIM_TABLE_NONE = 254
public let BOT_STOP_NO_ELIGIBLE = 0
public let BOT_STOP_ENDED = 1
public let BOT_STOP_EVENTS = 2
public let BOT_STOP_MAX = 3
public let BOT_PACE_NONE = 0
public let BOT_PACE_BUNDLED_PASSIVE = 1
public let BOT_PACE_MOVE = 2
public let BOT_PACE_ROUND_TRANSITION = 3
public let MOVE_ATTACK = 0
public let MOVE_COVER = 1
public let MOVE_PASS = 2
public let MOVE_PICKUP = 3
public let MOVE_GOOD = 4
public let MOVE_WAIT = 5
public let GAME_STATUS_WAITING = 0
public let GAME_STATUS_PLAYING = 1
public let GAME_STATUS_GAME_OVER = 2
public let PLAYER_STATUS_IDLE = 0
public let PLAYER_STATUS_READY = 1
public let PLAYER_STATUS_IN = 2
public let PLAYER_STATUS_OUT = 3
public let ROSTER_E_LENGTH = -1
public let ROSTER_E_VERSION = -2
public let ROSTER_E_COUNT = -3
public let ROSTER_E_ID = -4
public let ROSTER_E_NAME = -5
public let ROSTER_E_BRAIN = -6
public let ROSTER_E_TITLE = -7
public let ROSTER_E_DUPLICATE = -8
public let ROSTER_E_FULL = -9
public let ROSTER_E_SEAT = -10
public let ROSTER_E_PERM = -11
public let ROSTER_E_CAP = -12
public let ROSTER_E_PADDING = -13
public let ROSTER_E_STATUS = -14
public let ROSTER_E_GAME_ID = -15
public let ROSTER_E_SHORT = -16
public let ROSTER_E_GOOD = -17
public let ROSTER_E_FLAG = -18
public let MSG_MAGIC = 247
public let MSG_FORMAT_V6 = 2
public let MSG_FORMAT_CLOCK = 3
public let MSG_FORMAT_REMATCH = 4
public let MSG_PICKUP_HOLD_S = 15
public let MSG_FORMAT_RULES = 5
public let MSG_FORMAT_RULES_REMATCH = 6
public let MSG_VARIANT_PASS = 1
public let MSG_VARIANT_KNOWN = 1
public let MSG_PHASE_WAITING = 0
public let MSG_PHASE_ACCEPT = 1
public let MSG_PHASE_LIVE = 2
public let MSG_PHASE_FINISHED = 3
public let MSG_FLAG_FAIR_DEAL = 1
public let MSG_FLAG_GZIP = 2
public let MSG_MAX_NAME = 64
public let MSG_MAX_NAME_CHARS = 16
public let MSG_MAX_JOINS = 8
public let MSG_SEED_LEN = 32
public let MSG_PARENT_LEN = 8
public let MSG_HEADER_LEN = 59
public let MSG_CLOCK_OFF = 58
public let MSG_NEW_OFF = 60
public let MSG_HEADER_LEN_CLOCK = 62
public let MSG_OPEN_OFF = 62
public let MSG_CARRY_OFF = 63
public let MSG_FOOL_OFF = 67
public let MSG_HEADER_LEN_REMATCH = 69
public let MSG_NO_OPENING = 255
public let MSG_NO_FOOL = 255
public let MSG_MAX_NEW = 254
public let MSG_NEW_NOTHING = 255
public let MSG_NO_BASE = -1
public let MSG_BASE_NOTHING = -2
public let MSG_MAX_ACTIONS = 1024
public let MSG_MAX_ACTION_BYTES = 4096
public let MSG_EOK = 0
public let MSG_ESHORT = -1
public let MSG_EMAGIC = -2
public let MSG_EFORMAT = -3
public let MSG_EFLAGS = -4
public let MSG_EPHASE = -5
public let MSG_EPLAYERS = -6
public let MSG_EVARIANT = -7
public let MSG_ESEAT = -8
public let MSG_ENAME = -9
public let MSG_ESEED = -10
public let MSG_EACTION = -11
public let MSG_ETURN = -12
public let MSG_EROUND = -13
public let MSG_ECAP = -14
public let MSG_ETRAIL = -15
public let MSG_ECHAIN = -16
public let MSG_EJOINS = -17
public let MSG_EBODY = -18
public let MSG_REBASE_REAPPLY = 0
public let MSG_REBASE_DISCARD_ROUND = 1
public let MSG_REBASE_DISCARD_ILLEGAL = 2
public let MSG_NAME_OK = 0
public let MSG_NAME_EMPTY = 1
public let MSG_NAME_TOO_LONG = 2
public let MSG_LOBBY_START = 1
public let MSG_LOBBY_INVITE = 2
public let MSG_LOBBY_WAITING = 3
public let MSG_LOBBY_JOIN = 4
public let MSG_LOBBY_FULL = 5
public let MSG_TURN_STAGED = 1
public let MSG_TURN_SENDING = 2
public let MSG_TURN_READY = 4
public let MSG_TURN_SUPERSEDED = 8
public let MSG_TURN_RETRACTING = 16
public let MSG_TURN_BOARD_WATCHING = 32
public let MSG_TURN_HELD = 64
public let MSG_TURN_GENESIS = 128
public let MSG_TURN_CANCEL_NOOP = 0
public let MSG_TURN_CANCEL_RESTAGE = 1
public let MSG_TURN_CANCEL_CLEAR = 2
public let MSG_TURN_ADMIT_OK = 0
public let MSG_TURN_ADMIT_RETRACTING = 1
public let MSG_TURN_ADMIT_SUPERSEDED = 2
public let MSG_TURN_ADMIT_HELD_PICKUP = 3
public let MSG_TURN_ARRIVE_SKIP = 0
public let MSG_TURN_ARRIVE_LATCH = 1
public let MSG_TURN_ARRIVE_ADOPT = 2
public let MSG_TURN_ARRIVE_RETRACT = 3
public let MSG_TURN_BYTES_NONE = 0
public let MSG_TURN_BYTES_HOST = 1
public let MSG_TURN_BYTES_SEALED = 2
public let MSG_TURN_SEND_FOREIGN = 0
public let MSG_TURN_SEND_NOOP = 1
public let MSG_TURN_SEND_BLIND = 2
public let MSG_TURN_SEND_DECODE = 3
public let MSG_TURN_SEND_UNREADABLE = 4
public let MSG_TURN_SEND_REBASE = 5
public let MSG_TURN_SEND_OTHERGAME = 6

/// TableView (c struct), copied out of the kernel's storage.
public struct TableViewSnap: Sendable, Equatable {
    /// sizeof(TableView) under this build: what a host allocates to hand C one.
    public static let cSize = 1360
    public let status: Int
    public let powerSuit: Int
    public let firstAttacker: Int
    public let defender: Int
    public let mySeat: Int
    public let fool: Int
    public let deckCount: Int
    public let discardPileLength: Int
    public let hasFlipped: Bool
    public let hasGoodTimestamp: Bool
    public let flipped: CardSnap
    public let goodMask: Int
    public let version: Int
    public let battles: [BattleSnap]
    public let seats: [ViewSeatSnap]
    public let myHand: [CardSnap]
    public let elimination: [Int]
    public let gameId: String
    public let title: String
    public init(status: Int, powerSuit: Int, firstAttacker: Int, defender: Int, mySeat: Int, fool: Int, deckCount: Int, discardPileLength: Int, hasFlipped: Bool, hasGoodTimestamp: Bool, flipped: CardSnap, goodMask: Int, version: Int, battles: [BattleSnap], seats: [ViewSeatSnap], myHand: [CardSnap], elimination: [Int], gameId: String, title: String) {
        self.status = status
        self.powerSuit = powerSuit
        self.firstAttacker = firstAttacker
        self.defender = defender
        self.mySeat = mySeat
        self.fool = fool
        self.deckCount = deckCount
        self.discardPileLength = discardPileLength
        self.hasFlipped = hasFlipped
        self.hasGoodTimestamp = hasGoodTimestamp
        self.flipped = flipped
        self.goodMask = goodMask
        self.version = version
        self.battles = battles
        self.seats = seats
        self.myHand = myHand
        self.elimination = elimination
        self.gameId = gameId
        self.title = title
    }
}
public func readTableView(_ p: UnsafeRawPointer) throws -> TableViewSnap {
    let n_battles = Int(p.load(fromByteOffset: 5, as: Int8.self))
    if n_battles < 0 || n_battles > 64 { throw SGLayoutError.count(field: "TableView.battles", got: n_battles, capacity: 64) }
    let n_seats = Int(p.load(fromByteOffset: 1, as: Int8.self))
    if n_seats < 0 || n_seats > 8 { throw SGLayoutError.count(field: "TableView.seats", got: n_seats, capacity: 8) }
    let n_my_hand = Int(p.load(fromByteOffset: 8, as: Int8.self))
    if n_my_hand < 0 || n_my_hand > 64 { throw SGLayoutError.count(field: "TableView.my_hand", got: n_my_hand, capacity: 64) }
    let n_elimination = Int(p.load(fromByteOffset: 6, as: Int8.self))
    if n_elimination < 0 || n_elimination > 8 { throw SGLayoutError.count(field: "TableView.elimination", got: n_elimination, capacity: 8) }
    let n_game_id = Int(p.load(fromByteOffset: 17, as: UInt8.self))
    if n_game_id < 0 || n_game_id > 65 { throw SGLayoutError.count(field: "TableView.game_id", got: n_game_id, capacity: 65) }
    let n_title = Int(p.load(fromByteOffset: 18, as: UInt8.self))
    if n_title < 0 || n_title > 201 { throw SGLayoutError.count(field: "TableView.title", got: n_title, capacity: 201) }
    var battles: [BattleSnap] = []
    battles.reserveCapacity(n_battles)
    for i in 0..<n_battles { battles.append(try readBattle(p + 28 + i * 2)) }
    var seats: [ViewSeatSnap] = []
    seats.reserveCapacity(n_seats)
    for i in 0..<n_seats { seats.append(try readViewSeat(p + 156 + i * 108)) }
    var myHand: [CardSnap] = []
    myHand.reserveCapacity(n_my_hand)
    for i in 0..<n_my_hand { myHand.append(try readCard(p + 1020 + i)) }
    var elimination: [Int] = []
    elimination.reserveCapacity(n_elimination)
    for i in 0..<n_elimination { elimination.append(Int(p.load(fromByteOffset: 1084 + i, as: Int8.self))) }
    return TableViewSnap(status: Int(p.load(fromByteOffset: 0, as: Int8.self)), powerSuit: Int(p.load(fromByteOffset: 2, as: Int8.self)), firstAttacker: Int(p.load(fromByteOffset: 3, as: Int8.self)), defender: Int(p.load(fromByteOffset: 4, as: Int8.self)), mySeat: Int(p.load(fromByteOffset: 7, as: Int8.self)), fool: Int(p.load(fromByteOffset: 9, as: Int8.self)), deckCount: Int(p.load(fromByteOffset: 10, as: Int16.self)), discardPileLength: Int(p.load(fromByteOffset: 12, as: Int16.self)), hasFlipped: p.load(fromByteOffset: 14, as: UInt8.self) != 0, hasGoodTimestamp: p.load(fromByteOffset: 15, as: UInt8.self) != 0, flipped: try readCard(p + 16), goodMask: Int(p.load(fromByteOffset: 20, as: UInt32.self)), version: Int(p.load(fromByteOffset: 24, as: UInt32.self)), battles: battles, seats: seats, myHand: myHand, elimination: elimination, gameId: sgUTF8(p, 1092, n_game_id), title: sgUTF8(p, 1157, n_title))
}

/// PushEvent (c struct), copied out of the kernel's storage.
public struct PushEventSnap: Sendable, Equatable {
    /// sizeof(PushEvent) under this build: what a host allocates to hand C one.
    public static let cSize = 73
    public let type: Int
    public let seat: Int
    public let msg: Int
    public let from: Int
    public let to: Int
    public let battle: Int
    public let hasTarget: Bool
    public let target: CardSnap
    public let cards: [CardSnap]
    public init(type: Int, seat: Int, msg: Int, from: Int, to: Int, battle: Int, hasTarget: Bool, target: CardSnap, cards: [CardSnap]) {
        self.type = type
        self.seat = seat
        self.msg = msg
        self.from = from
        self.to = to
        self.battle = battle
        self.hasTarget = hasTarget
        self.target = target
        self.cards = cards
    }
}
public func readPushEvent(_ p: UnsafeRawPointer) throws -> PushEventSnap {
    let n_cards = Int(p.load(fromByteOffset: 6, as: UInt8.self))
    if n_cards < 0 || n_cards > 64 { throw SGLayoutError.count(field: "PushEvent.cards", got: n_cards, capacity: 64) }
    var cards: [CardSnap] = []
    cards.reserveCapacity(n_cards)
    for i in 0..<n_cards { cards.append(try readCard(p + 9 + i)) }
    return PushEventSnap(type: Int(p.load(fromByteOffset: 0, as: Int8.self)), seat: Int(p.load(fromByteOffset: 1, as: Int8.self)), msg: Int(p.load(fromByteOffset: 2, as: Int8.self)), from: Int(p.load(fromByteOffset: 3, as: Int8.self)), to: Int(p.load(fromByteOffset: 4, as: Int8.self)), battle: Int(p.load(fromByteOffset: 5, as: Int8.self)), hasTarget: p.load(fromByteOffset: 7, as: UInt8.self) != 0, target: try readCard(p + 8), cards: cards)
}

/// ViewRules (c struct), copied out of the kernel's storage.
public struct ViewRulesSnap: Sendable, Equatable {
    /// sizeof(ViewRules) under this build: what a host allocates to hand C one.
    public static let cSize = 12
    public let firstAttackerBadge: Int
    public let defenderBadge: Int
    public let canSayGood: Bool
    public let showDeckPile: Bool
    public let showFlippedSlot: Bool
    public let showTrumpIcon: Bool
    public let botToMove: Bool
    public let deckPile: Int
    public let deckBadge: Int
    public init(firstAttackerBadge: Int, defenderBadge: Int, canSayGood: Bool, showDeckPile: Bool, showFlippedSlot: Bool, showTrumpIcon: Bool, botToMove: Bool, deckPile: Int, deckBadge: Int) {
        self.firstAttackerBadge = firstAttackerBadge
        self.defenderBadge = defenderBadge
        self.canSayGood = canSayGood
        self.showDeckPile = showDeckPile
        self.showFlippedSlot = showFlippedSlot
        self.showTrumpIcon = showTrumpIcon
        self.botToMove = botToMove
        self.deckPile = deckPile
        self.deckBadge = deckBadge
    }
}
public func readViewRules(_ p: UnsafeRawPointer) throws -> ViewRulesSnap {
    return ViewRulesSnap(firstAttackerBadge: Int(p.load(fromByteOffset: 0, as: Int8.self)), defenderBadge: Int(p.load(fromByteOffset: 1, as: Int8.self)), canSayGood: p.load(fromByteOffset: 2, as: UInt8.self) != 0, showDeckPile: p.load(fromByteOffset: 3, as: UInt8.self) != 0, showFlippedSlot: p.load(fromByteOffset: 4, as: UInt8.self) != 0, showTrumpIcon: p.load(fromByteOffset: 5, as: UInt8.self) != 0, botToMove: p.load(fromByteOffset: 6, as: UInt8.self) != 0, deckPile: Int(p.load(fromByteOffset: 8, as: Int16.self)), deckBadge: Int(p.load(fromByteOffset: 10, as: Int16.self)))
}

/// AnimPlan (c struct), copied out of the kernel's storage.
public struct AnimPlanSnap: Sendable, Equatable {
    /// sizeof(AnimPlan) under this build: what a host allocates to hand C one.
    public static let cSize = 11752
    public let steps: [AnimPlanStepSnap]
    public let pre: AnimCountsSnap
    public let totalMs: Int
    public let veilIds: [Int]
    public init(steps: [AnimPlanStepSnap], pre: AnimCountsSnap, totalMs: Int, veilIds: [Int]) {
        self.steps = steps
        self.pre = pre
        self.totalMs = totalMs
        self.veilIds = veilIds
    }
}
public func readAnimPlan(_ p: UnsafeRawPointer) throws -> AnimPlanSnap {
    let n_steps = Int(p.load(fromByteOffset: 0, as: Int32.self))
    if n_steps < 0 || n_steps > 128 { throw SGLayoutError.count(field: "AnimPlan.steps", got: n_steps, capacity: 128) }
    let n_veil_ids = Int(p.load(fromByteOffset: 11588, as: Int32.self))
    if n_veil_ids < 0 || n_veil_ids > 160 { throw SGLayoutError.count(field: "AnimPlan.veil_ids", got: n_veil_ids, capacity: 160) }
    var steps: [AnimPlanStepSnap] = []
    steps.reserveCapacity(n_steps)
    for i in 0..<n_steps { steps.append(try readAnimPlanStep(p + 8 + i * 88)) }
    var veilIds: [Int] = []
    veilIds.reserveCapacity(n_veil_ids)
    for i in 0..<n_veil_ids { veilIds.append(Int(p.load(fromByteOffset: 11592 + i, as: UInt8.self))) }
    return AnimPlanSnap(steps: steps, pre: try readAnimCounts(p + 11272), totalMs: Int(p.load(fromByteOffset: 11584, as: Int32.self)), veilIds: veilIds)
}

/// AnimFrame (c struct), copied out of the kernel's storage.
public struct AnimFrameSnap: Sendable, Equatable {
    /// sizeof(AnimFrame) under this build: what a host allocates to hand C one.
    public static let cSize = 88
    public let step: Int
    public let elapsedMs: Int
    public let landed: Int
    public let nextMs: Int
    public let done: Int
    public let deck: Int
    public let discard: Int
    public let hand: [Int]
    public let flipped: CardSnap
    public let inFlightFromDeck: Int
    public let inFlightToFlipped: Int
    public let veiled: UInt64
    public init(step: Int, elapsedMs: Int, landed: Int, nextMs: Int, done: Int, deck: Int, discard: Int, hand: [Int], flipped: CardSnap, inFlightFromDeck: Int, inFlightToFlipped: Int, veiled: UInt64) {
        self.step = step
        self.elapsedMs = elapsedMs
        self.landed = landed
        self.nextMs = nextMs
        self.done = done
        self.deck = deck
        self.discard = discard
        self.hand = hand
        self.flipped = flipped
        self.inFlightFromDeck = inFlightFromDeck
        self.inFlightToFlipped = inFlightToFlipped
        self.veiled = veiled
    }
}
public func readAnimFrame(_ p: UnsafeRawPointer) throws -> AnimFrameSnap {
    let n_hand = Int(p.load(fromByteOffset: 60, as: Int32.self))
    if n_hand < 0 || n_hand > 8 { throw SGLayoutError.count(field: "AnimFrame.hand", got: n_hand, capacity: 8) }
    var hand: [Int] = []
    hand.reserveCapacity(n_hand)
    for i in 0..<n_hand { hand.append(Int(p.load(fromByteOffset: 28 + i * 4, as: Int32.self))) }
    return AnimFrameSnap(step: Int(p.load(fromByteOffset: 0, as: Int32.self)), elapsedMs: Int(p.load(fromByteOffset: 4, as: Int32.self)), landed: Int(p.load(fromByteOffset: 8, as: Int32.self)), nextMs: Int(p.load(fromByteOffset: 12, as: Int32.self)), done: Int(p.load(fromByteOffset: 16, as: Int32.self)), deck: Int(p.load(fromByteOffset: 20, as: Int32.self)), discard: Int(p.load(fromByteOffset: 24, as: Int32.self)), hand: hand, flipped: try readCard(p + 64), inFlightFromDeck: Int(p.load(fromByteOffset: 68, as: Int32.self)), inFlightToFlipped: Int(p.load(fromByteOffset: 72, as: Int32.self)), veiled: p.load(fromByteOffset: 80, as: UInt64.self))
}

/// AnimBeats (c struct), copied out of the kernel's storage.
public struct AnimBeatsSnap: Sendable, Equatable {
    /// sizeof(AnimBeats) under this build: what a host allocates to hand C one.
    public static let cSize = 6168
    public let beats: [AnimBeatSnap]
    public let placedIds: UInt64
    public let firstGoodMask: Int
    public init(beats: [AnimBeatSnap], placedIds: UInt64, firstGoodMask: Int) {
        self.beats = beats
        self.placedIds = placedIds
        self.firstGoodMask = firstGoodMask
    }
}
public func readAnimBeats(_ p: UnsafeRawPointer) throws -> AnimBeatsSnap {
    let n_beats = Int(p.load(fromByteOffset: 0, as: Int32.self))
    if n_beats < 0 || n_beats > 128 { throw SGLayoutError.count(field: "AnimBeats.beats", got: n_beats, capacity: 128) }
    var beats: [AnimBeatSnap] = []
    beats.reserveCapacity(n_beats)
    for i in 0..<n_beats { beats.append(try readAnimBeat(p + 8 + i * 48)) }
    return AnimBeatsSnap(beats: beats, placedIds: p.load(fromByteOffset: 6152, as: UInt64.self), firstGoodMask: Int(p.load(fromByteOffset: 6160, as: Int32.self)))
}

/// AnimSurfacePlan (c struct), copied out of the kernel's storage.
public struct AnimSurfacePlanSnap: Sendable, Equatable {
    /// sizeof(AnimSurfacePlan) under this build: what a host allocates to hand C one.
    public static let cSize = 252
    public let totalMs: Int
    public let settleMs: Int
    public let beats: [AnimSurfaceBeatSnap]
    public init(totalMs: Int, settleMs: Int, beats: [AnimSurfaceBeatSnap]) {
        self.totalMs = totalMs
        self.settleMs = settleMs
        self.beats = beats
    }
}
public func readAnimSurfacePlan(_ p: UnsafeRawPointer) throws -> AnimSurfacePlanSnap {
    let n_beats = Int(p.load(fromByteOffset: 0, as: Int32.self))
    if n_beats < 0 || n_beats > 10 { throw SGLayoutError.count(field: "AnimSurfacePlan.beats", got: n_beats, capacity: 10) }
    var beats: [AnimSurfaceBeatSnap] = []
    beats.reserveCapacity(n_beats)
    for i in 0..<n_beats { beats.append(try readAnimSurfaceBeat(p + 12 + i * 24)) }
    return AnimSurfacePlanSnap(totalMs: Int(p.load(fromByteOffset: 4, as: Int32.self)), settleMs: Int(p.load(fromByteOffset: 8, as: Int32.self)), beats: beats)
}

/// BotDriveOut (c struct), copied out of the kernel's storage.
public struct BotDriveOutSnap: Sendable, Equatable {
    /// sizeof(BotDriveOut) under this build: what a host allocates to hand C one.
    public static let cSize = 552
    public let actions: [BotDriveActionSnap]
    public let stop: Int
    public let ended: Int
    public init(actions: [BotDriveActionSnap], stop: Int, ended: Int) {
        self.actions = actions
        self.stop = stop
        self.ended = ended
    }
}
public func readBotDriveOut(_ p: UnsafeRawPointer) throws -> BotDriveOutSnap {
    let n_actions = Int(p.load(fromByteOffset: 540, as: Int32.self))
    if n_actions < 0 || n_actions > 9 { throw SGLayoutError.count(field: "BotDriveOut.actions", got: n_actions, capacity: 9) }
    var actions: [BotDriveActionSnap] = []
    actions.reserveCapacity(n_actions)
    for i in 0..<n_actions { actions.append(try readBotDriveAction(p + 0 + i * 60)) }
    return BotDriveOutSnap(actions: actions, stop: Int(p.load(fromByteOffset: 544, as: Int32.self)), ended: Int(p.load(fromByteOffset: 548, as: Int32.self)))
}

/// Roster (c struct), copied out of the kernel's storage.
public struct RosterSnap: Sendable, Equatable {
    /// sizeof(Roster) under this build: what a host allocates to hand C one.
    public static let cSize = 1235
    public let title: String
    public let seats: [RosterSeatSnap]
    public init(title: String, seats: [RosterSeatSnap]) {
        self.title = title
        self.seats = seats
    }
}
public func readRoster(_ p: UnsafeRawPointer) throws -> RosterSnap {
    let n_title = Int(p.load(fromByteOffset: 1, as: UInt8.self))
    if n_title < 0 || n_title > 201 { throw SGLayoutError.count(field: "Roster.title", got: n_title, capacity: 201) }
    let n_seats = Int(p.load(fromByteOffset: 0, as: Int8.self))
    if n_seats < 0 || n_seats > 8 { throw SGLayoutError.count(field: "Roster.seats", got: n_seats, capacity: 8) }
    var seats: [RosterSeatSnap] = []
    seats.reserveCapacity(n_seats)
    for i in 0..<n_seats { seats.append(try readRosterSeat(p + 203 + i * 129)) }
    return RosterSnap(title: sgUTF8(p, 2, n_title), seats: seats)
}

/// MsgHeader (c struct), copied out of the kernel's storage.
public struct MsgHeaderSnap: Sendable, Equatable {
    /// sizeof(MsgHeader) under this build: what a host allocates to hand C one.
    public static let cSize = 656
    public let e: MsgEnvelopeSnap
    public let digest: [Int]
    public init(e: MsgEnvelopeSnap, digest: [Int]) {
        self.e = e
        self.digest = digest
    }
}
public func readMsgHeader(_ p: UnsafeRawPointer) throws -> MsgHeaderSnap {
    var digest: [Int] = []
    digest.reserveCapacity(32)
    for i in 0..<32 { digest.append(Int(p.load(fromByteOffset: 624 + i, as: UInt8.self))) }
    return MsgHeaderSnap(e: try readMsgEnvelope(p + 0), digest: digest)
}

/// Card (c struct), copied out of the kernel's storage.
public struct CardSnap: Sendable, Equatable {
    /// sizeof(Card) under this build: what a host allocates to hand C one.
    public static let cSize = 1
    public let suit: Int
    public let value: Int
    public init(suit: Int, value: Int) {
        self.suit = suit
        self.value = value
    }
}
public func readCard(_ p: UnsafeRawPointer) throws -> CardSnap {
    return CardSnap(suit: Int(Int32(bitPattern: UInt32(p.load(fromByteOffset: 0, as: UInt8.self)) << 29) >> 29), value: Int(Int32(bitPattern: UInt32(p.load(fromByteOffset: 0, as: UInt8.self)) << 24) >> 27))
}

/// Battle (c struct), copied out of the kernel's storage.
public struct BattleSnap: Sendable, Equatable {
    /// sizeof(Battle) under this build: what a host allocates to hand C one.
    public static let cSize = 2
    public let attack: CardSnap
    public let defense: CardSnap
    public init(attack: CardSnap, defense: CardSnap) {
        self.attack = attack
        self.defense = defense
    }
}
public func readBattle(_ p: UnsafeRawPointer) throws -> BattleSnap {
    return BattleSnap(attack: try readCard(p + 0), defense: try readCard(p + 1))
}

/// ViewSeat (c struct), copied out of the kernel's storage.
public struct ViewSeatSnap: Sendable, Equatable {
    /// sizeof(ViewSeat) under this build: what a host allocates to hand C one.
    public static let cSize = 108
    public let status: Int
    public let handCount: Int
    public let awaitingAttack: Bool
    public let isAi: Bool
    public let id: String
    public let name: String
    public init(status: Int, handCount: Int, awaitingAttack: Bool, isAi: Bool, id: String, name: String) {
        self.status = status
        self.handCount = handCount
        self.awaitingAttack = awaitingAttack
        self.isAi = isAi
        self.id = id
        self.name = name
    }
}
public func readViewSeat(_ p: UnsafeRawPointer) throws -> ViewSeatSnap {
    let n_id = Int(p.load(fromByteOffset: 4, as: UInt8.self))
    if n_id < 0 || n_id > 37 { throw SGLayoutError.count(field: "ViewSeat.id", got: n_id, capacity: 37) }
    let n_name = Int(p.load(fromByteOffset: 5, as: UInt8.self))
    if n_name < 0 || n_name > 65 { throw SGLayoutError.count(field: "ViewSeat.name", got: n_name, capacity: 65) }
    return ViewSeatSnap(status: Int(p.load(fromByteOffset: 0, as: Int8.self)), handCount: Int(p.load(fromByteOffset: 1, as: Int8.self)), awaitingAttack: p.load(fromByteOffset: 2, as: UInt8.self) != 0, isAi: p.load(fromByteOffset: 3, as: UInt8.self) != 0, id: sgUTF8(p, 6, n_id), name: sgUTF8(p, 43, n_name))
}

/// AnimPlanStep (c struct), copied out of the kernel's storage.
public struct AnimPlanStepSnap: Sendable, Equatable {
    /// sizeof(AnimPlanStep) under this build: what a host allocates to hand C one.
    public static let cSize = 88
    public let type: Int
    public let seat: Int
    public let from: Int
    public let to: Int
    public let nCards: Int
    public let durationMs: Int
    public let startMs: Int
    public let deck: Int
    public let discard: Int
    public let hand: [Int]
    public let inFlightFromDeck: Int
    public let inFlightToFlipped: Int
    public let reveals: UInt64
    public init(type: Int, seat: Int, from: Int, to: Int, nCards: Int, durationMs: Int, startMs: Int, deck: Int, discard: Int, hand: [Int], inFlightFromDeck: Int, inFlightToFlipped: Int, reveals: UInt64) {
        self.type = type
        self.seat = seat
        self.from = from
        self.to = to
        self.nCards = nCards
        self.durationMs = durationMs
        self.startMs = startMs
        self.deck = deck
        self.discard = discard
        self.hand = hand
        self.inFlightFromDeck = inFlightFromDeck
        self.inFlightToFlipped = inFlightToFlipped
        self.reveals = reveals
    }
}
public func readAnimPlanStep(_ p: UnsafeRawPointer) throws -> AnimPlanStepSnap {
    var hand: [Int] = []
    hand.reserveCapacity(8)
    for i in 0..<8 { hand.append(Int(p.load(fromByteOffset: 36 + i * 4, as: Int32.self))) }
    return AnimPlanStepSnap(type: Int(p.load(fromByteOffset: 0, as: Int32.self)), seat: Int(p.load(fromByteOffset: 4, as: Int32.self)), from: Int(p.load(fromByteOffset: 8, as: Int32.self)), to: Int(p.load(fromByteOffset: 12, as: Int32.self)), nCards: Int(p.load(fromByteOffset: 16, as: Int32.self)), durationMs: Int(p.load(fromByteOffset: 20, as: Int32.self)), startMs: Int(p.load(fromByteOffset: 24, as: Int32.self)), deck: Int(p.load(fromByteOffset: 28, as: Int32.self)), discard: Int(p.load(fromByteOffset: 32, as: Int32.self)), hand: hand, inFlightFromDeck: Int(p.load(fromByteOffset: 68, as: Int32.self)), inFlightToFlipped: Int(p.load(fromByteOffset: 72, as: Int32.self)), reveals: p.load(fromByteOffset: 80, as: UInt64.self))
}

/// AnimCounts (c struct), copied out of the kernel's storage.
public struct AnimCountsSnap: Sendable, Equatable {
    /// sizeof(AnimCounts) under this build: what a host allocates to hand C one.
    public static let cSize = 312
    public let deck: Int
    public let discard: Int
    public let hand: [Int]
    public let nBattles: Int
    public let battles: [Int]
    public let paired: Int
    public let flipped: CardSnap
    public init(deck: Int, discard: Int, hand: [Int], nBattles: Int, battles: [Int], paired: Int, flipped: CardSnap) {
        self.deck = deck
        self.discard = discard
        self.hand = hand
        self.nBattles = nBattles
        self.battles = battles
        self.paired = paired
        self.flipped = flipped
    }
}
public func readAnimCounts(_ p: UnsafeRawPointer) throws -> AnimCountsSnap {
    let n_hand = Int(p.load(fromByteOffset: 40, as: Int32.self))
    if n_hand < 0 || n_hand > 8 { throw SGLayoutError.count(field: "AnimCounts.hand", got: n_hand, capacity: 8) }
    var hand: [Int] = []
    hand.reserveCapacity(n_hand)
    for i in 0..<n_hand { hand.append(Int(p.load(fromByteOffset: 8 + i * 4, as: Int32.self))) }
    var battles: [Int] = []
    battles.reserveCapacity(256)
    for i in 0..<256 { battles.append(Int(p.load(fromByteOffset: 48 + i, as: UInt8.self))) }
    return AnimCountsSnap(deck: Int(p.load(fromByteOffset: 0, as: Int32.self)), discard: Int(p.load(fromByteOffset: 4, as: Int32.self)), hand: hand, nBattles: Int(p.load(fromByteOffset: 44, as: Int32.self)), battles: battles, paired: Int(p.load(fromByteOffset: 304, as: Int32.self)), flipped: try readCard(p + 308))
}

/// AnimBeat (c struct), copied out of the kernel's storage.
public struct AnimBeatSnap: Sendable, Equatable {
    /// sizeof(AnimBeat) under this build: what a host allocates to hand C one.
    public static let cSize = 48
    public let first: Int
    public let nEvents: Int
    public let type: Int
    public let seat: Int
    public let flags: Int
    public let outsMask: Int
    public let attackPassSeats: Int
    public let placedIds: UInt64
    public let goodMask: Int
    public init(first: Int, nEvents: Int, type: Int, seat: Int, flags: Int, outsMask: Int, attackPassSeats: Int, placedIds: UInt64, goodMask: Int) {
        self.first = first
        self.nEvents = nEvents
        self.type = type
        self.seat = seat
        self.flags = flags
        self.outsMask = outsMask
        self.attackPassSeats = attackPassSeats
        self.placedIds = placedIds
        self.goodMask = goodMask
    }
}
public func readAnimBeat(_ p: UnsafeRawPointer) throws -> AnimBeatSnap {
    return AnimBeatSnap(first: Int(p.load(fromByteOffset: 0, as: Int32.self)), nEvents: Int(p.load(fromByteOffset: 4, as: Int32.self)), type: Int(p.load(fromByteOffset: 8, as: Int32.self)), seat: Int(p.load(fromByteOffset: 12, as: Int32.self)), flags: Int(p.load(fromByteOffset: 16, as: Int32.self)), outsMask: Int(p.load(fromByteOffset: 20, as: UInt32.self)), attackPassSeats: Int(p.load(fromByteOffset: 24, as: UInt32.self)), placedIds: p.load(fromByteOffset: 32, as: UInt64.self), goodMask: Int(p.load(fromByteOffset: 40, as: Int32.self)))
}

/// AnimSurfaceBeat (c struct), copied out of the kernel's storage.
public struct AnimSurfaceBeatSnap: Sendable, Equatable {
    /// sizeof(AnimSurfaceBeat) under this build: what a host allocates to hand C one.
    public static let cSize = 24
    public let kind: Int
    public let transition: Int
    public let passing: Int
    public let controls: Int
    public let durationMs: Int
    public let startMs: Int
    public init(kind: Int, transition: Int, passing: Int, controls: Int, durationMs: Int, startMs: Int) {
        self.kind = kind
        self.transition = transition
        self.passing = passing
        self.controls = controls
        self.durationMs = durationMs
        self.startMs = startMs
    }
}
public func readAnimSurfaceBeat(_ p: UnsafeRawPointer) throws -> AnimSurfaceBeatSnap {
    return AnimSurfaceBeatSnap(kind: Int(p.load(fromByteOffset: 0, as: Int32.self)), transition: Int(p.load(fromByteOffset: 4, as: Int32.self)), passing: Int(p.load(fromByteOffset: 8, as: Int32.self)), controls: Int(p.load(fromByteOffset: 12, as: Int32.self)), durationMs: Int(p.load(fromByteOffset: 16, as: Int32.self)), startMs: Int(p.load(fromByteOffset: 20, as: Int32.self)))
}

/// BotDriveAction (c struct), copied out of the kernel's storage.
public struct BotDriveActionSnap: Sendable, Equatable {
    /// sizeof(BotDriveAction) under this build: what a host allocates to hand C one.
    public static let cSize = 60
    public let seat: Int
    public let pacingClass: Int
    public let move: LegalMoveSnap
    public init(seat: Int, pacingClass: Int, move: LegalMoveSnap) {
        self.seat = seat
        self.pacingClass = pacingClass
        self.move = move
    }
}
public func readBotDriveAction(_ p: UnsafeRawPointer) throws -> BotDriveActionSnap {
    return BotDriveActionSnap(seat: Int(p.load(fromByteOffset: 0, as: Int8.self)), pacingClass: Int(p.load(fromByteOffset: 1, as: UInt8.self)), move: try readLegalMove(p + 2))
}

/// RosterSeat (c struct), copied out of the kernel's storage.
public struct RosterSeatSnap: Sendable, Equatable {
    /// sizeof(RosterSeat) under this build: what a host allocates to hand C one.
    public static let cSize = 129
    public let id: String
    public let name: String
    public let brain: String
    public init(id: String, name: String, brain: String) {
        self.id = id
        self.name = name
        self.brain = brain
    }
}
public func readRosterSeat(_ p: UnsafeRawPointer) throws -> RosterSeatSnap {
    let n_id = Int(p.load(fromByteOffset: 0, as: UInt8.self))
    if n_id < 0 || n_id > 37 { throw SGLayoutError.count(field: "RosterSeat.id", got: n_id, capacity: 37) }
    let n_name = Int(p.load(fromByteOffset: 1, as: UInt8.self))
    if n_name < 0 || n_name > 65 { throw SGLayoutError.count(field: "RosterSeat.name", got: n_name, capacity: 65) }
    let n_brain = Int(p.load(fromByteOffset: 2, as: UInt8.self))
    if n_brain < 0 || n_brain > 24 { throw SGLayoutError.count(field: "RosterSeat.brain", got: n_brain, capacity: 24) }
    return RosterSeatSnap(id: sgUTF8(p, 3, n_id), name: sgUTF8(p, 40, n_name), brain: sgUTF8(p, 105, n_brain))
}

/// MsgEnvelope (c struct), copied out of the kernel's storage.
public struct MsgEnvelopeSnap: Sendable, Equatable {
    /// sizeof(MsgEnvelope) under this build: what a host allocates to hand C one.
    public static let cSize = 624
    public let format: Int
    public let flags: Int
    public let phase: Int
    public let gameId: UInt64
    public let turn: Int
    public let lastActorSeat: Int
    public let nPlayers: Int
    public let variant: Int
    public let round: Int
    public let parent8: [Int]
    public let seed: [Int]
    public let sentAt: Int
    public let nNew: Int
    public let opening: Int
    public let carryKey: Int
    public let carryFool: Int
    public let joins: [MsgJoinSnap]
    public init(format: Int, flags: Int, phase: Int, gameId: UInt64, turn: Int, lastActorSeat: Int, nPlayers: Int, variant: Int, round: Int, parent8: [Int], seed: [Int], sentAt: Int, nNew: Int, opening: Int, carryKey: Int, carryFool: Int, joins: [MsgJoinSnap]) {
        self.format = format
        self.flags = flags
        self.phase = phase
        self.gameId = gameId
        self.turn = turn
        self.lastActorSeat = lastActorSeat
        self.nPlayers = nPlayers
        self.variant = variant
        self.round = round
        self.parent8 = parent8
        self.seed = seed
        self.sentAt = sentAt
        self.nNew = nNew
        self.opening = opening
        self.carryKey = carryKey
        self.carryFool = carryFool
        self.joins = joins
    }
}
public func readMsgEnvelope(_ p: UnsafeRawPointer) throws -> MsgEnvelopeSnap {
    let n_joins = Int(p.load(fromByteOffset: 76, as: Int32.self))
    if n_joins < 0 || n_joins > 8 { throw SGLayoutError.count(field: "MsgEnvelope.joins", got: n_joins, capacity: 8) }
    var parent8: [Int] = []
    parent8.reserveCapacity(8)
    for i in 0..<8 { parent8.append(Int(p.load(fromByteOffset: 22 + i, as: UInt8.self))) }
    var seed: [Int] = []
    seed.reserveCapacity(32)
    for i in 0..<32 { seed.append(Int(p.load(fromByteOffset: 30 + i, as: UInt8.self))) }
    var joins: [MsgJoinSnap] = []
    joins.reserveCapacity(n_joins)
    for i in 0..<n_joins { joins.append(try readMsgJoin(p + 80 + i * 66)) }
    return MsgEnvelopeSnap(format: Int(p.load(fromByteOffset: 0, as: UInt8.self)), flags: Int(p.load(fromByteOffset: 1, as: UInt8.self)), phase: Int(p.load(fromByteOffset: 2, as: UInt8.self)), gameId: p.load(fromByteOffset: 8, as: UInt64.self), turn: Int(p.load(fromByteOffset: 16, as: UInt16.self)), lastActorSeat: Int(p.load(fromByteOffset: 18, as: UInt8.self)), nPlayers: Int(p.load(fromByteOffset: 19, as: UInt8.self)), variant: Int(p.load(fromByteOffset: 20, as: UInt8.self)), round: Int(p.load(fromByteOffset: 21, as: UInt8.self)), parent8: parent8, seed: seed, sentAt: Int(p.load(fromByteOffset: 62, as: UInt16.self)), nNew: Int(p.load(fromByteOffset: 64, as: UInt8.self)), opening: Int(p.load(fromByteOffset: 65, as: UInt8.self)), carryKey: Int(p.load(fromByteOffset: 68, as: UInt32.self)), carryFool: Int(p.load(fromByteOffset: 72, as: UInt8.self)), joins: joins)
}

/// LegalMove (c struct), copied out of the kernel's storage.
public struct LegalMoveSnap: Sendable, Equatable {
    /// sizeof(LegalMove) under this build: what a host allocates to hand C one.
    public static let cSize = 58
    public let type: Int
    public let cards: [CardSnap]
    public let attackCards: [CardSnap]
    public init(type: Int, cards: [CardSnap], attackCards: [CardSnap]) {
        self.type = type
        self.cards = cards
        self.attackCards = attackCards
    }
}
public func readLegalMove(_ p: UnsafeRawPointer) throws -> LegalMoveSnap {
    let n_cards = Int(p.load(fromByteOffset: 1, as: Int8.self))
    if n_cards < 0 || n_cards > 28 { throw SGLayoutError.count(field: "LegalMove.cards", got: n_cards, capacity: 28) }
    let n_attack_cards = Int(p.load(fromByteOffset: 1, as: Int8.self))
    if n_attack_cards < 0 || n_attack_cards > 28 { throw SGLayoutError.count(field: "LegalMove.attack_cards", got: n_attack_cards, capacity: 28) }
    var cards: [CardSnap] = []
    cards.reserveCapacity(n_cards)
    for i in 0..<n_cards { cards.append(try readCard(p + 2 + i)) }
    var attackCards: [CardSnap] = []
    attackCards.reserveCapacity(n_attack_cards)
    for i in 0..<n_attack_cards { attackCards.append(try readCard(p + 30 + i)) }
    return LegalMoveSnap(type: Int(p.load(fromByteOffset: 0, as: Int8.self)), cards: cards, attackCards: attackCards)
}

/// MsgJoin (c struct), copied out of the kernel's storage.
public struct MsgJoinSnap: Sendable, Equatable {
    /// sizeof(MsgJoin) under this build: what a host allocates to hand C one.
    public static let cSize = 66
    public let seat: Int
    public let name: String
    public init(seat: Int, name: String) {
        self.seat = seat
        self.name = name
    }
}
public func readMsgJoin(_ p: UnsafeRawPointer) throws -> MsgJoinSnap {
    let n_name = Int(p.load(fromByteOffset: 1, as: UInt8.self))
    if n_name < 0 || n_name > 64 { throw SGLayoutError.count(field: "MsgJoin.name", got: n_name, capacity: 64) }
    return MsgJoinSnap(seat: Int(p.load(fromByteOffset: 0, as: UInt8.self)), name: sgUTF8(p, 2, n_name))
}
