# datagen: one C file on libclang, built exactly the way structgen is - same
# library, same discovery, same CFLAGS (tools/llvm.mk).
include ../llvm.mk

datagen: build/datagen
.PHONY: datagen
.DEFAULT_GOAL := datagen
