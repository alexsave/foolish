[package]
name = "rsparse"
version = "0.1.0"
edition = "2024"

[dependencies]
proc-macro2 = { version = "1.0.107", features = ["span-locations"] }
serde_json = "1.0.151"
syn = { version = "3.0.5", default-features = false, features = ["full", "extra-traits", "visit", "parsing", "clone-impls", "printing", "proc-macro"] }
